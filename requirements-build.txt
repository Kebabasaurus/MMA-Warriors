# Exact, offline build toolchain recorded by build-toolchain.json.
# Build scripts verify these versions and never install packages automatically.
PyInstaller==6.20.0
pyinstaller-hooks-contrib==2026.5
packaging==24.2
setuptools==76.1.0
