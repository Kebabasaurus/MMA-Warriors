# Changelog

### Grand Prix lifecycle and reviewable delivery boundary — 18 September 2026

- Added an end-to-end four-card Grand Prix regression through the real atomic
  event settlement owner, covering unique stage identities, advancing fighters,
  JSON save/reload shape, final champion completion, committed retries and
  injected state/RNG rollback. The required nine-suite gate passes.
- Made root `MMA Warriors.spec` the game package authority actually invoked by
  `Build Portable.bat`. It owns the entry point, assets, output and dependency
  policy; the prior optional-module exclusions were not silently activated.
- Added a machine-readable, per-file hashed source-delivery manifest plus tools
  to refresh it and validate an isolated external copy. Active careers/logs,
  generated packages, caches and blanket analysis history are explicitly
  excluded; required source, assets, database, tests, fixtures and contracts are
  classified. The analysis index and authoritative feature ledger remain explicit
  delivery dependencies while unrelated generated analysis and local universe
  selection state are excluded. Text identities normalize checkout line endings,
  so the same commit validates from LF or CRLF worktrees. A 758-file external snapshot passed hashes, import/script/fixture,
  packaging, syntax, smoke, Grand Prix and QA checks.
- Reconciled the feature backlog: P1.4 remains complete, P2.2/L0 clearly means
  delivered audit tooling rather than a fresh century qualification, Chronicle
  export is not rescheduled, and the finite Luna queue owns this closeout.
- The final source-bound canonical run passed all 187 registered suites and
  routed four generated audit artifacts outside the repository. Its 344-file
  Python source digest is retained in the run summary; no EXE or century audit
  was run.

### Durable regression evidence and complete root-test inventory — 18 September 2026

- Added optional unique, atomic runner summaries with the exact selection,
  interpreter, current Python-source fingerprint, per-suite timing/status and
  truthful failed/interrupted/not-run outcomes. Corrected the old failure message
  to state that isolated runtime data is removed.
- Registered the seven previously omitted root tests and added an inventory gate
  requiring every runnable root test to be registered or explicitly excluded.
- Preserved earlier portrait review artifacts and added source-current review
  sheets/manifests for the four ranked cohorts. The 50-test main portrait suite
  and the other six newly registered tests pass against current authored layers.
- Added explicit output paths to the three move-expansion comparisons. Retained
  runner mode now gives those and move coverage a unique per-run artifact folder
  with hashes in the run summary, preventing routine overwrite of prior evidence.

### Media plan and account-review identity fixes — 18 September 2026

- Enforced cumulative managed-campaign spending ceilings from actual nested
  receipt costs. Capped plans fail closed on incomplete prior cost evidence before
  cash, capacity or campaign effects.
- Disambiguated same-named event, fighter and sponsor targets through an explicit
  display-to-source map. Refresh preserves the same source ID and clears a
  departed selection; duplicate IDs stay visible but unselectable until an
  explicit sanctioned save/migration boundary.
- Scoped Media Rights account review to the active contract ID, preventing a
  predecessor at the same outlet from contributing deliveries or shortfalls.
- Added focused regressions. The eight-suite Media/Foundation/finance/UI gate and
  an isolated native Tk duplicate-selector check passed.

### Luna handoff readiness review — 18 September 2026

- Clarified the next-work report's full ten-card execution scope, dependencies,
  resumable checkpoint, concrete verification commands and combined closeout.
  Removed the example instruction that stopped after only the three Media fixes.
- Added bounded blocker handling, safer accounting/identity acceptance detail
  and explicit snapshot/packaging deliverables. Optional features and executable
  builds remain outside scope. Documentation only; no runtime fixes or new
  game-test passes are claimed by this review.

### Current audit and implementation handoff — 18 September 2026

- Added `docs/LUNA_MAX_NEXT_WORK_PLAN.md`: three reproduced Media defects,
  focused verification/tooling and delivery gaps, current architecture, an
  ordered implementation queue, and explicit acceptance/stop rules for Luna.
- Linked the report from the quick start and developer status/index maps.
  Distinguished accepted features and audit tooling from optional expansions
  and unperformed century/release qualification.
- Audit/documentation only: ten selected isolated suites passed; runtime fixes,
  full release certification, user-save changes and executable builds are not
  claimed or performed by this change.

### Focused developer context — 18 September 2026

- Replaced the large always-read `AGENTS.md` with a compact workflow and routing
  guide. Preserved all 25 original sections in indexed developer contracts, with
  byte-for-byte source snapshots and extraction hashes for verification; exact-path
  Git attributes protect those snapshots from newline normalization.
- Made the root README a quick start and moved its detailed feature explanations
  to `docs/PLAYER_GUIDE.md`, updating relative links and retaining original bytes
  in the archive. Added architecture, state-ownership, testing, current-status
  pointers and analysis-evidence maps for focused lookups.
- Developer updates now go to the owning linked contract; universal instructions
  stay in `AGENTS.md`. This replaces repeated feature prose across root documents.
  Gameplay, save formats, tests, source-module locations and executables are unchanged.

### Documentation cleanup — 18 September 2026

- Archived eight older reviews, proposals and handoff notes under
  `docs/archive/2026-09-18`, preserving their contents and original-path inventory.
- Added `docs/README.md` as the current documentation index and redirected the
  feature backlog's historical handoff references. Active plans, technical
  contracts, licences and verification evidence retain their existing locations.
- Documentation only; no game, save, database or executable changes.

### 3.0.10 — Grand Prix and child-scheduling release extension

- Added player-planned multi-event Grand Prix competitions for 4, 8 or 16
  entrants. A bracket can occupy 1–4 event cards within its natural round
  limits, with the opening date selected by the player and later stages placed
  on the next best available date after settlement.
- Injury and confirmed drug-failure withdrawals use normal matchmaking
  eligibility for replacements while preserving the bracket's division,
  gender, title state and slot. If no eligible replacement exists, the whole
  competition moves together; preliminary positives do not trigger a move.
- Tournament-only draws now enter a 15-minute decider and judges select the
  winner if the decider reaches its limit. Ordinary non-tournament draws are
  unchanged. AI promotions can schedule a separate rare Grand Prix at most
  once per year without changing ordinary card scheduling.
- Added persistent series identity, stage progression, replacement and
  postponement evidence, save/load migration, UI event-count selection and
  dedicated regression coverage. The maintained shipping suite remains green;
  no user save is modified by verification.

### G5 closeout — approved expansion scope

- G5 is closed for the approved first-playable scope. The delivered bounded
  slices cover the Testing Desk catalogue and frozen preliminary cases,
  resumable title-miss decisions and replacement/rank/title-lineage evidence,
  Academy Coach v1, company proposals/ownership history v1, one-night
  tournament history, regional feasibility/host invitations, spectator pause
  policies and pinned checkpoints, editor preflight/conversion, and the
  source-bound L0 audit with retained reports/manifests and read-only history.
- The focused G5 verification run passes **164 tests** and the maintained
  regression runner reports `ALL REQUESTED ISOLATED REGRESSION SUITES PASSED`.
  The expected headless Matchmaking coordinate probe remains the documented
  environment skip. No EXE or user save changed.
- Active confirmation/appeal/sanction tables, concurrent rights/audience-
  segment economics, and century-scale qualification remain explicit follow-up
  decisions. Multi-event Grand Prix and the approved child-scheduling
  extension are now covered by the release entry above.

### J5 testing policy decisions recorded

- The player controls testing frequency; AI and spectator promotions use the
  same process only a few times per year. Preliminary positives have seven
  days to reach confirmation, followed by a seven-day appeal window paid by the
  fighter.
- For a first confirmed offence the player chooses a warning or time
  suspension. A repeat offence receives a longer suspension and removes the
  fighter from the company. Any effect on a booked fight, title or recorded
  result stops for an explicit player decision rather than applying
  automatically.
- Provider tiers, effectiveness/cost curves and bounded suspension durations
  remain implementation tuning; no result or sporting record may change
  without the explicit decision boundary.

### Stage 4 closeout — approved first-playable scope

- Stage 4 is complete for G2/G3/G4: the shared management/read-model loop,
  commercial lifecycle and Staff/AI autonomy/employment contracts are covered
  by the maintained regression, smoke, stability, native-window, persistence,
  UI-data and long-run evidence suites. The maintained runner finished with
  `ALL REQUESTED ISOLATED REGRESSION SUITES PASSED`; compilation and diff checks
  are clean. The expected headless Matchmaking coordinate probe remains the
  documented environment skip.
- One optional gate remains: G5 policy/calibration work (expanded testing
  confirmation/appeals/sanctions, broader child/tournament/rights economics and
  century-scale qualification). It is not required for the approved first
  playable upgrade and remains explicitly held. No EXE or user save changed.

### A6/A7 recruitment packs and followed stories accepted

- The approved Scouting Decision Packs v1 slice is complete: two-to-four
  identity-linked candidates, explicit purpose/notes/state, immutable dossier
  snapshots with current availability warnings, deterministic legacy IDs and
  read-only refreshes. It does not reveal hidden ratings, request free reports,
  sign candidates, spend cash or consume scouting RNG.
- The approved Followed Stories/Story Briefings v1 slice is complete: durable
  thread/fighter/company follows, explicit acknowledgement cursors, snooze and
  unfollow retention, grouped recorded beats, and honest expired-cursor coverage
  gaps. Readers never generate narrative or mutate subscriptions; malformed and
  duplicate legacy rows remain safe. Focused A6/A7 coverage passes 12 tests and
  the maintained full regression runner is green. No EXE or user save changed.

### A5 relationship cases accepted

- The approved Talent Relations v1 slice is complete: source-linked cases for
  existing Contract Promises and Career Journeys, stable fighter/case identity,
  evidence-backed partial delivery, truthful transfer/departure handling,
  duplicate-safe legacy IDs, and idempotent acknowledge/action/review controls.
  Case readers remain non-mutating and do not grant morale, trust, title merit,
  deadline, cash or RNG effects; existing authorised support workflows remain
  the only effect boundary. Relationship, Story Briefing, Staff and UI-data
  coverage is green, including the focused 78-test run and the maintained full
  regression runner. New relationship formulas, negotiated extensions and
  punitive rules remain gated follow-ups. No EXE or user save was changed.

### Stage 4 integrated verification

- The maintained `run_regression_suite.py` completed with
  `ALL REQUESTED ISOLATED REGRESSION SUITES PASSED`, including Fight Night,
  traits/profile/persistence, UI-data, Media, Staff, finance, scouting,
  regional, Combat Sports, Foundation, tournament, booking, J5 title/testing,
  L0 audit/checkpoints, native release, audio/window lifecycle, fight-engine,
  database-editor and stability coverage. The expected headless Matchmaking
  coordinate probe remains the documented skip. No EXE or user save was
  changed.

### J5/L0 accepted built scope

- The user accepted the currently built J5 and L0 implementation scope. J5
  covers the player-owned Testing Desk catalogue/quote and frozen case
  evidence, title-miss decisions, last-minute replacement identity/rank
  snapshots and settlement lineage. L0 covers the isolated source-bound audit
  runner, bounded seed/time controls, checkpoints, source-mismatch refusal,
  raw reports, manifest history and read-only diagnostics. Optional active
  confirmation/appeal/sanction tables and century-scale qualification remain
  explicitly gated follow-ups rather than being silently enabled. The combined
  current J5/title/replacement/audit verification run passes 158 tests; no EXE
  or user save was changed.

### J5 named-holder review wording

- Upcoming Cards and archived Results now label a saved named-belt holder
  directly in the title-eligibility line, alongside win/retain eligibility.
  The wording is sourced only from the retained snapshot and remains
  `unknown` for legacy rows without the field. Focused
  profile/title/replacement/audit coverage remains **114 tests**.

### J5 special-belt settlement role evidence

- Settlement evidence now reads the saved named-belt-holder role, including
  legacy snapshots whose generic champion flags are false. A holder's
  successful defence is recorded as `retained` rather than a new title win;
  challenger and unknown-role outcomes remain distinct. This is an
  evidence-only correction and does not enable new sanction rules. Focused
  profile/title/replacement/audit coverage passes **114 tests**.

### J5 special-belt eligibility evidence

- Title-miss snapshots now distinguish a named special-belt holder from a
  generic champion flag. The holder is recorded as eligible to retain and to
  win, with a stable `special_belt_holder` evidence field; challengers still
  use the existing merit check. Review and settlement readers retain this
  distinction without enabling new sanction rules. Focused
  profile/title/replacement/audit coverage passes **113 tests**.

### J5 named-belt departure lineage

- Special-belt vacancy now runs at every player, feeder and AI departure
  boundary (loan, transfer, academy return, retirement, expiry, cuts, swaps
  and regional signing). The vacancy writer accepts an explicit promotion
  owner, records the holder ID/reason in that promotion's lineage and removes
  only the titles actually vacated from the fighter. Child exits therefore
  cannot mutate the player's belt catalogue or leave a named belt attached to
  a departed fighter; AI holder-protection also recognises named-belt holders.
  Focused profile/title/replacement/audit coverage passes **113 tests** and
  the affected departure compatibility set passes **124 tests**.

### J5 narrow title-stakes weigh-in parity

- Live title-miss handling now applies to divisional-title and named
  special-belt bouts as well as the broad `title` flag. Their scale limit,
  explicit player decision, replacement review and retained sanction state
  stay aligned through save/reload; replacement success keeps the title on the
  line unless a champion corner was explicitly replaced. Focused
  title/replacement coverage passes **73 tests**.

### J5 per-corner title eligibility evidence

- Saved title-miss snapshots now retain explicit `eligible_to_win` and
  `eligible_to_retain` facts for each recorded corner. Review and archived
  Results expose those values as evidence (or `unknown` for legacy rows)
  without recomputing today's rankings or changing sanction rules. Focused
  title/replacement/audit coverage passes **73 tests**.

### J5 observed title-settlement evidence

- Official result settlement now appends the observed title outcome, method,
  winner/loser identities, before/after holder flags and reason to the retained
  sanction snapshot and matching fight log. The merge preserves belt scope and
  per-corner evidence; legacy logs are never matched by list position or a
  duplicate display name. Review and archived Results expose the settlement
  line without rerunning any rule. Focused title/replacement/audit coverage
  passes **73 tests**.

### J5 explicit sanction envelope scope

- Decision-branch sanction snapshots now retain `scheduled_title`, `belt_id`
  and `title_key` alongside the original miss and eligibility evidence. The
  fields are additive and preserve legacy rows; no current division or
  ranking is inferred when the source did not retain a scope. Focused
  title/replacement/audit coverage remains **73 tests**.

### J5 malformed sanction settlement guard

- Settlement evidence now fails closed around malformed legacy sanction
  envelopes. A wrong-type retained snapshot is left untouched while the
  separate settlement evidence and stable-ID fight-log path remain available;
  no otherwise valid card is aborted merely because an old field cannot accept
  the additive evidence. Focused coverage remains **73 tests**.

### J4/J5 Fight History title evidence

- Profile Fight History now carries retained title-decision and official
  settlement evidence into its read-only detail card, alongside the existing
  at-the-time rankings, records and replay provenance. Missing legacy evidence
  remains unavailable; current fighter state is never substituted. The
  focused profile/title/replacement/audit set passes **111 tests**.

### J5 special-belt holder replacement guard

- Last-minute replacement now identifies a named special-belt holder by the
  saved holder ID (or a unique legacy name) instead of relying only on the
  generic champion flag. Replacing that holder removes the special belt from
  the bout and records it as not contested; duplicate-name legacy holders fail
  closed. Focused profile/title/replacement/audit coverage passes **111 tests**.

### J5 stable belt scope

- Title-miss snapshots now retain an explicit belt ID when available and a
  stable division/special-belt scope for later history and settlement joins.
  Upcoming review and archived Results display that scope without inferring a
  legacy belt from a fighter's current division or changing the saved event.
  Focused title coverage remains **32 tests**.

### J5 pending title-state wording

- Pending title-miss cards now display **Title scheduled; decision pending** in
  review and archived Results views. They no longer appear as not contested
  merely because final sanction settlement has not happened. Focused coverage
  passes **33 tests**.

### J5 pending-stakes identity

- Title decisions now recognise divisional and named special-belt stakes even
  when a legacy fight omitted the broad `title` flag. Modern snapshots retain
  their stable title scope; legacy readers never infer it from a fighter's
  current division. Focused title coverage passes **35 tests**.

### J5 replacement rank snapshots

- Last-minute replacement history now retains a pre-bout ranking envelope for
  the removed corner, incoming replacement and retained opponent, including
  identity, employer, company/world rank and capture date. Free agents show no
  fictional company rank before signing. Focused replacement/title coverage
  passes **17 tests**.

### J5 title-merit parity for replacements

- Last-minute replacement filtering and commit validation now treat
  `title`, `divisional_title`, and named special belts consistently. Legacy
  title bookings can no longer bypass challenger merit through the emergency
  dropdown. Focused replacement/title coverage passes **17 tests**.

### L0 audit budget visibility

- Simulation Lab play-audit progress now shows elapsed seconds and remaining
  time beside the year/week percentage, using the same monotonic bounded
  budget as the stop decision. It does not affect checkpoints or simulation
  state. L0 regression coverage passes **36 tests** without a long run.

### J5 archived decision evidence

- Results preparation now displays retained title-miss permitted choices and
  recorded miss amounts alongside action/status, title state, reason and any
  replacement identity. It uses the same defensive projection as Upcoming
  Cards and archives without recalculating or mutating the event. Focused
  title/replacement/preparation coverage remains **32 tests**.

### L0 bounded audit clock contract

- Play-audit timeout detection now uses one tested monotonic helper, applying
  the same 30–1,800 second bounds as the Simulation Lab. Invalid clock
  metadata fails closed, and the runner still stops only after a completed
  weekly step so its checkpoint remains exact. The L0 regression suite passes
  **35 tests** without running a long simulation.

### J5 legacy sanction discoverability

- Upcoming Cards now uses the central title-decision reader, including a safe
  fallback for view-only callers. A retained `review_required` sanction
  envelope without a separate decision-state row is surfaced as **Review
  required** after reload; terminal sanctions remain ordinary. The reader is
  non-mutating and targeted UI/title coverage passes **15 tests**.

### J5 title-decision review surface

- Upcoming Cards now has a read-only **Review Title Decision** panel for
  saved weigh-in interruptions. It lists affected bouts, recorded corners,
  status/action, title state, replacement identity, reason and permitted
  choices from the central source-bound projection. Opening it never reruns
  weigh-ins, resolves rankings, settles the card or mutates the event. The
  focused title/replacement/preparation suite passes **32 tests** and the
  affected UI/data set passes **226 tests**; no EXE or user save was rebuilt or
  modified.

### J5 central title-decision read model

- Results, Upcoming Cards and archives now consume one defensive projection of
  retained title-miss decisions, including stable fighter references, allowed
  choices, action/status, title flags, replacement identity and missed-corner
  evidence. Legacy rows are labelled without inventing durable fight IDs. The
  title/replacement/preparation suite passes **31 tests**; no EXE or user save
  was rebuilt or modified.

### J5 frozen workflow contract

- Case workflow projections now carry a defensive copy of the provider,
  policy, version, sample-count and quote terms accepted at commission. Case
  review and future confirmation/appeal handlers can share those historical
  facts without rereading mutable settings or re-quoting a case. The catalogue
  suite passes **31 tests**; no EXE or user save was rebuilt or modified.

### J5 pending-card discoverability

- Upcoming Cards now marks a saved title-miss interruption as `Decision
  required` or `Review required` using retained participant references. The
  reader does not rerun weigh-ins, resolve rankings or mutate the scheduled
  event, so a save/reload interruption remains visible before Watch or settle.
  UI/data coverage passes **238 tests**; no EXE or user save was rebuilt or
  modified.

### J5 commission approval preview

- The Staff Testing Desk now makes manual testing an explicit approval flow.
  Before committing, the player sees provider, policy, effective sample count,
  quoted maximum, staff-discount estimate, projected cash impact, coverage and
  blockers. The preview is read-only; only the confirmed manual action can
  collect samples, charge cash or create preliminary evidence. No injury,
  proven violation or sanction mechanics were enabled. The confirmation step
  rejects a stale provider, policy, quote, cash or roster state after the
  dialog closes. The focused J5 set passes **106 tests**; no EXE or user save
  was rebuilt or modified.

### J5 frozen-settings visibility

- Testing case rows now make prospective configuration changes explicit: the
  ledger identifies when the current provider or policy differs from the
  provider/policy frozen at commission, while retaining the original quote and
  evidence. Missing current settings fail closed without repairing the saved
  envelope or rerunning a case. The focused J5/provider/replacement/title/audit
  set passes **103 tests**; no EXE or user save was rebuilt or modified.

### L0 current-source checkpoint

- Fresh seed `260720` has reached the first yearly boundary at **48/96
  observer weeks**. The retained checkpoint carries the complete 21-file
  runtime inventory, source digest `5fe46d42117f8b0d0a18c510516e0f36e52f569a32bf04cb8e816254d9e24b37`,
  and an empty hard-invariant list. The second year is still running; this is
  partial evidence only and does not close the multi-seed, longer-duration or
  threshold-policy gates.

### L0 source-inventory completeness

- The observer identity now hashes `views.py` and `drug_testing_catalogue.py`
  alongside the existing runtime modules. Checkpoints therefore fail closed
  after changes to the reader/Testing Desk contracts instead of being
  mislabelled as current-source evidence. The L0 regression suite passes **31
  tests**; no EXE or user save was rebuilt or modified.

### J5 explicit policy-cycle recovery

- The explicit Drug Testing policy-cycle action now fails closed for a
  malformed rules envelope or an unknown legacy policy label instead of
  crashing while looking up the next option. It restores the documented
  Standard policy only at that user-invoked settings boundary and records the
  recovery in Inbox; passive readers and manual testing remain non-repairing.
- Added regression coverage for both malformed envelopes and retired policy
  labels. The focused J5 suites pass **38 tests**; no EXE or user save was
  rebuilt or modified.

- The affected Testing/Staff/UI compatibility set was rerun after the
  recovery boundary and passes **294 tests**. This confirms the explicit
  settings repair does not change passive-reader purity or retained-case
  identity behavior.

- The focused provider, replacement, title-decision and Drug Testing set was
  rerun as well and passes **63 tests**, including both explicit recovery
  cases.

### J5 empty frozen-contract guard

- Retained cases that contain a provider or policy ID but an empty frozen
  snapshot now render as unavailable and require review; the raw evidence is
  unchanged and legacy rows that omit the fields remain readable.
- The focused J5/provider/replacement/title suite passes **64 tests**. The
  current-source L0 run was restarted after this source-bound change; no EXE
  or user save was rebuilt or modified.
- The affected Testing/Staff/UI compatibility set now passes **295 tests**
  after the empty-snapshot guard.

### L0 additional source-bound two-year seed

- Fresh seed `260718` completed **96/96 observer weeks** against the then-current
  source digest that includes the cross-seed coverage reader. It produced **21,187
  fights**, two yearly boundary rows, **0 report-accounting errors**, no
  hard-invariant findings and zero genuine overdue commitments; three
  owner-goal rows were explicitly classified as spectator-paused.
- The raw report, 487 MB checkpoint and complete manifest row are retained.
  Source-bound grouping correctly keeps the earlier `260717` run separate after
  the reader change; a fresh same-source comparison is still needed before
  claiming cross-seed coverage. The authorised century qualification and
  threshold-policy approval remain open. No EXE or user save was rebuilt or
  modified.

### L0 interrupted same-source comparison

- A second source-matched two-year recipe (`260719`) reached its first yearly
  boundary at **48/96 weeks**. Its paused checkpoint and manifest row retain
  the isolated world, RNG state, source/configuration identity and first-year
  evidence for an explicit resume or review; no active career was mutated.
- The paused row is intentionally excluded from complete cross-seed counts.
  The authorised century qualification and threshold-policy approval remain
  open; no EXE or user save was rebuilt or modified.

### J5 challenger-only title-miss guard

- The confirmed **Remove Belt** choice now fails closed when only a challenger
  missed and no champion/interim holder is recorded to vacate. The saved
  scale/fine evidence and decision remain intact, while the bout is held for
  review instead of guessing at a vacancy or title settlement.
- Champion, interim, both-miss and Keep Belt paths remain covered. The focused
  J5 decision/provider suites pass **51 tests**; confirmation, appeals,
  sanctions, AI/spectator handling and the remaining policy tables stay gated.

### J5 replacement identity retention

- Last-Minute Replacement history and its Foundation receipt now preserve the
  deterministic candidate reference for ID-less legacy fighters, alongside a
  modern `fighter_id` when available. The removed corner also contributes a
  stable source reference to retry keys rather than a visible row position.
- Duplicate legacy candidates remain independently selectable after refresh.
  Focused replacement/title/provider coverage now passes **52 tests**; the
  remaining J5 confirmation, appeal, sanction, AI/spectator and policy-table
  work is still gated.

### J5 case identity retention

- Manual screening cases now retain a deterministic `fighter_reference` for
  ID-less legacy fighters, with source-order suffixes for exact duplicate
  retained rows. Modern fighter-ID hashes remain compatible with existing
  saved cases, preserving retry idempotency.
- Duplicate legacy careers using one test ID remain separate cases instead of
  silently collapsing. Focused J5/provider/replacement/title coverage now
  passes **53 tests**; the remaining policy-gated mechanics are unchanged.

### J5 legacy replacement resolution

- Title-miss replacement execution now resolves an ID-less legacy replacement
  by its saved fighter ID when present, or by a unique retained name as a
  compatibility fallback. Duplicate names fail closed rather than redirecting
  the bout to another career.
- The optional Foundation owner is guarded for headless compatibility. Focused
  J5/provider/replacement/title coverage now passes **54 tests**.

### J5 legacy cost projection

- Drug Testing Cases now shows a retained legacy `cost` when an older row has
  no quote envelope, keeping recorded spend visible in row details and summary
  totals. Malformed values remain bounded/unavailable and the raw case is
  untouched.
- Focused J5/provider/replacement/title coverage now passes **55 tests**.

### L0 cross-seed recipe coverage reader

- Audit History's pure summary now groups complete current-source runs by
  duration and lists their unique seeds, making one-year/two-year cross-seed
  coverage visible without reading or rewriting raw reports.
- The summary remains diagnostic only; it does not infer thresholds, resume a
  run or alter a career. Focused L0 coverage now passes **30 tests**. Century
  qualification and threshold approval remain open; no EXE or user save was
  rebuilt or modified.

### L0 current-source two-year qualification

- Fresh seed `260717` completed the full **96/96-week** isolated spectator
  observer loop against the current source identity. It produced **20,816
  fights**, two yearly boundary rows, **0 report-accounting errors**, no
  hard-invariant findings and no genuine overdue commitments; three owner-goal
  rows were explicitly classified as spectator-paused rather than overdue.
- The raw report, 476 MB checkpoint and `complete` manifest row are retained
  with the source/configuration digest. This closes the multi-year evidence
  slice of L0, but the authorised century/cross-seed qualification and explicit
  threshold-policy approval remain open. No EXE or user save was rebuilt or
  modified.

### L0 spectator backlog classification

- Spectator audits now separate paused player-owned owner objectives from true
  overdue commitments. `spectator_paused_owner_goals` preserves the evidence
  without treating intentionally paused work as a qualification failure; live
  player-world overdue semantics are unchanged.
- Audit regression coverage now passes **28 tests**, including the
  spectator/non-spectator distinction and read-only guarantees. No EXE or user
  save was rebuilt or modified.

### L0 refreshed current-source evidence

- Fresh seed `260715` completed 48/48 observer weeks against the new source
  identity: 10,237 fights, zero report-accounting errors, no hard-invariant
  findings, zero genuine overdue commitments and two explicitly paused
  spectator objectives. Raw report, checkpoint and complete manifest row are
  retained.
- This remains one-year evidence, not the authorised multi-year/century
  qualification or threshold-policy approval. No EXE or user save was rebuilt
  or modified.

### L0 qualification review summary

- Audit History now shows a pure qualification summary for retained runs:
  complete/paused/failed counts, unique seeds, current-source matches,
  available reports and threshold-policy status.
- Source comparison is limited to valid year-boundary recipes; malformed or
  non-year rows remain visible as unknown without being rewritten. Focused
  audit coverage now passes **29 tests**. Multi-year/century runs and threshold
  approval remain open; no EXE or user save was rebuilt or modified.

### L0 final current-source evidence pass

- Frozen-source seed `260716` completed 48/48 observer weeks: 9,898 fights,
  zero report-accounting errors, no hard-invariant findings, zero genuine
  overdue commitments and two explicitly paused spectator owner-goal rows.
- The manifest now retains four seeds (three complete, one paused), three raw
  reports and one current-source match. The result is still one-year evidence;
  authorised multi-year/century qualification and threshold approval remain
  open. No EXE or user save was rebuilt or modified.

### L0 audit manifest/index

- Each isolated play-audit report is now indexed by a bounded, atomic manifest
  row keyed to seed, target duration and source/configuration identity. Rows
  retain completed weeks, terminal status, identity/source digests and the
  paired report/checkpoint paths.
- Retrying the same recipe updates one row rather than duplicating evidence;
  malformed or absent manifests read as an explicit empty index without
  normalising the file. Focused audit coverage now passes **26 tests**.
- The authorised multi-year/century qualification and threshold policy remain
  open; no EXE or user save was rebuilt or modified.

### L0 evidence-history reader

- Simulation Lab now exposes a read-only Audit History window backed by the
  retained manifest. It shows status, seed, target, completed weeks, update
  time and source/config identity, and opens a selected raw report separately.
- Missing, oversized and malformed evidence fail closed without repairing the
  manifest or changing the active career. Focused audit coverage now passes
  **27 tests**. Qualification runs and threshold policy remain open; no EXE or
  user save was rebuilt or modified.

### L0 interrupted-run retention

- Yearly checkpoints now update the audit manifest with `paused`,
  `failed_invariant` or `complete` status, keeping incomplete qualification
  runs discoverable before a terminal report exists.
- A disposable two-year run reached 48/96 weeks and one clean yearly snapshot
  before being stopped; its checkpoint and paused manifest row were retained.
  Source/configuration binding refuses mixed-source resume. Qualification
  thresholds and the full authorised run remain open; no EXE or user save was
  rebuilt or modified.

### L0 current-source one-year qualification

- A fresh seed `260714` completed 48/48 observer weeks against the current
  source identity: 9,764 fights, one yearly boundary, zero report-accounting
  errors and no hard-invariant findings. The raw report, checkpoint and
  `complete` manifest row are retained.
- The report keeps measured backlog evidence visible, including two legacy
  owner-goal deadlines; no automatic repair or threshold judgement was made.
  Multi-year/century qualification and approved thresholds remain open; no EXE
  or user save was rebuilt or modified.

### L0 raw-report retention

- Play-audit reports are now atomically persisted beside their seed/year
  checkpoints and can be read back after the in-memory Simulation Lab state is
  recreated. Read-back is bounded and non-mutating; write failures are
  disclosed rather than touching the active save.
- Audit evidence-retention coverage now passes 25 focused tests. Multi-year /
  century qualification and threshold policy remain open; no EXE or user save
  was rebuilt or modified.
- The Simulation Lab report reader remains reachable after page rebuilds and
  shows an explicit unavailable state when the selected seed/year has no
  retained report.

### L0 explicit-seed audit contract

- Simulation Lab now lets the player choose a bounded play-audit seed. The
  seed is included in source/configuration identity, drives the isolated RNG,
  appears in reports and scopes checkpoint filenames, so resume cannot mix
  different runs. The default `260712` recipe remains compatible.
- Audit identity/path coverage now passes 24 focused tests. Multi-year/century
  qualification and cross-seed evidence remain open; no EXE or user save was
  rebuilt or modified.

### L0 live audit integration and staff identity repair

- The play-level audit now handles the live Combat Sports `events` counter,
  legacy event collections, malformed sport rows and Promotion's
  `reputation_score` field without failing at the yearly report boundary.
- Newly generated AI staff-market candidates receive deterministic,
  collision-safe stable IDs. Spectator handoff projections no longer produce
  false duplicate-employment findings, while real staff identity collisions
  remain blocking evidence.
- The fresh one-year observer run completed 48/48 weeks and 9,874 fights with
  zero report-accounting or hard-invariant findings. Focused audit/staff
  coverage passes 51 tests. Multi-year/century qualification and threshold
  policy remain open; no EXE or user save was rebuilt or modified.

### J5 duplicate case identity guard

- Drug Testing Cases now fail closed when a retained duplicate `case_id` is
  requested through the generic detail/workflow reader. The UI still keeps
  both duplicate legacy rows with deterministic display suffixes; no source
  evidence is rewritten and migration remains the repair boundary.
- The focused catalogue and legacy testing regressions pass **28 tests**, with
  clean compilation and diff checks. Provider confirmation, appeals,
  sanctions and sporting consequences remain policy-gated. No EXE or user save
  was rebuilt or modified.

### A1/A2/A3 planning and commitment card closeout

- Closed the approved A1 read-only Owned Calendar slice: MMA, owned-child and
  Combat Sports schedules project into one identity-bound index with source,
  owner, status, date, participants/reservations, legacy coverage and safe
  detail routing. Filters and refresh preserve source identity and never copy,
  rerun or mutate a schedule.
- Closed the approved A2 lifecycle-integrity slice: super-event terms are
  normalised once, state transitions and expiry/cancellation use stable
  terminal settlement keys, stale revisions fail closed, legacy money remains
  explicit/manual-review and repeated closeout cannot duplicate effects.
- Closed the approved A3 review-only cash-runway slice: settlement-aligned
  weekly forecasts, bounded sensitivity, break-even limits, sponsor/rights
  uncertainty and stale-input scenario snapshots remain non-binding and
  RNG/state stable. Owned Calendar, cash-runway, super-event, economics,
  Foundation and persistence checks pass. Controlled child scheduling, legacy
  refund/fee policy, probability claims and new demand curves remain explicit
  holds. No EXE or user save was rebuilt or modified.

### J8 durable tournament hub card closeout

- Closed the approved J8 one-night slice: tournament editions retain stable
  series/edition identities, entrant IDs, seed and rank snapshots,
  substitutions, stage/result references, champion and replay availability in
  the durable Results index. History filtering and detail review are
  read-only, preserve malformed evidence as Review required, and never rerun
  a draw or resolve current rosters.
- Matchmaking exposes a read-only seeded-field/alternate review with current
  readiness and rank evidence; actual replacement remains with the existing
  weigh-in owner. The J8 history/UI suite and maintained UI-data checks pass.
  Multi-event Grand Prix rules are now implemented by the separately approved
  release extension: 4/8/16-person fields can span their natural 1–4 event
  limits, medical/confirmed-drug-failure replacement and whole-series
  postponement preserve identity and eligibility, and tournament-only deciders
  prevent draws. The maintained suite and child-promotion long run pass.

### J7 structured company proposals and ownership history card closeout

- Closed the approved J7 v1 slice: fighter swaps and parent/child moves use
  append-only, stable-ID proposal snapshots with parties, fighter/rank facts,
  costs, expiry/return boundaries, warnings and terminal outcomes. Commit
  paths re-check ownership, duplicate-name ambiguity, bookings, title state,
  division availability and cash before mutating rosters, belts, finance or
  membership history; repeat commits remain idempotent and all departures use
  the sanctioned vacancy path.
- Bounded development loans return at the agreed safe boundary, delay behind a
  committed child bout with a player-facing notice, and remain save/load safe
  and retry-idempotent. Foundation, child-interaction, loan-return and
  narrative regressions pass. General treaties, goodwill modifiers and
  spin-off/acquisition allocation remain explicit policy holds. No EXE or user
  save was rebuilt or modified.

### J4 historical truth and company memory card closeout

- Closed the approved J4 slice: archived opponent records never fall back to
  today's mutable record, membership/title facts retain stable identities, and
  Company Timeline/as-of/history readers preserve incomplete or malformed
  evidence without inventing dates or pruning data.
- Foundation, Fighter Profile and identity/persistence regression groups are
  green. Destructive archive compaction remains a separate policy decision;
  no EXE or user save was rebuilt or modified.

### S3 specialty, progression and retention card closeout

- Closed the approved v1 S3 slice: the staff catalogue is explanatory and
  unknown-safe, the three authored non-Scout specialties are lead-scoped and
  capped at 2%, and progression requires paid, executed, evidence-backed work
  across distinct months/targets with annual and skill ceilings.
- Staff Management, specialty, Media and Event Economics coverage is green;
  satisfaction, departure-probability and negotiation-heat changes remain
  explicitly policy-gated. No EXE or user save was rebuilt or modified.

### J1 Explainable booking workbench acceptance closeout

- The approved v1 workbench now rechecks pair-level sporting rules at commit:
  rematch cooldowns, passed/changed dates, title-holder/challenger merit and
  special-belt ownership. Saved alternatives remain identity-bound and
  locked/TBA draft evidence is preserved.
- Added shallow-division, title-merit, rematch, date and fail-closed
  regressions; 19 booking-workbench tests, Foundation and UI-data checks pass.
  The medical planning note remains explicitly non-binding pending its separate
  policy gate. No EXE or user save was rebuilt or modified.

### S4 durable AI employment card closeout

- Closed the approved durable-employment slice: identity-preserving AI hires
  now use the six-month no-revenue runway gate, block duplicate role/month
  hires, retain malformed rows for review, and return expired staff to the
  shared market exactly once.
- Added long-run payroll and two-year expiry/market-boundary evidence; no EXE
  or user save was rebuilt or modified.

### J6 Academy coaching card closeout

- Closed the approved v1 Academy Coach slice: explicit hireable coaches now
  retain the legacy Trainer fallback, supervise one active cohort, apply one
  bounded quality adjustment, and expose expiry, handover, reassignment and
  comeback boundaries.
- Retired-fighter career links keep the original fighter record and use a
  separate stable staff identity. Academy Coach, Fighter Profile, persistence
  and UI-data coverage is green; no EXE or user save was rebuilt or modified.

### Distressed-buyout duplicate-name safety

- Distressed promotion buyouts now retain and release fighters by stable
  fighter identity instead of display name, so two careers sharing a name
  cannot be treated as the same roster member.
- Id-less legacy rows use an in-memory object fallback only for that boundary;
  title vacancy and membership recording remain unchanged.
- UI-data and Foundation regression suites pass; no EXE or user save was
  rebuilt or modified.

### S4 AI employment long-run boundary

- Added a two-year boundary regression covering AI staff expiry, market
  return, duplicate prevention and retry-safe contract processing.
- Expired staff retain their stable identities and reset market terms without
  creating duplicate employment rows.

### S3 quarter-close progression evidence

- Quarter-close progression now backfills only strict, paid, executed work
  rows that already carry a valid lead, target, month and work ID into the
  retained progression ledger.
- A skill gain can no longer appear without its credited action IDs and
  distinct work periods; malformed and duplicate receipts remain excluded.

### J6 retired-fighter Academy Coach links

- Academy Coach market rows can now preserve a link to a retired fighter's
  original identity and at-the-time record while assigning a separate staff
  identity and the authored skill-45 coaching baseline.
- Staff profiles explain the career link and the explicit comeback boundary;
  the retired fighter remains in the archive and is never silently converted
  or removed.
- Comeback negotiations fail closed while that linked coach is actively
  supervising a development block; the player must end the Academy
  assignment first.

### Cash runway unavailable-archive summary

- Finance now reports a malformed saved-scenario archive as unavailable instead
  of counting its diagnostic row as a real planning snapshot.
- The summary keeps the repair boundary and no-reservation guidance visible;
  valid current/stale snapshot counts remain unchanged.

### Cash runway scenario archive safety

- Saved runway scenarios now expose an explicit unavailable reader row when
  the retained scenario collection is malformed, instead of presenting a
  misleading empty archive.
- The Finance window labels that state and preserves the raw envelope; no
  scenario, cash, schedule, RNG or finance repair occurs during refresh.

### Cash runway marks malformed transaction shelves unavailable

- A non-list retained `week_transactions` envelope now produces an explicit
  unavailable paid-to-date diagnostic instead of looking like a confirmed
  zero-cost history.
- The forecast remains read-only: the raw finance value is preserved and no
  cash, RNG, schedule or ledger state is repaired during refresh.

### Profile title history retains opponent identity

- Title-history rows now carry the archived opponent name and stable opponent
  ID through the Profile History projection and its detail/export payload.
- History-row fingerprints include both fighter and opponent identity, so
  same-text title events cannot be retargeted or collapsed by display labels.

### Profile title-history reader boundary

- Profile title-history projections now bound malformed limits and ignore
  non-list belt-history containers, leaving malformed retained evidence for
  the load/migration boundary instead of crashing a reader.
- Added no-mutation coverage for malformed containers and non-finite limits;
  valid identity-bound rows and title mechanics are unchanged.

### Title-lineage migration keeps same-text historical facts

- Title-history migration now deduplicates with retained fighter, opponent,
  title-event and event identities in addition to visible text. Separate
  legacy rows that share a date, action, note or display name therefore remain
  separate historical evidence instead of one silently replacing the other.
- Added a regression covering same-text `Vacated` rows with distinct fighter
  IDs. The repair remains load-boundary-only and does not create synthetic
  history IDs or alter live title state.

### Title-lineage identity during migration

- Rebuilt MMA title-history rows now retain the winning fighter ID and
  opponent ID from archived bout results, including same-name careers.
- Lineal reconstruction prefers recorded winner identity over display-name
  parsing and keeps defense/crown transitions bound to the correct holder;
  legacy name-only rows still use the existing unambiguous fallback and now
  remain unrebuilt when the winner name is ambiguous.
- Added regression coverage for duplicate-name title results. No live title
  outcome, belt vacancy or saved career is rewritten by the reader.

### Staff progression evidence readout

- Staff specialty rows now show a compact progression summary: credited work,
  distinct periods, qualified quarters and current-year skill-cap usage.
- Staff profile readers expose the same summary next to the existing retention
  evidence, without inferring morale, departure or extra progression.

### Staff progression receipt integrity

- Quarter-close progression now requires a durable Staff `work_id` and
  `lead_id`; ID-less or ambiguous receipts are excluded from qualification.
- Duplicate work IDs fail closed as one ambiguous evidence set rather than
  allowing receipt order to farm a skill gain. Existing progression caps and
  quarter rules are unchanged.

### Title-miss replacement decision clarity

- The managed title-miss panel now labels the optional replacement flow as
  `LAST-MINUTE REPLACEMENT` and the commit control as `Last-Minute
  Replacement`, with the approved same-division/ranking review explained in
  the panel itself.
- This is presentation-only: the existing stable-identity candidate list,
  eligibility checks, replacement history and title rules are unchanged.

### Testing Desk case-scope presentation

- Drug Testing Cases now show retained event scope in the ledger and search
  stored event-reference fields, with manual/no-event cases labelled clearly.
- The projection remains read-only and source-bound; it does not resolve live
  events, repair saved cases or enable confirmation, appeal or sanction rules.

### Testing Desk quote transparency

- Provider quotes now retain and display the base-screening subtotal and the
  provider-service adjustment before showing the frozen total.
- The breakdown is explanatory only; it does not add provider-specific
  detection or disciplinary mechanics.

### Overdue commitment evidence

- Yearly play-level audit measurements now report a bounded overdue-commitment
  total and source breakdown for explicit super-event, booking, medical, Media,
  Staff, owner-objective, fighter-career and Academy deadlines.
- Missing clocks, malformed due fields and terminal rows fail closed; the audit
  remains a read-only projection and does not create tasks, repair saves, spend
  cash or consume RNG.

### Calendar task timing evidence

- Native calendar tasks now retain bounded elapsed-time diagnostics for both
  synchronous and cooperative advances.
- Isolated yearly audit snapshots record timing count, total, maximum and
  per-task summaries; malformed or non-finite timing rows are ignored or
  clamped and never influence gameplay or saved state.

### Roster and title supply evidence

- Yearly audit measurements now include free-agent availability, native
  record-based eligible challenger depth, current vacant-title count and
  bounded vacancy duration, plus explicit expired-offer counts.
- Vacancy duration and challenger counts use retained identities/rules only;
  malformed dates or records remain unknown and no reader repair is performed.

### Play Audit evidence presentation

- The managed audit reader now surfaces supply/commitment metrics and the
  latest calendar-task timing summary in a labelled evidence section.
- Legacy or malformed timing values are bounded for display only; the saved
  checkpoint and isolated world remain unchanged.

### Audit measurement evidence validation

- Yearly audit rows now receive a pure evidence-quality check for missing or
  malformed supply/title metrics, invalid vacancy relationships and incomplete
  calendar timing summaries.
- Findings are retained as warnings in the managed report and never repair
  checkpoints, alter gameplay or stop the isolated world.

### Scouting legacy search identity

- ID-less Scouting searches now use deterministic retained request/content
  fingerprints, including empty or opaque legacy rows, instead of
  `search:legacy:row-N` list-position keys.
- Saved assignment IDs remain authoritative and the reader stays non-mutating;
  a regression covers index-independent identities.

### Legacy Ledger identity

- Legacy Ledger fighter rows now use the source-bound fighter identity instead
  of the displayed rank, with deterministic duplicate suffixes. Sorting and
  filtering can no longer change the row target.
- Score breakdowns and profile routes remain unchanged and read-only.

### World Staff and Scouting reader identities

- ID-less AI staff employment rows now use deterministic retained
  employment/source fingerprints rather than list positions.
- ID-less Scouting watchlists use content-bound keys and retain duplicate
  generated legacy rows with safe suffixes; saved duplicate IDs keep their
  existing compatibility behaviour.

### Durable drug-testing case lifecycle envelope

- New compliance cases retain optional event identity and a defensive event
  reference, plus explicit lifecycle containers for confirmation, appeals,
  provisional restrictions, sanctions and final resolution.
- The workflow reader exposes stable statuses and counts while preserving the
  frozen screening evidence; confirmation and sanction rules remain gated and
  no reader commissions tests, spends cash, applies sanctions or consumes RNG.

### Fight History opponent-record evidence

- Historical fight rows now use a saved opponent ID or exact archived name
  before reading a same-date record snapshot.
- Missing at-the-time records show `Not recorded` rather than a current roster
  record or an unrelated same-date bout; unidentified legacy rows remain `-`.

### Testing Cases evidence context

- Drug Testing Cases details now show event scope/source and a compact status
  summary for confirmation, appeals, provisional restrictions and sanctions.
- Detail rendering uses bounded read-model totals for malformed quote evidence
  and remains observational; no provider commission, cash spend, sanction or
  RNG draw is introduced.

### Contract-batch quote safety

- Renewal workbench planning now bounds malformed/non-finite purse,
  popularity, momentum, age, term and cash facts while preserving valid quote
  formulas, identity order and partial-result semantics.
- Invalid retained values remain planning evidence only; quote generation does
  not negotiate, spend, mutate contracts or consume RNG.

### Company Proposal Ledger cash safety

- Proposal table and detail readers now bound malformed/non-finite retained
  cash terms while preserving source-bound IDs, parties and outcomes.
- Opening or filtering the append-only ledger remains non-repairing and
  cannot spend cash or consume RNG.

### Storylines non-finite evidence safety

- Storyline importance, update/beat dates and promotion scoreboard values now
  fail closed for NaN, infinity and overflow-sized retained data.
- Affected rows remain visible with bounded display defaults and a `Needs
  review` status; authored beats, stable IDs and the raw retained thread are
  preserved for load/migration review.
- Missing update dates also fail closed when their retained `started_month`
  fallback is non-finite, preventing malformed chronology from escaping via a
  default value.
- Profile storyline context and headless Followed Briefings share the same
  fail-closed boundary for filtering, sorting and calendar conversion, while
  preserving source-object identity and leaving repair to load/migration.

### Media desk numeric reader safety

- Media Rights dashboard, receipt history and selected campaign detail now
  treat overflow-sized/non-finite retained usage, amounts, heat and cost as
  bounded display evidence.
- Active offers, contracts, campaign history and all explicit spend/RNG owners
  remain unchanged by a refresh.
- Settled-receipt detail uses the same bounded projection for rights, sponsor,
  total and relationship amounts, keeping malformed immutable evidence visible.

### World Chronicle overflow safety

- Non-finite/overflow page limits and chronology dates now fail closed to bounded
  display values; invalid dates remain visible as `Date unavailable` with
  `Needs review` status.
- Authored chronology, source identities and context routing remain unchanged;
  opening or filtering the archive does not repair retained data.

### Database editor identity audit

- The database-editor UI audit now binds every field read/apply check to the
  saved fighter/company record, not its visible list position, so sorting and
  filtering cannot make acceptance coverage edit the wrong row.

### Historical selection fixture compatibility

- Frozen phase-31 selection verification now omits development-loan return
  fields only when they contain their exact untouched zero defaults, preserving
  the original bout and fixture checksums while rejecting populated or malformed
  loan state.
- Child-promotion interaction coverage silences both info and warning notice
  fallbacks, so the identity guard is verified without an unattended modal.

### Staff collection reader guard

- Staff roster, candidate and expiry refreshes now project malformed
  employment collections as an explicit empty view without mutating retained
  rows or changing stable action routing.
- Staff evidence-audit spend and summary revision readers fail closed for
  overflow-sized values while preserving the raw policy and work ledger.
- Roster cells also display `—` for non-finite skill, salary or morale values,
  keeping malformed employee evidence reviewable without coercion.

### Combat Sports contract reader boundary

- Combat Sports contract refreshes now project legacy child-division membership
  with `repair=False`, retaining saved rosters and stable fighter identities.
- Malformed containers and non-finite months/purses fail closed; explicit
  signing, release and calendar-expiry paths retain the repairing owner.

### Staff capability action-ID parity

- The Staff capability read model now exposes stable `safe_read_action_ids`
  separately from friendly action labels.
- The Staff grid and selected-department detail show those IDs, keeping saved
  brief allow-lists and calendar-worker recommendation authority aligned while
  mutating actions remain Full Auto-only.

### Staff guardrail input boundary

- Staff budget, action-ceiling and brief-quote paths now fail closed for
  non-finite or overflow-sized amounts, returning validation or zero planning
  evidence without rewriting policy state.

### Selected Combat Sports profile guard

- Selected company profiles now fail closed on malformed combat-sport world or
  player-division envelopes and show an explicit unavailable state.
- Valid roster, titles, event history, finance, cash, reputation and stability
  use defensive projections; opening or switching profiles remains non-repairing
  and RNG-free.

### Finance reader overflow guard

- Finance, History & Outlook and Cash Runway display readers now catch
  overflow-sized legacy numbers and reject non-finite tax rates.
- Raw finance/history envelopes remain unchanged; calendar, settlement,
  annual-history and cash mutations stay on their explicit owners.

### Game & Saves reader boundary

- Save-library refreshes and selected-save summaries now enumerate existing
  paths without creating missing Save/Database folders or active-slot state.
- Explicit save, copy, migration and database-selection actions retain their
  normal write boundaries.

### Combat Sports overview reader guard

- The Combat Sports landing tab now fails closed on malformed world/division
  containers and invalid detail rows, with bounded cash, reputation and event
  values.
- The overview remains a non-repairing `repair=False` projection; refreshes do
  not seed circuit weights, crowns, rankings, schedules, finance or RNG.

### Regional Feasibility reader hardening

- Defensive display projection now handles malformed/non-mapping invitations and
  snapshots, non-finite dates and counts, invalid scheduled rows and bad venue
  lists without raising or rewriting retained regional data.
- The review remains a read-only scouting-aware assessment: no offer generation,
  booking, reservation, finance mutation or RNG work occurs during refresh.

### Combat Sports history reader hardening

- Circuit Records & History now handles malformed world/division envelopes,
  non-finite financial values and invalid history dates without crashing or
  repairing retained state; unavailable rows remain labelled for review.
- The reader keeps the non-repairing circuit projection and cannot migrate
  titles, rankings, finance, events or RNG merely by opening or refreshing it.

### Regions profile reader hardening

- Regions profile and next-step readers now bound malformed/non-finite regional
  indicators and pairing counts and skip malformed scheduled-event rows while
  retaining explicit unavailable data states.
- Refresh remains observational: it does not repair the catalogue, generate
  invitations, book events, spend cash or consume RNG.

### Company standings projection hardening

- Rankings and Combat Sports company-power readers now bound malformed or
  non-finite roster ratings, reputation, stability, cash and champion values so
  invalid rows cannot crash or dominate standings.
- The projection remains presentation-only; saved ranking/circuit state,
  rosters and RNG are untouched during refresh. Non-mapping world and player
  division envelopes are rejected before any field is read.

### Owner-goal projection hardening

- Owner objective readers now bound malformed/non-finite cash, popularity,
  show-count, target, deadline, baseline and duration values through a shared
  non-mutating projection; Inbox/Goals cannot crash on legacy corruption.
- Observation, baseline and terminal-outcome writes remain owned by the
  completed-week worker rather than a page refresh.

### Drug Testing Cases reader hardening

- Compliance-case summaries and workflow projections now fail closed on
  non-finite/malformed sample counts and quote totals without rewriting retained
  cases; durable IDs and provider/policy evidence remain available for review.
- The ledger is still observational: refresh/filter cannot commission samples,
  charge cash, apply sanctions or consume RNG.

### Regional Prospects baseline hardening

- Non-repairing regional-candidate assessments now fail closed on malformed or
  non-finite saved history baselines while retaining the raw fighter record.
- Eligibility, scouting visibility, row identity and feeder state remain
  unchanged for valid baselines; opening/filtering cannot migrate or consume
  RNG.

### Inbox reader hardening

- Inbox display and action readers now fail closed on non-finite/malformed
  message dates and fighter contract terms without normalising saved messages.
- Source-bound selection, filters, unread/action labels and explicit mutation
  handlers remain unchanged.

### Event Log reader hardening

- The sidebar Event Log now reports a non-list retained collection as explicit
  load/migration review evidence instead of treating a string or other value as
  log lines; genuinely empty archives retain their normal empty state.
- Valid lines remain complete, ordered and semantically styled, with scroll
  position preserved and no fight/settlement/simulation work triggered.

### Cash-runway malformed finance guard

- Bounded malformed production, medical, drug-testing, sponsorship, rights,
  merchandise/tax, academy and tier/capacity values in the read-only runway
  forecast instead of crashing or repairing retained finance data.
- Preserved the raw finance envelope, scheduled cards, cash and RNG state;
  added regression coverage for malformed scalar and nested finance values.

### Revenue-mix history reader hardening

- Malformed archive, weekly-history, transaction and month values are now
  treated as unavailable by the Finance revenue-mix projection.
- Valid event streams and non-event revenue remain separated, duplicate
  transaction IDs remain filtered, and retained finance/archive data is never
  rewritten by the reader.

### AI-upgrade rollback timeline guard

- A failed AI replacement now records an ID-linked return fact before the
  temporarily released incumbent is restored to the promotion roster.
- The append-only Company Timeline remains truthful without changing the
  existing replacement selection, affordability, finance or RNG behaviour.

### Owned-calendar date projection hardening

- The read-only owned-company calendar now bounds malformed current and event
  dates and reports partial coverage for invalid retained values.
- Stable event/owner identities remain intact without changing schedules,
  reservations, finance or RNG state.

### Cash-runway break-even evidence

- Event runway quotes now report gate-plus-merchandise break-even attendance
  only when venue capacity can reach the committed-cost threshold.
- Conditional rights and sponsor receipts remain excluded from that threshold;
  saved scenario details expose the same read-only evidence.

### Staff reader non-finite guard

- Staff skill, contract-term, contract-target, offer-score and warning readers
  now fail closed for non-finite (`Infinity`/`NaN`) and other malformed legacy
  numbers without rewriting the retained staff row.
- Uncertain contract terms remain expired for access checks; explicit contract
  mutation paths retain their existing validation boundary.

### Staff lead projection guard

- Morale-adjusted lead selection and specialty-saving read models now treat
  non-finite skill, morale and subtotal inputs as bounded unavailable values.
- Malformed rows cannot win lead comparisons through `Infinity`/`NaN`; cost-note
  readers return a safe zero without rewriting retained staff data.

### Staff capability/evidence audit guard

- Capability, employment-audit and progression read models now surface
  non-finite salary, contract, morale and work values as invalid evidence.
- Stable staff IDs and retained rows are preserved; opening or refreshing these
  readers does not repair the save.

### Media Rights non-finite reader guard

- Contract eligibility, renewal/terminal history, terms and event-outcome quote
  readers now bound `Infinity`/`NaN` and malformed values to zero or
  `Needs review` while preserving fee-alias truth.
- Retained contracts remain untouched and quote fallback does not draw extra
  RNG or run settlement work.

### Sponsor preview non-finite guard

- Activation previews, offer assessments and sponsor-fee readers now bound
  malformed trust, stability, fee, fit, term and conduct values.
- Contracted maximums stay separate from the current full/half-fee estimate;
  preview refreshes preserve saved offers/deals and do not spend or draw RNG.

### Testing Desk quote guard

- Provider/policy planning quotes now bound malformed or non-finite base costs,
  sample counts and authored multipliers.
- Quotes remain planning-only: no cash, RNG or live screening-policy mutation
  can occur while comparing testing providers.

### Native dialog inventory regression

- Added a source-level inventory for direct routine information dialogs so each
  retained fallback is tied to an owning page/status surface or an explicit
  managed-reader/headless compatibility path.
- Covered shared warning/info status-helper fallbacks as well, preventing a
  dynamic messagebox route from appearing outside the named compatibility
  helpers.
- Asserted that Fight Day's native three-way choice is headless-only; desktop
  play continues to use the themed decision panel.

### Spectator policy source guard

- Newly armed spectator stop policies retain the current game revision.
- Automatic fast-forward and Resume fail closed on an explicit revision
  mismatch before any calendar work starts.
- Legacy policies without a revision remain compatible; clearing or replacing
  a stale policy remains available and ordinary loading is unaffected.
- Preserved validation errors, destructive confirmations and final approvals.

### Spectator scheduler guard

- Enforced the saved-policy revision check at the shared advance scheduler, so
  direct callers cannot bypass the public spectator fast-forward/Resume guard.
- Rejection occurs before due-card inspection, job creation, callbacks or Tk
  scheduling; compatible legacy policies and normal player advances are unchanged.
- Added regression coverage for the direct-entry bypass and retained no-rewrite
  behaviour for stale policies.

### Spectator policy load preservation

- Rule-default processing now retains the saved `source_revision` on spectator
  stop policies, so loading cannot silently downgrade a stale policy to a
  legacy-compatible one.
- Policies created before the field receive an empty legacy-compatible value;
  explicit policy actions remain the only write boundary.
- Added regression coverage for both revision retention and legacy projection.

### Spectator policy reader safety

- The simulation desk now labels an explicit revision mismatch as stale and
  explains the clear/re-arm recovery path without modifying saved data.
- Malformed date targets render as unavailable instead of reaching calendar
  formatting and crashing the reader.
- Added read-only regression coverage for stale status and malformed targets.

### AI signing membership history

- Automatic AI free-agent signings now append an ID-linked promotion `join`
  fact before moving the fighter into the promotion roster.
- The stable transition key keeps retries idempotent and fills the Profile
  History Company Timeline without changing deal economics or RNG behaviour.
- Added regression coverage for the membership-before-roster mutation boundary.

### Signing-boundary membership completion

- Player free-agent signings, automatic TBA fills, last-minute replacement
  picks and direct contract negotiations now write their incoming company fact
  before changing roster collections.
- Regional feeder negotiations write both the feeder departure and player-
  company arrival at that boundary; negotiated swaps remain the single owner
  for both sides of a transfer.
- The narrative contract-signing hook can suppress its membership write after
  a domain owner has already recorded it, preventing duplicate timeline rows.
- Existing contract, purse, finance, replacement and negotiation mechanics are
  unchanged; the focused foundation/UI-data bundle passes 198 tests.

### Booked-fight restoration history

- The explicit load repair that restores a fighter for an already scheduled
  bout now records a player-company `join` or `return` fact before moving the
  fighter from the free-agent/retired collection.
- Stable source keys keep repeated loads idempotent, while unresolved or
  ambiguous references remain fail-closed and the original booking evidence is
  untouched.

### Intake membership boundary completion

- AI opening rosters/depth contracts, academy and rival-academy graduates, AI
  emergency signings, regional wonderkid/proving-ground intake and regional
  youth intake now record destination membership before roster insertion or
  free-agent removal.
- The post-move narrative signing hook suppresses duplicate membership writes;
  intake selection, contract, finance, development and RNG behaviour are
  unchanged.
- Source-order regression gates cover every intake owner and the regional
  departure helper; the foundation suite now passes 34 tests.

### Combat Sports private-market history

- Player-owned Combat Sports youth/free-agent signings now write the stable
  destination-company membership fact before changing the signable pool or
  division roster.
- Cash, contract, sport-employer and ranking behaviour remain unchanged, with
  no duplicate membership rows.

### Intake stability verification

- The broad stability playtest passed for seeds 2201, 2202 and 2203 through
  Month 4, recording 179, 187 and 180 events with 1, 1 and 2 ordinary
  retirements respectively.

### Fight-day decisions move to a managed workflow

- A due card now opens a themed, identity-bound decision panel with separate
  Watch Live, Simulate Now, and Stay on This Week actions.
- Opening the panel is read-only; the selected card is revalidated before any
  preparation or settlement, and stale cards fail closed.
- The remaining destructive confirmations still require explicit approval;
  routine advancement notices continue to aggregate in Inbox.
- Spectator fast-forward precondition and empty-result notices now stay on the
  persistent simulation desk instead of opening native informational dialogs.

### Routine Scouting and Academy feedback stays in workflow

- Scouting blockers now use the visible Target Board/Control Centre status
  line when available; legacy/headless callers retain a safe fallback dialog.
- Academy balance, capacity, showcase, promotion, network, and empty-history
  feedback now appears in the managed Academy status/report area.
- Academy scout lead reports use a managed reader window, while spending,
  releases, early debuts, and other irreversible choices remain explicit.
- Academy alumni now open in a managed table retaining pathway, amateur/pro
  record, current rating, and title-win facts instead of a long modal dump.
- Contracts now keeps empty-selection feedback, batch-review blockers,
  combat-sport renewal results, and auto-negotiation summaries in the page
  alert strip when available; renewal/release commits remain explicit.

### Roster division feedback stays on the page

- Roster and Division Management now route missing selections, empty/already-open
  division states, close/reopen results, and closed-division release summaries
  to the themed Roster notice strip when the page is available.
- Notices retain the affected fighter/division and the relevant payroll or
  future-bout consequence; destructive close and release confirmations remain
  explicit and identity-safe.

### Free-agent market feedback stays recoverable

- Stale negotiation targets, invalid purse/cash blockers, and successful market
  signings now appear in the Free Agents action notice beside the controls.
- The terms window and contract commit remain explicit; the notice change does
  not alter signing, finance, or roster ownership.

### Media Desk decisions stay in context

- Campaign strategy, campaign execution, brief validation and retarget/cancel
  outcomes now remain in dedicated Media Desk status lines after refreshes.
- Rights offer selection, counters, market reviews, renewals and contract
  blockers use the rights status line; the existing replacement, market-spend
  and end-deal confirmations remain explicit.
- The change preserves the existing media resolver, offer identity and finance
  boundaries; it only keeps routine outcomes visible beside the action that
  produced them.

### Context readers keep management guidance visible

- Transfer preflight blockers, Combat Sports release outcomes, company archive
  gaps, career-plan validation and Staff roster/contract guidance now use the
  owning page or managed reader status surface when available.
- Staff profiles are now scrollable managed readers that retain the full role,
  specialty, progression, tenure-story and scouting evidence package.
- Read-only context actions remain non-mutating; destructive releases and
  contract endings still require their existing confirmations.

### Company and spectator guidance stays with the selected reader

- Company selection, takeover and return-to-spectator blockers now appear in
  the Company reader's persistent next-step notice when the page is available.
- Child promotions, regional feeders, unavailable companies and already-active
  spectator state explain the next action without interrupting the player;
  legacy/headless callers retain a safe dialog fallback.
- Takeover confirmation and roster, finance and title transition boundaries are
  unchanged.

### Company Editor refreshes are read-only projections

- Belt, interim-title, special-belt, rule and broadcast-contract displays now
  use defensive local projections during repainting.
- Opening or refreshing the editor cannot seed defaults, normalise malformed
  legacy envelopes or rewrite saved title state; explicit management actions
  remain the mutation boundary.
- Malformed special-belt defence/history fields render as bounded evidence
  instead of interrupting the reader.

### Reader-boundary stability verification

- The seeded long-run stability playtest completes through Month 4 across
  three independent seeds after the recent save, editor, gym and company
  reader changes.
- Calendar event recording and ordinary retirement handling remain intact;
  no executable or user save is changed by the verification run.

### Game Settings opens without hidden repairs

- Rule, commentary and Fight Night audio values are projected locally while
  the settings editor is open.
- Missing or malformed saved values are only normalised when the player
  presses Apply; cancelling the editor leaves the retained settings untouched.

### Receipt and archive readers retain routine outcomes

- Settlement-receipt selection/read gaps stay beside the immutable Media receipt
  list, and Staff work-receipt gaps stay in the Staff evidence strip.
- Missing tournament brackets and unavailable Fight Night replays remain visible
  in Results/archive detail; a post-settlement presentation warning also routes
  to Results when available.
- Legacy or headless callers retain compatibility dialogs without changing
  settlement, evidence, RNG or identity ownership.

### Milestone approvals keep project outcomes in context

- Company Milestones & Super Events now reports missing selections, approval
  blockers and planning cancellation outcomes in its own status line.
- The existing payment/commitment owners and explicit destructive cancellation
  confirmation remain unchanged.

### World Editor guidance stays in context

- Current Career Editor name, employer, duplicate and selection guidance now
  appears in the editor header when the page is available; headless callers
  retain the compatibility dialog fallback.
- Fighter saves, retirements and detailed-rating saves report the affected
  fighter and pending career-save state beside the editor controls.
- Retire confirmation, malformed values and all identity/title/membership
  mutation boundaries remain explicit and unchanged.

### Gym viewers remain read-only

- Opening a gym profile now reads the synchronized membership projection
  without recalculating member counts or first-week capacity in the viewer.
- Calendar advancement and explicit roster/mutation paths remain responsible
  for synchronization, so profile refreshes cannot rewrite the saved world.

### Game & Saves keeps routine outcomes in context

- Quick-save, universe selection/clone/validation, backup/restore, database
  export/import/load, career-start conversion completion and missing-selection
  guidance now use the Game & Saves status line when the page is open.
- Overwrite, delete, migration, restore and conversion approvals remain
  explicit; write failures retain their recovery/error dialogs.
- Legacy and headless callers keep a compatibility fallback, with no change to
  atomic save paths, recovery copies, active-career switching or database data.
- Universe section editing keeps successful saves and validation results in its
  own status line; malformed JSON and failed writes remain actionable errors.

### Database Editor keeps routine guidance visible

- Selection, stale-record, required-field and empty bulk-edit guidance now uses
  the editor header status line when available.
- Detailed validation/preflight findings remain available in their diagnostic
  views; successful checks are summarised inline.
- Save/delete confirmations and identity-safe editor boundaries are unchanged.

### Simulation Lab keeps routine guidance in its work areas

- Missing fighters, invalid tournament fields, unavailable sandbox brackets or
  tournament nights, mixed-gender blockers and missing audit results now appear
  in the simulator result or bracket report areas.
- The openweight confirmation remains explicit; sandbox state and career data
  are unchanged by the presentation migration.
- Play-audit export completion is reported in the managed audit reader and the
  exported copy does not alter the active career.
- Invalid provider or policy selections in Testing Providers & Policy now stay
  in that reader's summary line; J5 confirmation and sanction rules remain
  gated.

### Scheduler and booked-card editor keep validation in context

- Past dates, duplicate/already-scheduled fighters, availability and rules
  blockers, TBA/tournament review states, comparisons, and replacement outcomes
  now use the Matchmaker's Desk or editor notice strip.
- Card staging and final schedule/settlement boundaries remain unchanged and
  identity-bound.

### World Chronicle adds read-only exports

- The filtered Chronicle projection can be saved as JSON or a readable text
  report from the managed Chronicle window.
- Source facts and unavailable/legacy labels are retained; export errors stay
  in the window status line and never mutate history.

### Combat Sports overview keeps routine guidance in context

- Missing sport selections, startup-funding blockers, failed child-division
  opens, and history navigation now use the managed overview status line.
- Irreversible child-division launch confirmation remains unchanged.

### Fighter Search keeps selection feedback in context

- Profile and comparison actions now report missing selections in the directory's
  own action strip while preserving identity-safe paging and scouting visibility.

### Promoter Dashboard decision details explain impact

- Selected notices now show priority, consequence, and next destination in a
  separate readout beside the original message.
- The explanation stays source-bound and read-only; selecting a notice never
  resolves it or changes the underlying inbox, calendar, or finance state.

### Company and Region pages surface actionable next steps

- Industry standings and Region profiles now explain their current operational
  constraint or opportunity and link to the relevant management reader.
- Links are source-bound and revalidated against the selected company/region,
  keeping duplicate identities and list refreshes from redirecting actions.
- The cards remain read-only until the player enters the linked workflow.

### Staff desk surfaces its next safe action

- Staff now highlights the first actionable concern—expiring contracts, evidence
  exceptions, role coverage gaps, or queued briefs—and links to the matching
  review surface.
- The card revalidates saved staff identities and policy state before opening;
  it never hires, fires, negotiates, commits work, spends cash, or grants
  delegated authority by itself.
- Scouting's Target Board now prioritises recommended targets, stale intel,
  active assignments, and decision packs with the same identity-bound review
  links; it never commissions a report or changes the shortlist automatically.
- World Hub now connects selected promotion, story, and gym context through a
  read-only next-step card, rechecking the source row before opening Chronicle,
  company, gym, story, or Scouting readers.

### Simulation Lab adds an evidence-backed 100-year audit

- The audit surface now offers a canonical deterministic 100-year observer run
  alongside the configurable play-audit recipe.
- Yearly checkpoints retain title-holder, regional-feeder and bounded-history
  metrics in addition to population, finance, Academy and combat-sport health.
- A pure qualification pass stops only the isolated audit for extinction,
  title starvation, runaway cash or unbounded history, while lower free-agent
  and feeder supply remains an actionable warning. No active career save is
  repaired or changed.

### MMA child promotions support future-dated cards

- The child-promotion manager now commits an exact future month/week and
  optional card name to the shared calendar.
- Planning is read-only for roster, camps, finance, and RNG; the normal AI
  matchmaking and settlement pipeline runs only at the due boundary.
- Explicit plans bypass the probabilistic show roll, while readiness, matchup,
  and budget failures remain visible as `Needs replacement` with an Inbox task.
- Cancellation resolves by saved promotion/event identity, and legacy plans
  receive deterministic IDs only at the load/migration boundary.

### Grand Prix super-events use an idempotent execution loop

- Accepted milestone invitations retain their canonical deposit, setup,
  security, revenue, and revision terms through card build and settlement.
- Card validation, stale-project guards, malformed-evidence handling, and
  terminal Success/Failed/Cancelled/Expired history now share one closeout
  boundary, so rewards and company effects cannot apply twice.

### Explicit migration for legacy career profile labels

- Game & Saves now offers **Migrate Profiles** for a selected save slot.
- Known historical labels (`Standard Prime`, `Early Peak`, `Late Developer`, and
  `Long Prime`) are converted to the current career-arc names across player,
  promotion, retired, and combat-sport fighter rows.
- The migration is opt-in, leaves healthy slots untouched, records each source
  path in the migration metadata, and requires a recovery backup before writing.

### Profile injury risk reflects live trait effects

- The legacy roster detail now shows `effective_injury_tendency` with the
  retained base risk beside it.
- Profile and roster readers remain presentation-only; saved baselines are not
  rewritten when traits evolve.

### Company-card replays keep promotion identity

- New AI and independent replay packages retain a stable promotion reference.
- Company-card view/watch actions resolve that reference from the selected
  standings row; duplicate same-name promotions cannot open one another's
  archive.
- Legacy name-only archives remain readable for unique rows and fail closed when
  the source company is ambiguous.

### Company Rankings keep duplicate promotion data attached

- Company Rankings now build each row from its own promotion object and stable
  entity ID instead of looking up the first matching display name.
- Duplicate same-named promotions retain independent roster strength, cash and
  power values through refresh and selection.

### Ranking scopes disambiguate duplicate promotions

- Promotion scope choices now retain the selected source promotion, adding a
  readable region/identity suffix only when names collide.
- Scope selection is restored by source identity when a promotion's display
  label changes after a refresh.
- Company and P4P/divisional rank maps use stable promotion keys, preventing a
  same-named promotion from inheriting another roster's rank.
- ID-less legacy promotions use a roster-bound fingerprint so identical names
  and regions remain separate ranking groups.

### Finance milestone outlook tolerates malformed history

- Milestone projections now use bounded values and valid retained rows only.
- A malformed finance envelope or mixed legacy history cannot crash the
  Finance outlook or trigger a repair while the page is opened.

### Finance readers keep strategic-investment projections non-mutating

Finance, runway and dashboard repainting now calculate strategic-project
ownership and upkeep through an explicit non-repairing projection. Malformed
or legacy finance envelopes remain untouched while the reader shows bounded
values; investment purchases and calendar upkeep retain the repairing action
boundary.

### Regional Prospects throughput stays observational

Regional Prospects now calculates its graduation-throughput summary through a
non-repairing backlog projection. Refreshing or filtering the table no longer
writes the monthly eligibility cache or repairs malformed rules; calendar and
simulation progression retain ownership of that cache.

### Rules Help tolerates malformed legacy rules

- Help-page construction and filtering now read the saved UI-search preference
  only when the retained rules envelope is a mapping; a malformed envelope is
  left unchanged while the authored catalogue remains searchable.

### Media capacity reads no longer repair saves

- The read-only weekly action-capacity helper now consumes the non-repairing
  finance projection, so stale or malformed media state is not seeded or
  advanced by opening the Media Desk.

### Company standings keep duplicate entities distinct

- Industry standings now bind rows to stable promotion or sport-scoped circuit
  identities behind the visible company name.
- Sorting/filtering restores the selected source row, and duplicate names no
  longer redirect the profile, trend or power-breakdown reader.
- Legacy name-only restoration succeeds only for a unique visible match.

### Staff autonomy selector remains a read-only projection

- Refreshing the Staff page now reads the retained company/department mode
  directly instead of normalizing the save-owned policy envelope. Malformed
  legacy policy remains unchanged until an explicit policy edit.

### Combat Sports history opens use athlete identity

- Records/History ranking rows now resolve the selected athlete through saved
  title/fighter IDs; a display-name fallback is accepted only when unique in
  the selected circuit, preventing duplicate-career profile misrouting.
- Title lineage, finance and event archive tabs now render malformed retained
  rows as explicit unavailable evidence with safe dates and amounts instead of
  crashing the reader or dropping the source record.

### Media Desk selectors keep fighter identity

- Spokesperson and same-division target selectors now retain source fighter
  maps, preserve choices through refreshes and add readable suffixes for
  duplicate names.
- Campaign and callout actions resolve through those maps before any unique-name
  compatibility fallback, preventing same-name misrouting.

### Regional Prospects keep promotion/fighter identity

- Regional Prospect rows now use a source-bound promotion/fighter key with
  deterministic duplicate handling instead of a sorted-position suffix.
- Filtering or status changes restore the selected prospect only when the same
  regional source remains visible; rating visibility still uses that row's
  promotion.

### Awards and records keep fighter identity

- The Records leaderboard now retains source fighter rows, restores the selected
  athlete after filter/category refreshes, and opens profiles by `fighter_id`.
- ID-less legacy rows use deterministic fingerprints; duplicate names are never
  resolved from the visible leaderboard name or sorted position.

### Achievements keep historical fighter identity

- New fighter achievement entries retain the saved `fighter_id` and deduplicate
  by that immutable identity, allowing same-named careers to earn independent
  milestones.
- The achievements window resolves profiles through the identity-aware award
  resolver; id-less legacy names open only when unique and otherwise fail closed.

### Legacy profile links fail closed on duplicate names

- Ranking detail, rivalry summaries and generic tree-profile compatibility
  links now share a unique-name resolver, so a missing or duplicate display
  name cannot redirect the player to the wrong career.

### Fight History keeps opponent snapshots matched

- An unidentified legacy bout no longer borrows the first same-date rating
  snapshot when several fights share a date; the reader shows the explicit
  unavailable state until a matching opponent identity is present.

### Staff legacy rows keep their source across sorting

- Staff roster, candidate and expiry tables now fingerprint populated legacy
  records instead of embedding the current sorted position in their row key.
- Completely empty malformed rows remain visible behind an explicit
  non-durable fallback; saved `staff_id` rows and duplicate suffix handling are
  unchanged.

### Roster refreshes keep the selected fighter

- The player roster reader now restores its selected fighter by source identity
  after filters and status sorting rebuild the table, refreshing the detail card
  for that same career.
- A fighter that is filtered out or has left the roster is cleared rather than
  silently replaced by the first visible row.

### Market refreshes keep the selected free agent

- Scouting, status and closed-division filter rebuilds now restore the selected
  free agent by source identity when that career remains visible.
- If the candidate is filtered out, the market scouting panel stays unselected;
  refreshing the reader never tops up the pool or mutates market state.

### Legacy Ledger keeps duplicate companies separate

- Company-era rows now use promotion IDs or deterministic name/region keys with
  duplicate suffixes, so identical company names cannot collide in the records
  window or redirect an era detail panel.

### Academy rival programs keep promotion identity

- The rival-program reader now keys rows by promotion identity with deterministic
  duplicate suffixes, preventing same-name companies from colliding during a
  redraw.

### Media campaign history keeps source identity and readable detail

- Campaign history rows now prefer saved campaign/operation/evidence IDs, with
  content-bound fingerprints and deterministic duplicate suffixes for legacy
  rows instead of positional indexes.
- Refreshes retain the selected campaign when its source identity remains
  visible and show a read-only detail strip with the full retained outcome.

### Fighter Search keeps identity across pages

- Search rows now use saved fighter/company identities, deterministic legacy
  fingerprints and duplicate suffixes instead of visible global indexes.
- Refreshes restore a selected fighter only when that source identity is still
  visible, keeping profile and comparison actions correctly bound after
  pagination or filtering.

### Scouting selection details stay observational

- Regional Prospect selection, Scouting Target Board summaries and comparison
  uncertainty text now read the retained dossier with `migrate=False`.
- Selecting or comparing a fighter cannot run scouting migration/repair or
  rewrite saved report evidence; explicit report assignment and load/migration
  remain the write boundaries.

### Academy rival-program tables stay observational

- The Academy's rival-program list now reads a defensive projection when an
  older promotion has no youth-program block. Opening or refreshing the page
  cannot seed a promotion strategy or persist default rival-academy fields.
- Calendar progression and explicit rival-academy actions retain the existing
  repair/mutation path.

### Detailed-skill readers no longer generate legacy ratings

- Fighter stats, detailed-skill popups and profile skill cards now consume a
  deterministic read model. Opening a profile cannot call the RNG-backed
  ``ensure_detailed_skills`` repair owner or write to the fighter.
- Empty legacy profiles show a bounded projection from their saved broad
  ratings; partial profiles preserve their saved values and derive only the
  three known compatibility fields for display. Explicit load, editor,
  simulation and migration paths retain ownership of persisted repairs.
- Profile cards and the detailed-skills popup label that legacy projection so
  an estimate is not mistaken for a newly persisted scouting report.

### Combat Sports readers preserve saved circuit state

- The Combat Sports overview and records/history pages now use non-mutating
  projections for legacy circuit data. Opening or refreshing them cannot assign
  sport weight classes, crown champions, or rewrite ranking/record caches.
- Explicit management, migration and simulation paths retain ownership of those
  repairs and gameplay changes.
- Company standings and the selected Combat Sports company profile use the same
  projection boundary, so switching or refreshing company context cannot trigger
  circuit migration either.

### Rankings refresh no longer rewrites fighter rank snapshots

- The Rankings page calculates its visible ordering and scopes from retained
  rows without calling the ranking mutation owner. Stored current/previous
  movement evidence remains intact until calendar or simulation processing
  records a new boundary.

### Company readers no longer seed missing promotion strategy

- Child-promotion management, company profiles and selected-company data now
  read a defensive strategy projection. Opening or repainting those readers
  cannot generate a new strategy or add compatibility defaults to a saved
  promotion.

### Rankings hide private score fields under Scouting Mode

- Divisional and Pound-for-Pound rows now show `Hidden` for the derived score when the fighter's private ratings are not visible.
- Public rank, movement and scouting-safe OVR estimates remain available; sorting and ranking ownership are unchanged.

### Regional Prospects refresh keeps assessment read-only

- Regional Prospects now reads stored history baselines without repairing fighter fields while rebuilding the table.
- Simulation and explicit migration paths retain the established baseline owner; the UI uses a bounded non-repairing assessment projection.

### Matchmaking draft refresh preserves saved card order

- Matchmaking now applies main-event/tier presentation metadata to shallow display copies during refresh.
- The saved draft fights and their identity map remain untouched until an explicit add, move, edit, or remove action.
- Available-fighter refresh no longer repairs the transient event-name field as a side effect.

### Game & Saves stays a library reader

- Refreshing Game & Saves no longer creates the active-universe marker, normalizes the default database, or repairs autosave rules as a side effect.
- The selected database still shows the saved marker when present and otherwise labels the packaged Default Universe without writing it.
- Existing load, copy, delete, backup, folder and new-game actions remain the explicit mutation boundaries.

### Finance page is now a read-only projection

- Finance refreshes no longer call the save-repairing defaults helper or write derived payroll/offer keys back into the retained envelope.
- Annual outlook rows are projected locally from saved history and the current boundary; opening Finance cannot seal or overwrite annual records.
- Malformed finance containers remain visible through bounded values and explicit unavailable rows while existing commit/calendar owners keep the repair boundary.

### Upcoming cards reader no longer rewrites events

- Upcoming now projects generated event names and local card ordering onto a display copy, keeping the saved scheduled event and its fight order untouched during refresh.
- Edit/cancel actions still use the original event through the stable identity map.
- Booking economics forecasts use the same non-repairing projection path, so malformed finance defaults cannot be normalized just by opening Upcoming.
- The broadcaster status strip now treats malformed saved media-rights data as unavailable instead of raising during a booking-page repaint.

### Regions profile reader health

- Regions now projects malformed or incomplete profile rows into an explicit unavailable/neutral view instead of crashing the profile pane.
- Lists, teams and numeric regional indicators are bounded on display copies; the retained region catalogue is never repaired by refresh.

### Finance runway malformed-envelope guard

- Cash-runway forecasts now keep working when a legacy finance container is malformed, using a bounded office-cost fallback without repairing the retained save.
- The projection remains state/RNG stable and labels missing rights as zero rather than crashing the Finance view.

### Company profile story reader boundary

- Company profile follow state now reads the defensive subscription projection, so opening a company cannot repair malformed follow/snooze rows.
- Follow and unfollow remain explicit mutation actions and continue resolving the saved subscription identity after the write boundary.

### Results landing reader health

- Results now projects malformed retained shelf rows into explicit unavailable entries instead of crashing or silently dropping them.
- Valid result identities and read-only detail routing remain unchanged; the raw malformed value is retained inside the display diagnostic for load/migration review.

### Fighter Search history reader boundary

- World Fighter Search now derives universe-record display from the profile's non-mutating history-baseline projection; paging/searching no longer writes imported-record metadata.
- Existing historical values remain unchanged and the fallback is bounded to the saved baseline fields.

### Scouting evidence parsing is fail-closed

- Malformed report confidence and completion dates no longer crash Scouting readers or get coerced into fresh evidence; confidence falls back safely and invalid dates remain reviewable.
- Current/stale/full report predicates and estimates preserve the retained report while refusing to infer a date or discovery state from invalid data.

### Scouting Target Board reader boundary

- Target Board and Scouting Centre refreshes no longer call the scouting migration path or normalize saved reports/searches just to render a page.
- Report rows, search rows and malformed containers are projected defensively; scouting estimates use retained evidence without commissioning, rerolling or changing shortlist state.

### Scouting decision-pack reader boundary

- Decision Packs now refresh from retained rows without invoking the normalizer; malformed rows remain visible as unavailable review entries.
- Pack selection uses saved IDs or deterministic legacy fingerprints, duplicate rows receive suffixes, and a refresh cannot redirect an edit to another pack.

### Followed-story readers stay non-mutating

- Fighter profile follow-state checks now use the defensive subscription projection; opening a profile no longer normalizes malformed snooze/cursor rows.
- Followed Story Briefings skip malformed retained thread rows safely while keeping the raw story shelf unchanged. Explicit follow, acknowledge, snooze, and unfollow actions remain the write boundary.

### Defensive World Hub promotion reader

- World Hub promotion rows now use stable promotion identities with deterministic duplicate handling, so a refresh or sort cannot redirect the selected company.
- Repainting the World Hub no longer repairs a missing legacy `show_history` container; malformed history remains an empty, read-only display until the sanctioned migration boundary.

### Inbox reader preserves legacy evidence

- Inbox refreshes now project missing dates/read flags onto display copies and
  keep malformed rows explicitly unavailable; the retained message shelf is not
  normalized or rewritten just by opening the page.
- Source-bound message maps still route Resolve, medical decisions and context
  actions to the original saved item, while sorting/filtering uses the safe
  projection. Added no-mutation and legacy-row coverage.

### Drug Testing case-reader health

- The frozen Drug Testing Cases reader now reports malformed retained case
  envelopes explicitly as `Unavailable` or `Needs review` without invoking the
  repair initializer.
- Empty states distinguish “no cases recorded” from unavailable saved evidence,
  while the existing screening timeline and no-injury/no-sanction boundary stay
  intact. Case timeline and table accents now use the shared semantic palette.

### Staff policy readers are non-mutating

- Staff summary, effective-autonomy and management-panel refreshes now read
  retained policy data defensively instead of invoking the save-repairing
  initialiser.
- Malformed legacy briefs, exceptions, caps or modes remain available to the
  evidence audit and render safe fallback values; explicit policy/brief actions
  keep the existing write boundary.
- Added regression coverage proving summary/autonomy reads preserve malformed
  envelopes byte-for-byte.
- Brief rows now retain the saved brief ID through refreshes, add deterministic
  suffixes for duplicate IDs, and route Commit/Requeue/Cancel through the saved
  row map instead of treating a Treeview position as identity.

### Identity-safe Media Rights offers

- Media Rights market rows now use saved offer IDs or deterministic term
  fingerprints for their Treeview identities, with duplicate suffixes and an
  explicit malformed-row state. Refreshes retain the selected offer by source
  identity instead of a visible position.
- Accept, counter and reject resolve the selected row through the saved offer
  map; id-less legacy rows remain readable but cannot be mutated accidentally.
  The existing offer, contract and settlement field shapes are unchanged.
- Media offer, plan, booking and UI/story regression coverage now passes 167
  focused tests.

### Identity-safe Booking Workbench commits

- Booking Workbench and locked-slot replacement actions now commit by the saved
  candidate fighter identity after refresh, rather than trusting a visible option
  position. Missing or duplicate candidate IDs fail closed with no booking,
  replacement, receipt or RNG draw; the existing index API remains only as a
  compatibility boundary for older callers and fixtures.
- Malformed saved alternative rows remain reviewable as unavailable entries, and
  medical drafts continue to use the selected stored row without re-reading it by
  a mutable index. Added order-change and duplicate-identity regressions.

### Defensive Dashboard readers

- Dashboard result/change panels now use bounded, non-mutating read projections.
  Valid retained rows are copied for display, while malformed result containers,
  journal rows and scouting report/search entries produce explicit unavailable or
  review evidence instead of aborting a refresh or opening a different record.
- Spectator cards keep their newest-result and newest-story ordering, and the
  existing notice selection/content identity remains unchanged. Refreshing the
  dashboard never repairs the source journal, result archive or scouting data.
- Added regression coverage for malformed dashboard inputs, source immutability
  and helper routing; the focused UI/story suite now passes 101 tests.

### Defensive story and chronicle readers

- Storylines, World Chronicle and World Hub news now consume bounded presentation projections
  instead of raw retained rows. Malformed legacy entries remain visible as
  `Needs review`/`Unavailable` with an explanation, while stable story and
  chronicle identities, recorded beats, and context links remain intact.
- Reader refreshes never repair, deduplicate, regenerate or mark stories read;
  repair remains confined to the explicit load/migration or follow/acknowledge
  mutation boundaries.
- Story and Chronicle context links prefer saved fighter IDs and refuse
  ambiguous name-only company links, so duplicate display names cannot open the
  wrong profile or company.

### Named scouting watchlists

- Scouting now supports explicit named watchlists alongside the compatibility
  Main Watchlist. Players can create, rename, switch and archive lists from
  the Target Board; archived lists retain their fighter IDs, reports, alerts
  history and decision-pack references.
- Watchlist actions resolve by saved watchlist ID, keep duplicate display names
  distinguishable, and never commission free scouting, sign a fighter or
  delete evidence. Malformed legacy rows remain untouched for the normal
  migration boundary.

### Fight Night control centre

- Fight Night now opens on a clear control-centre landing page with separate
  actions for the due live event, the Results database, retained replays and
  the complete office/event log.
- Recorded cards are indexed by their saved result identity and labelled
  `Replay ready` or `Results only`; double-clicking a row opens only the
  preserved card data and never reapplies results or regenerates a replay.
- The original event log remains complete and read-only, with the existing
  semantic colours for headers, results, key moments and office actions.

### Tournament history reader safety

- Tournament History now keeps editions with malformed retained draw, seed,
  entrant or stage containers addressable and marks the affected fields for
  review instead of crashing or inferring bracket results. The permanent index
  writer also preserves malformed tournament evidence under a legacy-raw key.
  Review-required rows carry a visible table status as well as detail text.
  Valid historical facts and replay links remain unchanged; the reader stays
  read-only.

### Finance and Media read-model resilience

- Finance history, annual outlook and transaction detail now retain malformed
  legacy rows as explicit Unavailable/Unknown evidence instead of aborting a
  refresh or coercing bad amounts into real cash movements.
- Finance reconciliation reports a diagnostic for malformed retained values;
  valid balances are unchanged and no repair or settlement is triggered by a
  reader.
- Media Rights dashboard, offer comparison, contract terms and settlement
  receipt readers now tolerate malformed legacy contracts, offers, campaigns
  and receipts while keeping the original payload available for review.
- Rights/offer cards preserve stable receipt identity and show clear review
  states for unavailable rows; opening the Media desk remains read-only and
  does not generate offers, charge cash or consume RNG.
- Added regression coverage for malformed rights numbers, finance history
  projections and retained Media receipt/offer rows.
- The 12-week cash-runway table and summary now fail closed on malformed
  forecast rows/sections, with duplicate-offset protection and no change to
  the planning-only, no-reservation contract.
- Saved scenario review keeps malformed snapshots visible, bounds displayed
  assumptions and preserves duplicate IDs without applying edits or rerunning
  the forecast.
- Sponsor settlement activation now fails closed on malformed legacy duty
  evidence, preserving the existing full/half-fee rule and explicit at-risk
  explanation.
- Media settlement receipt tables/details now tolerate malformed numeric
  amounts and sponsor-duty rows while retaining selectable identities and the
  complete stored payload.
- Owner Objectives now keep malformed collections/rows visible as explicit
  review evidence, with invalid deadlines bounded in the reader and no
  boundary observations created by refresh.
- The explicit Media state boundary preserves malformed non-list contract and
  offer containers under legacy-raw keys before preparing safe working lists;
  valid legacy alias migration remains unchanged.
- Media settlement now treats malformed rights/package metrics, sponsor fees,
  relationship values and delivery-strike evidence as explicit review data:
  valid fields retain their normal transitions, invalid fields are not guessed
  over, and an audience/receipt row is still retained instead of crashing the
  event boundary.
- Monthly media expiry and successor activation now hold malformed entitlement
  terms for review instead of guessing a deadline or activating an invalid
  renewal; the original evidence stays intact.
- Active rights lookup is now a pure read for dashboard/account paths, so a
  missing legacy finance envelope is not created just by opening Media.
- Custom rights packages with malformed numeric or market fields now receive
  bounded authored fallbacks during media-system initialisation rather than
  aborting startup; valid custom package values remain unchanged.
- Rights history no longer labels a row `Fulfilled` when its retained month,
  entitlement or strike evidence is malformed; such rows are explicitly
  `Needs review` until a sanctioned repair boundary supplies the facts.
- AI media-deal review now refuses malformed saved offers without discarding
  their raw evidence; valid sibling offers remain scoreable and malformed
  promotion strategy/size values are bounded before selection.
- Monthly media-market refresh keeps malformed outlet volatility, budget and
  base-fee fields unchanged, marks the outlet for review and uses safe values
  only for the market snapshot instead of crashing or guessing over legacy
  evidence.
- Media offer expiry preserves malformed rows and unknown expiry evidence for
  review. The signing owner now requires a stable outlet identity and positive
  numeric terms before accepting an offer, so malformed legacy offers cannot
  spend cash or create a contract.
- Sponsor and Media Rights counteroffers now fail closed on malformed numeric
  terms or company evidence. The original offer remains available with an
  explicit review marker; valid one-counter negotiation and fee/guarantee
  synchronisation are unchanged.
- Monthly sponsor and legacy media-rights expiry now holds malformed terms for
  review and leaves their raw values untouched; valid agreements still expire
  once through the existing calendar owner.
- Regional-host invitation expiry, exact-date binding and settlement now fail
  closed on malformed retained dates, guarantee terms, result containers or
  cash values. Invalid invitations remain reviewable without event mutation or
  payout; valid one-entitlement and zero-penalty flows are unchanged.

### Company hub legacy-data display safety

- Company detail readers now keep malformed history, finance containers and
  Staff rows visible as explicit unavailable/Unknown evidence rather than
  raising during a profile refresh.
- Valid company summaries retain their existing values and actions; no
  roster, finance, contract or promotion state is repaired by opening the hub.
- Added a UI-data regression guard for the defensive projection.

### Media sponsor preview clarity and legacy safety

- Active sponsor cards now show the contracted maximum, current estimated
  payment, explicit full/half-fee basis, term and activation reason together.
- Sponsor preview and comparison readers tolerate malformed legacy trust,
  stability, fee, term, roster and campaign-history values without repairing
  finance state or consuming RNG; unknown inputs remain a bounded review state.
- Added regression coverage for malformed sponsor previews and read-only state
  preservation.

### Foundation and Staff receipt diagnostics

- Foundation work-card readers now retain bounded numeric values plus explicit
  Recorded/Unknown states and data-quality diagnostics when legacy attempt,
  quote or spend fields are malformed.
- Staff budget checks skip malformed calendar boundaries and spend values
  without mutating the retained work log; receipt tables and detail readers
  display Unknown instead of crashing or implying a real zero.
- Added regression coverage for malformed receipt amounts, UI-safe detail
  formatting and budget-reader purity.

### S4 Staff employment evidence audit

- The read-only Staff Evidence Audit now checks the employed roster and shared
  market together for malformed rows, missing roles, missing IDs, invalid or
  negative salary/contract terms, and stable-ID collisions across sources.
- Findings retain a source label and Staff ID, and the audit window exposes
  those fields with horizontal scrolling; no compatibility repair, reassignment,
  payroll charge or RNG work occurs while the audit is opened or refreshed.
- Added regression coverage for cross-source duplicate IDs, malformed
  obligations, missing identity/role evidence and audit purity.

### S4 staff contract reader safety

- Staff term/status readers now fail closed on malformed legacy contract
  months, keeping pages and Academy Coach assignment checks usable without
  rewriting the retained value.
- Staff roster, candidate, expiry and specialty tables now keep malformed
  rows visible as unavailable evidence instead of crashing on missing or
  non-numeric fields; selected malformed rows fail closed rather than opening
  a guessed employee profile.
- Contract target/offer scoring and expiry-warning readers now tolerate
  malformed salary, reputation, heat and term values, including non-dictionary
  legacy rows, without changing the retained evidence.
- The calendar contract tick now holds malformed terms for explicit review and
  keeps non-dictionary rows in place; payroll aggregation skips invalid rows
  instead of crashing or rewriting their evidence.
- Finance cash-runway and roster-cost snapshots use the same defensive payroll
  projection, so malformed salary evidence cannot crash a planning refresh or
  change the saved staff row.
- AI monthly operating costs now apply that boundary too: malformed salary rows
  remain in the promotion for audit and contribute no invented payroll, while
  valid staff salaries continue to be charged normally.
- Staff compatibility-profile loading now tolerates malformed role and skill
  fields without changing those raw values; derived specialty/reputation data
  uses a bounded fallback so legacy saves still open safely.
- Staff skill, role-info and scouting-capacity/workload helpers now ignore
  malformed rows and values safely, keeping scouting and negotiation surfaces
  usable without reassigning a saved employee.
- Finance refresh and month-end office/payroll work use the same defensive
  Staff projection, so malformed employee rows cannot break the Finance tab or
  the calendar's recurring-cost task.
- Added regression coverage for read-only malformed-term, table-projection and
  contract-offer handling.

### A3 cash-runway malformed event-date revision safety

- Scenario source revisions now sort malformed legacy event dates defensively
  and expose a diagnostic count instead of crashing during save/read.
- Raw scheduled events retain their original values; malformed dates do not get
  coerced or silently repaired by the planning view.
- Added regression coverage for scenario saving and schedule purity.

### A1 owned-calendar malformed-source diagnostics

- The read-only Owned Calendar index now exposes indexed/shown counts and a
  partial-coverage diagnostic when legacy schedule containers or event rows are
  malformed. Valid event identity, owner/source separation and filtering are
  unchanged.
- The dashboard summary surfaces malformed source-row counts without repairing,
  resolving, rescheduling or mutating any calendar data.
- Added regression coverage for mixed valid/malformed MMA and child schedules.

### S3 progression malformed-work evidence

- Quarter-close staff progression now ignores malformed legacy spend/month
  evidence instead of crashing or allowing it to qualify a skill gain.
- Invalid rows remain retained in the work ledger for audit; malformed skill
  baselines fail closed without changing the employee.
- Added regression coverage for valid qualification alongside malformed work
  rows.

### S4 AI staff expiry malformed-term retention

- The AI staff contract worker now holds malformed legacy contract terms for
  explicit review instead of silently expiring or dropping the employee.
- Non-dictionary legacy staff rows are retained for audit, malformed-term
  notices are emitted once per calendar month, and payroll projection remains
  defensive. Valid expiry and market return behavior is unchanged.
- Added regression coverage for retention, no duplicate notices and no data
  loss.

### J4 membership archive malformed-row diagnostics

- Membership retention summaries now report valid versus malformed retained
  rows explicitly. The Profile Company History header surfaces the malformed
  count so a shorter readable interval list cannot be mistaken for lost data.
- Raw append-only membership rows remain untouched; readable event pages still
  filter safely and no migration/repair runs during refresh.

### S4 AI staff affordability legacy-salary safety

- AI staff runway previews now tolerate malformed legacy salary fields without
  mutating or dropping the retained employee record. The invalid salary is
  treated as zero for this diagnostic projection and remains available for
  explicit review.
- Added regression coverage for malformed salary evidence and state/RNG purity.

### J1 booking-workbench malformed option safety

- Booking proposal readers now render malformed option lists/rows as explicit
  unavailable evidence instead of raising or silently dropping alternatives.
- Booking and locked-slot commits preflight the retained option before the
  write-boundary normaliser, so a rejected malformed click cannot rewrite the
  raw proposal or route to a different fighter.
- Added regression coverage for malformed proposals and preserved the valid
  deterministic commit path.

### A2 super-event malformed-evidence closeout safety

- Super-event closeout now uses bounded numeric fallbacks for malformed legacy
  terms and fails closed with `Needs review` when settlement attendance,
  capacity, profit or excitement evidence is invalid.
- Valid projects retain the existing one-time closeout and effects; malformed
  packages apply no popularity, stability, safety, cash or history effects.
- Added regression coverage for malformed terms and settlement packages.

### A3 cash-runway legacy transaction diagnostics

- Hardened the read-only cash-runway forecast against malformed or
  non-dictionary legacy `week_transactions` rows. Valid entries still feed the
  paid-to-date figure; invalid entries are excluded without coercion or save
  repair and are called out in the forecast's unknowns evidence.
- Added regression coverage for malformed dates, costs and row shapes, with
  state/RNG purity assertions. No settlement, finance write or EXE path was
  changed.

### J4 membership identity diagnostics

- Membership retention diagnostics now distinguish blank legacy IDs from true
  duplicate durable IDs. ID-less rows remain visible as incomplete evidence,
  while only repeated non-empty IDs are reported as identity collisions. The
  Company History header now exposes the count of ID-unknown rows explicitly;
  malformed month values are likewise classified as date-unknown. Per-fighter
  membership totals now walk all retained pages instead of reporting only the
  first 100 rows. Interval projections reject boolean, float and malformed-week
  values consistently with the summary diagnostic.

### J4 malformed title-history migration safety

- Lineal title-history migration now preserves malformed or forward-compatible
  legacy rows instead of crashing while rebuilding trustworthy entries. Such
  rows remain available for review, are excluded from lineal action counting,
  and cannot prevent the migration version from being sealed.

### J4 title-lineage reconciliation safety

- Title reconciliation now retains a name-only `Vacated`/`Interim Vacated`
  fact when a saved holder is missing from the roster, instead of silently
  dropping the lineage evidence. Duplicate-name primary holders fail closed
  unless exactly one fighter carries the explicit champion marker; an
  unambiguous marker is used to preserve the correct holder. Reconciliation is
  idempotent and never guesses a stable fighter ID.

### J4 complete-history pagination

- Profile Company & Title History now derives the full retained reader payload
  and presents it in 250-row pages. Selection remains source-identity bound,
  while Copy Details and Export JSON continue to include every recorded row.
  Paging does not rewrite, truncate or fabricate history.

### J4/L0 malformed membership archive state

- Membership retention diagnostics now distinguish malformed historical and
  top-level envelopes from a genuinely empty archive. The summary and Company
  History header show an explicit unavailable state and keep repair confined
  to the sanctioned load/migration boundary; no retained payload is rewritten.

### J1 booking workbench review purity

- Brief, proposal-review and tentative-medical readers now project the raw
  saved envelope defensively. Malformed legacy rows return an explicit empty
  or unavailable view instead of being normalized during refresh; explicit
  save/generate/commit actions retain the existing write boundary.

### J1 booking draft reader purity

- Draft Review no longer runs the Foundation booking-ID migration while merely
  reading the card. Legacy rows are shown as explicit `Legacy reference`
  entries until the sanctioned load/migration boundary assigns a durable ID;
  refreshes cannot add IDs, reorder the card or retarget a later edit.

### J4 duplicate-name interim title safety

- Interim-title clearing now uses the explicitly marked holder when duplicate
  names exist, and fails closed when a legacy name-only map cannot
  disambiguate. The cleared lineage row records the holder's stable fighter
  ID; no unrelated fighter flag or title history is changed.

### S3 observed Staff retention evidence

- Staff progression snapshots now include structured workload, saved-pay and
  role evidence alongside the existing contract/morale concerns. Staff Profile
  shows those facts and explicitly states that review is manual; no morale
  change, departure threshold or automatic departure is inferred. Malformed
  salary and missing-role legacy values remain bounded and readable.

### J4 membership timeline conflict evidence

- Membership interval readers now detect retained records whose end date
  precedes their start date, or whose date fields are malformed. Both source
  facts remain available, the derived interval is labelled incomplete and the
  Company History detail view explains the conflict instead of presenting an
  impossible range as recorded history. Same-week join/leave/return boundaries
  remain separate and as-of filtering stays read-only.

### S4 AI staff runway evidence

- Added a pure AI-employment affordability projection for the six-month
  recurring-cost rule. The Staff ledger now shows each non-feeder promotion's
  cash, monthly recurring basis, six-month runway and no-revenue runway
  months; it never refreshes, hires, spends, repairs IDs or consumes RNG.
  Market simulation reuses the same projection so the visible evidence and
  hiring gate cannot drift.

### S3 Staff progression reader safety

### S3 Staff progression reader safety

- Staff progression and retention snapshots now read the retained ledger
  defensively. Malformed legacy evidence remains visible without refresh-time
  normalization; quarter-close progression still uses the explicit mutation
  boundary.

### J4 Combat-sport membership history

- Combat-sport crossover, player release, private-market signing, flagship
  buyout and academy-child graduation now append identity-linked membership
  facts before changing roster ownership. Child and flagship transitions retain
  both sides of the move and use retry-safe transition keys; AI replenishment
  continues to record each generated join.

### J12 Current-career editor identity and transfer history

- The in-game World Editor now preserves fighter selection across filtering and
  sorting with stable owner-aware row identities. Employer changes append the
  destination membership fact at the transfer boundary; Free Agent remains an
  explicit no-company state.

### J12 Database Editor opening preflight

- Added a read-only Preflight action that separates structural schema/reference
  errors from playable-risk warnings. It reports thin divisions, missing
  matching free-agent depth, explicit payroll above company cash, and optional
  parent/title references with evidence and a suggested remedy. Preflight
  works on a copied candidate and never repairs, signs, deletes, spends or
  saves the live database.

### J12 Database Editor identity safety

- Standalone Database Editor fighter/company selection now follows the source
  record across sorting and filtering instead of retaining a stale visible
  index. Saved IDs are preferred; legacy records receive deterministic
  non-durable keys and duplicate rows are suffixed. Delete, add and duplicate
  actions resolve the selected object before mutation, preventing a refresh
  from retargeting another record. Added five identity regressions.

### S4 AI staff expiry compatibility

- Expired AI employees from legacy saves that lack a durable `staff_id` are
  now returned to the shared market as explicit legacy records instead of
  being silently dropped. Stable-ID rows keep retry-safe de-duplication, while
  duplicate ID-less rows remain separate. Added regression coverage for the
  retained market row and its legacy identity label.

Super-event completion now seals the terminal project state before applying
popularity, stability, safety or news effects. A stale/revision-conflicted
completion therefore returns Needs review without leaking derived changes.

Region Hub local-gym actions now use stable gym identities, including
deterministic duplicate handling, and the Events tab no longer adds a blank
placeholder row when it has scheduled events.

The Regions reader now binds selection to stable region keys. Profile, Region
Hub and Feasibility actions cannot follow a stale visible row after refresh or
reorder; older widgets fall back only to a still-valid displayed region.

Super-event closeout now has an explicit terminal-state guard. Stale
Completed/Failed/Cancelled/Expired offers cannot be closed again, completion
requires a scheduled project, successful outcomes retain the existing
`Success` wording while recording canonical `Completed` state, and the
booking owner rejects an unaccepted or terminal project before scheduling.
Lifecycle transitions now use an explicit state matrix; revision mismatches
surface `Needs review` without clearing the live offer, and scheduled-card
cancellation respects that fail-closed result.

The Testing Desk now saves a validated policy selection through a single
configuration boundary. Standard, Strict, Olympic and Off choices increment
revision evidence only when changed; provider-specific detection,
confirmation, appeals and sanctions remain clearly gated until their J5 rules
table is approved. Provider/sample comparisons remain deterministic and
read-only.

Company Milestones & Super Events opportunity rows now route by saved offer
identity. Legacy rows use deterministic terms fingerprints and duplicate IDs
receive stable suffixes; refreshes preserve a still-visible selection and no
action follows a mutable visible row index.

Super-event invitation expiries now use the terminal closeout owner. An
unaccepted expiry records a zero-commitment history row and applies its safety
impact once; a stale planning expiry remains visible with Needs review instead
of orphaning the live project.

The Official Record Book now routes detail selection by stable scope/record
identity rather than a sorted visible row index. World, promotion and event
history remains unchanged, with deterministic suffixes for duplicate display
keys.

Achievements & Milestones now uses stable achievement identities (with
deterministic legacy fingerprints and duplicate suffixes) for detail and
fighter-profile routing. Filtering or refresh cannot redirect a selected
achievement to another visible row.

World Hub news selection now follows source-bound chronicle keys through
refreshes. The reader preserves the selected story when possible and keeps an
explicit non-durable fallback for older windows that predate the keyed map.

Regional host invitations (J9.b) now form a deterministic, persisted planning
lane. At most one offer is issued per quarter when a region has two bookable
local roster fighters and a reliable fixed staging/venue quote. Offers show an
8-week target, four-week acceptance window, venue and capped 10% host
guarantee; accepting only marks the entitlement for ordinary Matchmaking.
Decline, expiry and cancellation record zero-penalty outcomes. A linked event
settles the guarantee once, and only after two local roster fighters complete
bouts; no RNG, deposit, travel charge or duplicate payment is introduced.

Upcoming scheduled-card rows now use saved event IDs (or deterministic,
explicit legacy fingerprints) for selection and action routing. Refreshing or
reordering the upcoming list preserves the selected event, while due-event,
edit, and cancel actions fail closed if their source-bound map is unavailable;
they no longer convert a visible row position into an event index.

The scheduled-card editor now applies the same rule to individual bout rows.
Saved fight/bout/booking IDs are preferred, legacy rows receive deterministic
fingerprints, and duplicate source rows stay distinct. Remove, move, title,
tier, plan, TBA replacement and last-minute replacement actions resolve the
selected bout through the identity map rather than a mutable row number.

Superfight Night's local card builder now uses the same source-bound approach:
fighter-based stable row fingerprints preserve the selected bout through
refresh/reorder, and Remove/Move fail closed instead of converting the visible
row into a list index.

World Chronicle context and detail readers now resolve the selected entry from
its stable chronicle key, with deterministic duplicate suffixes, rather than
indexing the mutable filtered list.

Storylines now uses a defensive story-thread index/read path on page open;
malformed legacy thread collections render as an explicit empty reader rather
than being repaired as a side effect of opening the page.

Booking Workbench Draft Review now routes every selected row through its saved
booking reference instead of the visible row position. Foundation-migrated
drafts keep durable IDs, legacy rows remain explicitly non-durable fallbacks,
and duplicate source IDs receive deterministic suffixes; reviewing or proposing
a locked-slot change therefore cannot retarget another bout after a reorder.
The proposal alternative table follows the same fighter-identity rule, so
refreshing the current eligibility read cannot send Fill or Save Medical Draft
to a different option.
Locked-slot alternatives use the same identity map, keeping the explicit
replacement commit attached to the selected fighter after a stale-data refresh.
The Company Proposal Ledger now preserves its saved proposal identity through
status refreshes, labels missing legacy IDs, and disambiguates duplicate source
IDs without redirecting the detail reader.
The Profile Company & Title History reader now binds selection to a deterministic
fact key, restores a still-visible row after as-of refreshes, and keeps legacy
rows explicitly non-durable instead of indexing into the visible list.
Career Journeys now routes profile and plan actions through fighter identity
keys, retaining explicit legacy fallbacks and duplicate suffixes instead of
using a mutable visible row index.
Finance weekly-history rows now use month/week identity keys (or explicit
legacy fallbacks), preserve selection through refreshes, and read detail from
the saved row map rather than a visible index.
The Matchmaking draft-card reader now binds every row to its saved booking
identity, labels legacy rows as non-durable fallbacks, disambiguates duplicate
IDs, and restores the selected bout after refresh. Remove, compare, fill-TBA,
title-toggle and move-up/down actions resolve the selected fight through that
identity map, preventing a reorder or refresh from retargeting another bout.
Inbox and Owner Objectives rows now use source-bound message/goal keys with
explicit legacy fingerprints and selection restoration; visible-mail actions,
goal navigation and bulk-read operations no longer convert a mutable row
position into a list index. Retired-fighter comeback actions likewise resolve
through fighter identity rows, and Staff actions fail closed when their stable
candidate/member map is unavailable instead of guessing from a visible index.
Media Desk story rows now use saved story/chronicle/event identities (or
explicit legacy fingerprints), preserve the selected story through refreshes,
and route Previous/Next/context actions from the selected entry rather than a
mutable headline position. A small compatibility alias accepts stale
story:<position> selections without inserting positional IDs into the table.
The assistant decision queue and Staff profile readers now require their
source-bound row maps as well; stale numeric selections fail closed instead of
opening whatever record happens to occupy that position after a refresh.
Academy showcase-history and alumni tables now retain selection by their saved
event/fighter/prospect identity (or explicit legacy fingerprints), so replay,
profile and matching-right actions cannot be retargeted by a refreshed list.
Player Combat Sports running-order bouts, scheduled cards and completed-card
rows now use saved identity keys or explicit legacy fingerprints. Remove and
Move actions resolve the selected bout object after refresh/reorder, so a
changed visible position cannot target another matchup.
Fighter Comparison measures now use stable hashed measure IDs and a source-bound
row map. Filtering or refreshing the comparison cannot route the detail panel
through a visible row number, and duplicate labels receive deterministic
suffixes while the full comparison payload remains intact.
Company History intervals now retain the source membership/event IDs that formed
their start and end boundaries. Profile timeline rows carry those identities
through the read-only UI, so an as-of refresh can preserve the authoritative
fact instead of relying on display-text fingerprints; legacy rows remain
explicitly non-durable.
The Drug Testing Cases ledger now treats case rows, summaries and workflow
details as strictly observational. Opening or refreshing the page no longer
normalises a malformed legacy case envelope; the saved evidence remains
untouched for the sanctioned load/migration boundary, with an explicit empty
ledger when the case collection is unavailable.
The Company Proposal Ledger follows the same boundary: its read model reads
the retained append-only collection directly and returns an empty/unavailable
view for malformed legacy history without repairing it during refresh.
Followed Story Briefings and the Storylines reader now use a defensive
subscription projection; malformed snooze/cursor rows remain untouched when
the reader refreshes, while explicit follow/acknowledge/snooze actions retain
the normal repair-and-write boundary.
Foundation work-receipt cards now tolerate malformed numeric fields as an
explicit zero in their diagnostic projection instead of crashing a Staff or
history reader; the original receipt is preserved unchanged for audit.
Testing Cases Treeview rows now use `case_id`-bound keys with deterministic
duplicate suffixes and explicit legacy fingerprints; refresh retains the
selected case where it remains visible and never changes the stored ID.
Talent Relations case rows now use a defensive read projection as well;
opening or refreshing the case register no longer repairs malformed legacy
obligations, while explicit case/open, acknowledge and action writes keep the
existing normalisation boundary.

Simulation Lab play-level audits now retain one atomic yearly checkpoint per
seed/target recipe. Checkpoints include source/configuration/native-profile
identity, RNG state, isolated observer-world state, measurements and output
cursor; Resume Play Audit refuses mismatched or malformed evidence without
touching the active career or user save. Yearly snapshots now include
read-only hard-invariant findings for duplicate identities/settlements,
orphan scheduled references, orphan child-loan references and impossible
negative remaining obligations; a finding stops the disposable audit safely
and keeps the checkpoint for review.

Staff autonomy now validates every handler response before sealing a work row.
Each action must return an evidence key and payload; recommendation modes cannot
return spend, and malformed results become a visible `Needs attention` exception.
The Staff capability grid shows handler registration and evidence-gate state per
department without normalising or mutating the save.

The Staff page now exposes a pure long-run evidence/qualification audit. It
reports executed, recommendation and progression-qualified work and identifies
missing or duplicate identities, unsupported actions, paid recommendations,
malformed payloads, incomplete briefs and orphaned progression credits. The
diagnostic never repairs the save, reruns a handler or grants progression.
The new View Evidence Audit reader expands each finding with its saved work,
brief, action and calendar-boundary identifiers while keeping the audit read-only.

The Staff retention-watch table now uses stable `staff_id` rows and restores a
still-visible selection after roster reordering. ID-less legacy staff remain
explicit fallback rows; no retention concern or contract state is recalculated
or changed by the refresh.

Staff roster, market and expiry actions now resolve through stable `staff_id`
row identities rather than visible list positions. Refreshes and sorting keep
the selected employee attached to the same profile, hire, negotiation or release
action; duplicate identities receive deterministic suffixes and ID-less legacy
rows remain explicitly non-durable fallbacks.
Refresh now restores still-visible selections by the same stable row ID after
reordering, with a regression covering current and candidate tables.

The Staff page now includes a read-only AI Staff Ledger. It presents non-feeder
promotion employees alongside shared market supply, contract term/expiry,
salary, roster payroll, morale and identity quality. Ledger rows use promotion
and staff IDs when available, deterministic suffixes for duplicate identities,
and an explicit legacy fallback; opening the reader never ticks contracts,
refreshes offers, runs affordability or changes the save.

Long-run audit checkpoints now include read-only Staff employment integrity
findings for missing/duplicate IDs, market-versus-employed collisions, missing
roles, and invalid or negative salary/contract terms. The audit reports the
evidence without normalising or changing the isolated world.

Child-promotion manager selection and loan/recall/transfer routing now use
`promotion_id` when available. Duplicate display names no longer route to the
first matching child; unique legacy names remain readable and ambiguous actions
fail safely.

Membership Timeline readers no longer normalise a malformed history collection
just because the page is opened. They return an empty/incomplete read model and
leave the saved envelope unchanged; a regression covers both event pages and
as-of intervals.

The feature-development plan now distinguishes six delivery gates from its
63 non-deferred named cards. It identifies 13 card areas with unfinished or
policy-gated slices and keeps 14 deferred/excluded D-cards outside the first
playable completion count.

Owned Calendar Treeview rows now use deterministic source-bound IDs instead of
visible list positions. The selected event survives owner/source filtering and
refresh/reordering, while duplicate IDs receive deterministic suffixes and
legacy rows remain explicitly non-durable.

The Finance runway now shows contracted sponsor maximums and activation-adjusted
sponsor estimates separately for due cards, including the reason an agreement is
currently at half-fee readiness. Forecast refreshes remain read-only; conditional
receipts are not inserted into cash closes.

Finance now offers explicitly named, non-binding cash-runway scenario snapshots.
Each snapshot stores bounded attendance assumptions, proposed edits, its source
revision and complete forecast; changed inputs mark it stale while preserving the
original evidence. The UI exposes Save Scenario Snapshot and Review Saved
Scenarios without reserving cash, rerunning the forecast or applying edits.
Low/base/high attendance sensitivities are now explicit, bounded 0–200% controls
and are reflected consistently in the table headings, forecast and saved record.

Super-event closeout now preserves an immutable commitment breakdown and
terminal settlement key. Approval deposits, setup/security still owed, and the
explicit no-refund/manual-review status remain visible after cancellation,
expiry, or completion, and repeated closeout calls return the original record.

Tournament History now has a permanent, searchable Results view. Completed
one-night editions retain identity-safe entrant IDs, authored seed order,
division/gender, substitutions, stage matches, champion status and original
result references in the compact result index, so the draw remains readable
after replay commentary ages out. The reader is observational, supports
promotion/search filters, and can open the source card without rerunning or
settling anything. Legacy cards without tournament evidence remain absent
from the index rather than receiving fabricated draws.

Matchmaking now offers a read-only Tournament Field Review for a selected
draft tournament. It shows the saved seed order, entrant readiness on the
selected date, and ready same-division alternates with their company/world
rank snapshots. The review does not reroll seeding, sign a replacement or
change the card; the existing weigh-in owner remains the only alternate
commit path.

Unreleased Fight History layout pass: the profile ledger now keeps a
versioned, save-backed column layout alongside the existing visibility list.
Choose Columns lets the player reorder visible fields, set a selected field's
width within a safe 70–360px range, show/hide any retained field and restore a
career preset. Hidden fields remain in the full row payload, and their widths
are retained for later restoration; no historical record is removed or
rewritten.
Native Treeview header resizing is captured on release into the same layout
envelope, so direct drag adjustments persist alongside chooser changes.

Drug Testing now has a clearer review workflow. The durable case ledger adds
recorded sample/spend totals and a five-stage visual timeline (collection,
preliminary screen, confirmation, appeal and final outcome), while preserving
the full frozen evidence payload. Staff now links directly to Testing Cases so
the Drug Testing Officer's read-only review and the player ledger use the same
case evidence. Confirmation, appeals, sanctions and provider-specific live
outcomes remain gated until their approved policy tables exist.

Manual compatibility testing now performs a funds preflight before selecting
fighters. An unfunded or empty-roster run records a visible deferral and makes
no charge, sample, case ID or test roll, preventing a partial compliance ledger
or negative cash balance.

Staff Full Auto briefs now retain completed action IDs and resume only the
unattempted actions after a partial failure. A successful first action is not
rerun, recharged or re-credited when a later action is requeued; the brief and
exception remain visibly partial until the remaining work succeeds. The Staff
brief table also shows completed actions versus selected actions while keeping
the original work-count column and full saved payload intact. The brief editor
now exposes the ordered action allow-list as a multi-select control, so players
can configure more than one supported action per brief.

Media Rights now distinguish cleanly fulfilled contracts from terms that expire
with event entitlements still owed. A delivery-failure termination is a separate
terminal state and automatically blocks its queued successor for review instead
of activating it after a breach. Settlement outcomes and receipts retain stable
outlet and contract IDs so same-named partners cannot be mixed in account review.
Renewal offers now open at the two-month/two-entitlement boundary without a
refresh-time draw or spend, and rejected renewal evidence remains in offer
history.

Company History now explicitly warns when the 10,000-fact review threshold is
exceeded while retaining every append-only membership fact and keeping the
paginated/as-of reader complete.

Legacy/manual Media settlement outcomes are now enriched from the active
contract identity at the recording boundary, so receipts remain auditable even
when a caller did not precompute the newer identity fields.

Staff progression now stores each work item's explicit eligibility flag and
quarter-close growth ignores paid work that was marked non-progression. Older
rows without evidence remain unknown rather than receiving an inferred skill
credit.

Media Rights keeps a read-only terminal-contract history summary on the desk,
so fulfilled, shortfall-expired and terminated deals remain visible after the
active package ends.

Fight Night's Round Read and Bout Desk canvas readers now accept mouse-wheel
input from their hovered content, including Linux wheel buttons, without
stealing scrolling from the action timeline or native text/table controls.
The scoped bindings preserve the existing reader contents and playback state.

Semantic table accents are now theme-aware. Fight History, Scouting and Sponsor
rows use readable positive/negative/warning/info colours on both dark and light
surfaces, and live theme switches retheme their known Treeview tags without
altering row data or selection.
Lazy-built screens receive the same semantic pass on first entry, so opening a
page after a theme change cannot reintroduce the old fixed accents.
Known Assistant and Event Log Text tags are included in the same retheme pass.
Additional management states (unread, owned/available, readiness, scouting
strength, ranking movement and injury) now use the same semantic registry. A
row with an authored dark background keeps or receives a foreground that still
passes contrast, so Light Office cannot turn a warning or recommendation into
low-contrast text.
Secondary Staff, booking, contract and read-only history states (ready,
preserved, partial, review, open, booked and closed) use the same registry too.

Sponsor offer cards now separate the contracted maximum per eligible event from
the current activation-adjusted estimate and remaining term. The card explains
the exact readiness reason and the full/half-fee basis; the preview is pure and
does not refresh offers, spend cash or draw RNG.

Sponsor portfolio rows now use saved agreement IDs (or deterministic legacy
term fingerprints) rather than positional `active-0` keys. Refreshing the
market keeps the same active partner or offer selected, duplicate legacy rows
remain distinct, and offer-only actions are visibly disabled for an active
agreement while its duty status and current read-only activation preview are
shown in the detail card.

Media Desk refreshes now report weekly action capacity through the read-only
counter. Opening or repainting the desk no longer advances or resets a stale
week marker; only an explicit campaign action owns that boundary.

Fighter Search and Database Editor debounced callbacks now cancel when
navigation leaves their page and fail safely if the root is closing. Hidden
pages are not redrawn by a stale 200 ms/150 ms callback, and the existing
pagination, filters and editor state remain unchanged.

The combined release-safety UI/media/profile/staff/persistence/scouting group
now passes 215 tests after these slices; no EXE or user save was rebuilt.

World Hub gym rows now follow a saved gym ID where available, with a
deterministic name/region fallback for legacy gyms and duplicate-row suffixes.
Refreshing after a sort or membership update keeps the same gym selected and
does not resynchronize or mutate gym membership counts.

Company standings selection now retains both company name and sport scope, so
an MMA row cannot be replaced by a same-named combat-sport circuit after a
filter/sort refresh.

Stacked Matchmaking now reserves a compact draft-card pane so the fighter
explorer keeps at least four readable rows at the 1280×760 viewport. The draft
card remains independently scrollable; controls and selection state are
unchanged.

Combat Sports contract rows now use stable sport/fighter identities instead of
visible list indexes. Refreshing or changing the shared expiry filter restores
the selected athlete when still present; duplicate source rows remain visible
with deterministic suffixes rather than crashing or selecting the wrong deal.

Matchmaking's delayed stacked-layout callbacks are now cancellable and
destroy-safe. Closing or rebuilding the page no longer leaves `after` scripts
pointing at dead Tcl commands, while the existing responsive placement remains
unchanged.

Commercial settlement receipts now use their saved receipt/event identity as
the read-only table key. Refreshing after a new settlement retains the selected
historical receipt instead of following a shifted list index; legacy rows with
no durable identity remain visible and are labelled as non-reassociable while
their full stored payload is retained.

Results rows now use the archive's saved `record_id`/`key` for their Treeview
identity as well. Filtering or inserting a newer result keeps the selected
event attached to its stored card; duplicate source identities get deterministic
UI suffixes and identity-less legacy shelf rows retain an explicit positional
fallback.

Staff autonomy scope now stays truthful in the controls: selecting a department
immediately shows that department's saved override, while Company default shows
the global mode. Refreshing the panel preserves the effective scope, so applying
a policy cannot silently target a different level.

Staff work-receipt rows now use their saved operation/work identity instead of
the current list position. New advice or completed work cannot make a selected
receipt open a different payload after refresh; duplicate identities get stable
UI suffixes and identity-less fallback cards remain visibly non-durable.

The Promoter Dashboard decision queue now keys notices by a content-bound
read-model identity and restores the selected notice when refreshes reorder the
queue. Changed source advice intentionally becomes a new notice; existing text,
priority and action routes are unchanged.

Unreleased J5 testing-desk contract slice: Testing Providers now compares three
authored provider tiers, coverage, turnaround and deterministic planning quotes;
more effective tiers cost more for the same sample count, without implying a
guaranteed finding. The selected provider and sample count are persisted as a
planning configuration, while the compatibility screening resolver remains
unchanged until confirmation, appeals and sanction rules are approved. Drug
Testing Officer recommendations can review frozen cases and the planning quote
without commissioning samples, charging cash, drawing RNG or applying a sanction.
`drug_testing_catalogue_regression_test.py` covers catalogue order, defensive
reads, monotonic quotes, configuration persistence and fail-closed selection.
Each new case also freezes the provider/policy snapshots, sample count and
actual paid total; disabled-policy quotes correctly show zero samples and zero
cost.
The case ledger now shows a pure workflow timeline and next-action projection,
separating preliminary alerts from confirmation, appeal and final-outcome steps
that remain gated.
The cases page now supports identity-safe search/status filters and shows the
provider, policy, screening label and next available action before opening the
full payload; the provider window highlights the active row and explains its
supported panels and confirmation posture.
Broadcast Producer recommendations now include resolved production quality,
contracted minimum, readiness, venue/region, bout count and settlement status
so media-delivery risk is visible before any production action. Reviews remain
read-only and do not spend, edit cards or settle rights.
Last-minute title replacements now use the existing challenger-merit owner
when building the dropdown and again at commit. Unqualified ready fighters are
not presented as title-eligible; if the owner is unavailable the picker fails
closed. Champion-corner replacement remains an explicit non-challenger path.
The weigh-in replacement dialog now labels each qualifying row and its summary
as a title-eligible challenger option when merit is required, while champion
replacements are described separately. This makes the existing ranking and
eligibility evidence visible at the decision point without changing the
replacement mechanics.
Legacy Fight Night archives now keep the Preparation reader reachable even
when the additive `preparation_timeline` field is absent. A selectable
“Unavailable” row and detail message disclose the coverage gap; no stage,
outcome or readiness result is fabricated and the stored archive remains
unchanged.
World Hub refreshes are now read-only with respect to gym membership. Counts
are synchronized at calendar boundaries and explicit gym mutation/view paths,
not during routine navigation or header repaint, preventing a page refresh
from rewriting gym load/capacity state.
Staff autonomy processing now runs after the completed calendar boundary's
cards, settlement and month-end business work. The worker receives the saved
completed month/week, so Full Auto or Recommendations cannot run against a
half-settled week or be stamped into the next calendar period.
Spectator fast-forward now leaves committed Staff briefs untouched: it does not
grant recommendation or Full Auto authority while the player is absent.
Automated Marketing commits now enforce the saved brief-level spend ceiling in
the same pure quote preflight as the company/action guardrails; an over-ceiling
quote becomes Needs attention before the Media resolver is called.
Staff autonomy now distinguishes broad Recommendations from Selective
Recommendations. Broad mode reviews every authored read-only action for the
department, while Selective mode reviews only actions explicitly allow-listed
on the brief. Mutating actions such as Marketing campaign commit are excluded
from both recommendation modes and remain Full Auto-only, with an explicit
Needs attention result when a selective brief requests one.
The Staff capability grid now shows the safe read-action set beside each
department, and the autonomy intro explains the four scopes in the same terms
as the worker. Its viewport now fits all registered departments so the lower
Doctor and Drug Testing Officer rows are not silently clipped.
The replacement-corner selector now uses stable corner labels (fighter name plus
corner number) and resolves the selected fighter by saved ID before explaining
title merit. Duplicate display names can no longer make the dialog describe the
wrong corner; legacy name-only rows retain their existing safe fallback.
The owned-company calendar now keeps stable event rows for MMA, owned children
and player combat-sport divisions, including participants, reservation count and
legacy identity status. Selecting or double-clicking a row opens a read-only
detail reader; it never resolves a fighter, reruns a card or changes ownership
or finance. Company, source sport and status filters preserve the selected event
by stable ID and report shown versus indexed rows; Reset only clears the view
filters.

Unreleased owner-objective boundary: owner goals now have stable IDs, explicit
semantics and additive evidence fields. New objectives are evaluated by the
completed-calendar worker after cards, finance and monthly business tasks have
settled; the worker records one observation per boundary and seals terminal
outcomes with an Inbox/news explanation. Repainting the goals page is now a
pure preview and cannot create baselines, maintenance windows or late
successes. Legacy rows remain readable with a deterministic ID and an explicit
“historical evidence unavailable” note. Shows use the canonical company event
count when available. The owner-goal regression suite covers preview purity,
deadline failure, exact-boundary completion and idempotent sealing.

Unreleased rankings readability pass: the Rankings landing page now labels the
number of matching rows separately from the capped number shown (50 for
pound-for-pound and 75 for division lists), alongside the existing leader,
champion, scope and board cards. No ranking order, eligibility or hidden rating
visibility changed.

Unreleased title-miss replacement guard: if a chosen short-notice replacement
is no longer valid, cannot be resolved, or misses the scale itself, the bout now
stops in an explicit Needs Review state. The recorded corners, replacement
identity, miss amount and existing one-time purse fine remain available; no
automatic catchweight conversion, second prompt or duplicate fine is possible
after reload. The title-decision regression covers invalid and second-miss
replacement paths.

Unreleased child-promotion loan boundary: the MMA Child Promotions manager
now asks for an agreed return month/week and shows it beside each protected
loan. The new optional fighter fields survive save/load; at the weekly world
boundary a completed term returns the fighter through the existing atomic
recall/membership/belt pathway. A still-committed child bout delays the move to
the first safe boundary and creates an Inbox/news notice. Legacy open-ended
loans remain recall-only. This adds no transfer fee, title waiver, treaty effect
or RNG draw; `child_promotion_loan_return_regression_test.py` covers persistence,
safe return, delay evidence, idempotency and RNG purity.

Unreleased company-relations proposals: fighter swaps and parent/child loans,
recalls and paid transfers now create durable, identity-linked proposal records
when the player explicitly starts the action. The read-only Company Proposal
Ledger retains both parties, fighter IDs and ranking snapshots, cash, expiry or
recall boundary, warnings and each status/outcome transition. Existing board,
contract, roster, finance, belt and membership owners still decide the result;
stale ownership, booking, title or cash state becomes Needs review without a
same-name substitution. Proposal creation and inspection are RNG-free and
terminal updates are idempotent. No new acceptance modifier, fee or goodwill
rule was added; the optional bounded return date is described in the loan-boundary
entry above and remains separate from general treaty/ownership mechanics.

Unreleased history coverage diagnostics: Company History now reports retained
membership facts against the portable-save bound, duplicate-ID/fighter/company
counts and known versus migration-unknown dates without trimming or repairing
the archive. An explicit measured retention policy is still required before
any future compaction or sidecar format.

The membership writer now preserves every append-only fact instead of silently
keeping only the last 10,000 rows. The 10,000 value remains a visible review
threshold, and long-history interval reads page through the complete retained
collection before applying the requested view.

Unreleased title-lineage identity guard: legacy belt maps that retain display
names no longer let a duplicate-name non-holder vacate the actual champion's
belt. The live champion/interim flag (or an unambiguous sole name) is required,
and the real departure still records the holder's stable fighter ID in history.

Unreleased regional planning review: Regions now offers a read-only Feasibility
Review that summarizes current bookable pairings, champion/top-10 depth, visible
versus unscouted regional free agents, unlocked venues and scheduled shows. The
assessment is scouting-aware and RNG-free; it never invents prices, reveals hidden
AI plans, reserves talent or creates invitations. Thin divisions remain explicit
merit blockers. `regional_feasibility_regression_test.py` covers the public counts,
scheduled-event filtering, scouting visibility and no-state-drift contract.

Unreleased spectator controls follow-up: Simulation now has a persisted,
week-boundary-safe stop policy. Players can stage an exact calendar target and
optionally pause after the next hosted event; watched event cursors and the
last completed boundary remain save data rather than Tk callbacks. A pause
records its reason, disables the policy, and exposes an explicit Resume path.
Policy evaluation is read-only and RNG-free, repeated stop requests are safe,
and spectator mode keeps its no-routine-prompt behaviour.

The same observer pass adds explicit pinned checkpoints. Pin Active State writes
an atomic compressed snapshot in a separate bounded folder with source save,
game date, byte size and verification metadata; the Checkpoints manager can
inspect, restore to a chosen slot, or delete one after confirmation. Pinned
files never replace the primary save or rolling recovery slots, and no
automatic checkpoint copies are created.

Unreleased Combat Sports presentation: the main Combat Sports page now leads
with selected-sport management cards for AI/owned roster depth, the next
committed child-promotion card, contracts within two months of expiry and the
latest recorded result. The selected sport survives refreshes; the existing
native rankings, card builder, records and full history remain unchanged.
Cards are read-only and do not schedule, recalculate or mutate sport data.

Unreleased World Hub presentation: selected promotion and gym rows now populate
compact context cards for identity, promotion snapshot, gym tier/effective
quality and capacity/morale. Refreshes retain row identity where available; the
complete tables, wrapped news and management actions remain unchanged.

Unreleased Scouting search usability: Target Board typing now uses a 200 ms
debounce, cancels obsolete callbacks and disposes pending work on navigation or
application close. Explicit filter changes remain immediate; selection and
pagination continue to resolve by fighter identity.

Unreleased Camp Plan usability: the plan window is now resizable and presents
draft status, workload trade-offs and live medical limits before the existing
explicit commit. Cancel leaves the fighter's current plan unchanged.

Unreleased title-miss evidence: player weigh-in decisions now retain a plain
snapshot on the affected bout with both corners, miss amounts, title roles,
eligibility context, available choices and the selected sanction. Replacement
handling captures the original corner identity before changing the booking. The
same evidence is carried into fight-log rows and the event preparation timeline
for post-settlement review. The decision window now previews each action's
consequence before commitment without running a resolver. Saved pending decisions
now reuse their recorded miss evidence without rerolling the scale, and terminal
choices are idempotent after reload; the broader resumable preparation/title-
outcome matrix remains gated for G5.
Results Database preparation details now repeat the recorded action, status,
title-on-line state and replacement reason for post-settlement review.

Pending snapshots also retain the calculated one-time purse fine, so an
interrupted preparation carries the original charge forward exactly once rather
than losing it or charging it twice.

Unreleased compliance case foundation: manual drug testing now records each
sample's stable test/case identity, fighter snapshot, policy and preliminary
screening result in a save-owned ledger. A preliminary alert is explicitly not
a proven violation and never mutates medical injury or disciplinary state. The
existing officer accuracy lift and cost discount remain unchanged; the `None`
policy now collects no samples, spends no cash and consumes no test RNG. A
read-only Drug Testing Cases window exposes the frozen evidence. Confirmation,
appeals, provider selection and sanctions remain gated until their approved
policy/balance table is defined.

Unreleased Staff specialty effects: three approved non-Scout specialties now
have bounded, lead-scoped mechanics. Campaign Coordinator reduces eligible paid
media campaign cost by 2%; Contract Administrator adds a 2% saving to the
existing negotiation-administration basis without changing fighter terms; and
Production Coordinator reduces production staging by 2% while leaving security,
guarantees, athlete pay, medical and testing untouched. The same pure saving is
used by previews and settlement, and the Staff readout labels the effect.

Unreleased J12 career conversion: Game & Saves now offers **Career → DB**, an
explicit new-start export that preserves stable fighter/promotion identities,
current supported skills/age, records, roster ownership and contracts while
resetting live time, cash, finance, inbox, pause targets and active work. A pure
preflight reports structural issues and every exclusion; completed history and
ongoing evidence are written to a sibling `<name>.conversion.json` provenance
manifest rather than silently becoming playable history. The destination never
overwrites an existing database, cancellation is safe, and staged write failure
cleans up both artifacts. Added `j12_conversion_regression_test.py` and registered
it with the isolated regression runner.

The Scouting Target Board now leads with four compact selected-target cards:
identity/division, intel freshness, scout advice, and market context. They are
read-only, scouting-aware and cleared with selection changes, while all report
ranges, filters, pagination and actions remain intact below.

Unreleased event-preparation timeline: End of Event summaries now present
campaign evidence, press-conference lines, weigh-in outcomes and final readiness
as compact status cards. The same recorded timeline is available from the
read-only Fight Night archive and Results card window. Cards are additive and
read-only: opening them never reruns preparation, consumes RNG or changes fight
mechanics; legacy packages without the field remain readable.
Completed stages also carry stable event-scoped keys so reloads can distinguish
recorded preparation from missing legacy evidence.

Unreleased Booking Workbench follow-up: added identity-linked tentative medical
planning for same-division alternatives. The workbench records earliest stored
return and camp facts, keeps the draft explicitly non-binding, and provides a
Medical Drafts review window. It never reserves a fighter, assigns camp, spends
cash, runs a weigh-in or creates a recovery promise; current readiness is checked
again at event week, with passed target dates shown as Needs replacement.
Added a read-only Draft Review for preserved pairings and unresolved TBA slots;
hosts that cannot run migration keep the fallback label Legacy reference when no
durable booking ID exists.

Unreleased Booking Workbench identity/editor follow-up: Foundation migration now
assigns deterministic, event/draft-scoped `booking_id` values to legacy draft and
event fights, retaining existing IDs and repairing duplicates without RNG or global
counter drift. Complete stable draft rows can open a locked-slot alternatives view.
The player chooses one eligible replacement for one corner; stale fingerprints,
medical/booking blockers and unavailable identities fail closed. Commit preserves
the title/tier/plans/unaffected corner, records replacement history and a single
idempotent receipt, and never edits scheduled event fights or silently changes a
pairing.

Unreleased Fight History integrity repair: when a replay and pre-bout snapshot do
not contain an opponent's at-the-time record, Profile History now displays
`Not recorded` rather than leaking today's record into the historical row. Valid
archived records remain authoritative, and no historical identity/rank data is
rewritten.

Unreleased membership-history foundation: contract signing/exit, free-agent,
automatic-TBA and last-minute market entries, child-promotion loan/recall/transfer, retirement, AI
expiry/cuts, regional exits, academy matching rights, editor removals, negotiated
swaps and closed-division load/live reconciliation now append deterministic, ID-linked
membership facts with action, effective date/precision, reason and source transaction. Replayed
callbacks deduplicate without simulation RNG or Foundation-counter drift, and the
read model supports bounded filtered pages with defensive copies. Fighter Profile
History now renders a compact Company Timeline from the derived intervals, labelling
unknown coverage. A full-history window supports as-of Month/Week filtering, title
context, clipboard copy and explicit JSON export without rerunning simulation.
Current imported rosters are intentionally not assigned invented join dates; the
searchable as-of view retains that distinction.

Unreleased Staff autonomy follow-up: Marketing briefs now expose two deliberate
actions. Review remains a zero-spend evidence card; Commit is available only when
the player explicitly selects Full Auto for Marketing and commits that action. It
runs the existing Media resolver after a pure preflight against the current
spokesperson, target, action capacity, cash reserve and staff ceilings, then
records actual spend/evidence once.
Briefs created without an action default to Review, and missing/stale/unaffordable
work becomes Needs attention instead of silently substituting or retrying.
Matchmaking recommendations now reuse the Booking Workbench read model to include
identity-linked ranked alternatives, fit/build scores, hard blockers and cautions;
they remain advice-only and do not generate, reserve or commit a bout.
Talent Relations contract reviews likewise include existing relationship-case IDs,
statuses and recorded reasons without opening, resolving or changing a case.
The Staff work-receipt table now opens a managed read-only evidence brief for
the selected row. It preserves the complete stored payload while presenting
Matchmaking alternatives (company/world rank, fit/build, blockers and cautions)
and Talent Relations cases in separated, readable sections; opening it never
normalises state, reruns a handler or consumes RNG/capacity.

Media settlement receipts now have the same structured inspection path. View
selected (or double-click) separates rights delivery, production quality versus
the contracted requirement, audience outcome, sponsor duties and duty-period
evidence, while retaining the complete receipt payload. Receipt inspection is
read-only and does not rerun settlement or regenerate offers.

The legacy Fight Night sidebar is now a styled, scrollable Event Log. It keeps
the complete stored event/office history, adds a line-count/read-only summary,
and applies theme-aware accents to headers, results, key moments, actions and
system notices. Refreshes and theme changes preserve the reader position and do
not alter the underlying log.

Unreleased Rules Help: added a searchable Tools page backed by eight stable,
authored topics for ranking scope, title merit, availability, staff autonomy,
campaign costs, contracts, finance and event preparation. Topics explain honest
blocked/unknown states and link back to existing screens; search and navigation
do not evaluate hidden ratings, mutable formulas or gameplay RNG. Fighter
Comparison now shows its as-of date and visible-cohort coverage, with known-
sample labels under Scouting Mode and no inferred percentile for hidden data.

Unreleased Fight Night presentation: replaced the tall stacked broadcast header
with larger-portrait fighter cards and a compact central clock. Added a dedicated
styled timeline, visual round-read bars, scrollable analysis/bout panels and a
playback-settings drawer. Portraits respond to window resizing. Replay retains
complete bout names, adds transcript/list scrollbars, prevents transcript editing
and fixes competing bout/round listbox selections. Preserved density frontiers,
scorecard seals, playback/audio timing, skip confirmations and event commit guards.

Unreleased Finance presentation: added a read-only decision strip to the Finance
landing page for cash on hand, recurring monthly commitments, expected media plus
active-sponsor income per event, and booked purse/medical exposure. These cards
reuse the existing ledger/runway calculations, label estimates and exposure
clearly, and leave the full transaction/history tables available; no new charge,
forecast probability or action is introduced.

Unreleased Contracts presentation: added a selected-fighter read-only brief with
division/rank, champion state, contract type, purse, term/expiry, morale and
recorded obligations. It preserves the full filterable ledger and existing
renewal/batch actions; selecting or refreshing the brief never negotiates or
charges a contract.

Unreleased Regions presentation: paired the full region profile with context
cards for economy, regulation, testing accuracy and local MMA interest, plus
tracked-area, promotion, gym and booked-show counts. Cards read existing region
and schedule data only; no regional rating, legal or testing mechanics changed.

Unreleased Industry Standings presentation: selected-company profiles now lead
with quick-read cards for industry rank, composite power, roster depth and
stability, while retaining the full roster, calendar, strategy and power-trend
details below. The cards use the selected sport/name standings row and do not
recalculate company or AI values.

Unreleased Results presentation: the selected event detail pane now leads with
read-only cards for date, main-event headline, stored bout count and recorded
gate/profit outcome. Full result text and replay actions remain available, and
empty/filtered states clear the cards instead of showing stale event data.

Unreleased trait overhaul: centralized all 46 trait definitions and explanations;
added category-coloured Overview/Identity cards with expandable effects, scouting
privacy and camp progress/history. Replaced random camp trait swaps with connected,
multi-month progression and a 12-month cooldown. Added bounded Weight Bully control,
Slow Starter opening and live strike-defence effects; Fight Finisher now checks the
opponent's hurt instead of the attacker's. Injury risk reads the current trait,
preserving legacy stored values through a migration anchor. New generation no longer
bakes injury/durability trait bonuses into permanent attributes. Corrected non-MMA
behaviour comparisons to Pressure, Counter and Cautious. No EXE or user save rebuilt.

Unreleased title-lineage integrity: distressed buyouts, AI expiry/retirement exits,
and World Editor roster departures now pass through the shared belt-vacancy recorder
before the fighter leaves. This preserves the outgoing holder, reason and date rather
than silently clearing the belt.

Unreleased commercial management: rebuilt Sponsor Market and Media Rights into
active decision screens with offer valuations, risk/status callouts, selectable
pitch briefs and one-shot negotiations. Sponsor activation duties now affect event
income; missed trust, stability or ranked-fighter media requirements pay half. Media
counters can improve guarantee, reach, delivery standards or commitment length, but
a failed counter withdraws the offer and harms the outlet relationship.

Unreleased AI booking logic: prioritize overdue fighters/divisions and inactive
champion defenses; use ID-linked scorecards for close rematches; discourage repeated
sweeps; prevent rivalry bypasses and rebooking already-selected prospects.

Unreleased AI matchmaking performance: indexed division pools and cached per-card
sorting, pair-loop overall ratings and rebuild checks, retaining existing booking
priorities, tie order, card limits and random draws.

Unreleased measured performance fixes: signature assignment calculates overall once
per fighter, authored signature migration builds one lookup per load, and compressed
saves use accelerated JSON encoding with bounded byte buffers.

Unreleased performance: debounced and paginated Fighter Search, removed routine
hidden Academy/Combat Sports redraws and header-triggered gym scans, streamed gzip
save output, preserved shared references during load staging, and added bounded
calendar task timings for follow-up profiling.

Unreleased portrait v3: added 50 hairstyles per gender (98 men's / 77 women's),
50 men's facial-hair presets (66 including none), and 50 presets per shared feature
and colour category. New hair has separate silhouettes and directional texture;
anatomical additions are bounded intermediate proportions. Women remain facial-
hair-free. Existing saved vectors, old IDs and country colour-family probabilities
are preserved. Added v3 contact sheets and expansion/compatibility regressions.
See [the portrait expansion review](docs/FIGHTER_PORTRAIT_EXPANSION.md).

Unreleased named portrait corrections: Markell Holmes now has dark skin, an afro
and a chinstrap; Brett Akey is bald. Conor McGregor has an authored short brown
fade, warm-brown full beard and facial controls based on UFC's July 2021 photo.
Optional `beard_colour` supports separate beard/scalp colours without changing
other portraits. Existing saves receive the named display corrections too.

Unreleased ranked portrait review: authored 49 entries at rating positions 51–100
from 45 reviewed real-fighter references, including four Legend copies. Matthew
Green subsequently received user-directed pale skin, short dirty-blond hair, blue
eyes and no facial hair; other features are retained. Added three authored-only hairstyles and burgundy dye;
generated pools and saved vectors are unchanged. Cristiane Justino receives female
portrait presentation without changing her mislabelled database gender or division.
See [reference ledger and limitations](docs/FIGHTER_PORTRAIT_TOP_100_REVIEW.md).

Unreleased ranked portrait continuation: authored cosmetic directions for every
fighter at rating positions 101–200, including independent archive-record copies.
Added labelled 180px review and 72/98/104px contact sheets. No portrait catalogue,
generated weights, fighter data or simulation values changed. See
[the 101–200 review](docs/FIGHTER_PORTRAIT_TOP_200_REVIEW.md).

Unreleased ranked portrait continuation: added complete photo-directed comic portrait
vectors for every fighter at rating positions 201–300, plus labelled 72/98/104/180px
review artifacts. Source photos are used only for research and are not shipped. Saved
identities, catalogue IDs, fighter data and simulation values remain unchanged. See
[the 201–300 review](docs/FIGHTER_PORTRAIT_TOP_300_REVIEW.md).

## 3.0.10 - 2026-09-06

- Fix Regional Prospects crashing while rendering fighter ratings because the
  row's promotion name was not passed into scouting visibility checks.

- Require credible AI title challengers, including vacant belts; preserve a three-win
  comeback route and enforce the repeat-series cooldown before close-bout exceptions.
- Upgrade profile history with pre-bout ranking badges, compact selectable columns
  and expandable full details; add Development progression and signed driver charts.
- Turn Overview into a visual corner dashboard with career-pulse meters, clearer
  recent-form framing and an ordered eight-attribute strength/development chart.

- Integrated the accepted 440-move fight system into normal play through a native,
  immutable runtime profile. Includes specialist positions, standing combinations,
  real hold-transition chains, contested cradle setups, role-aware survival moves,
  retained head-impact and kick-power adjustments, and expanded commentary.
- Preserved new signature/mastery IDs through saves, universe validation, the database
  editor, fighter development and Fight Night. Historical audit fixtures retain their
  separate legacy profile; no runtime audit patches or extra simulation passes are used.
- Added complete native/candidate bout and RNG parity, save/development integration
  tests and source-bound runtime coverage. User-approved rare-ID acceptance is measured
  over 25,000 bouts; all other coverage and full-calibration gates remain unchanged.
- Updated legacy identity fixtures to distinguish cage retention from standing-back
  progress, and verified retired-move development against both explicit engine profiles.
  Release certification records these reviewed test-only migrations without replacing
  simulation reports or exempting runtime/audit-helper changes.

- Fixed two survival templates that omitted the acting fighter's name and made
  jab-caused KO/TKO finish descriptions reuse the causal exchange's display label.
  Added all-template actor/move checks and regressions for both observed TKO failures.

- Repaired historical selection verification after default portrait fields and approved
  commentary variants changed its fixture/trace fingerprints. Explicit reference-bound
  reconstruction restores the original test inputs and rendering; immutable snapshots,
  strict comparisons and current-capture behavior are preserved.

- Added a reproducible current-candidate unused-move diagnostic with actual selection
  pools and resolved hold/style evidence, complete paired-bout/RNG checks, exclusive
  output and source fingerprints. Kept the standard coverage gate intact.

- Expanded defense commentary from three to 16 deterministic clauses per identity.
  Added wording and complete-bout/RNG parity coverage to the shipping suite.
  Paired whole-bout CPU samples show no measured slowdown; the benchmark removes the
  added formatting expressions from its old arm rather than formatting and discarding them.

- Fixed candidate coverage commands incorrectly exiting successfully when failures
  existed only inside their arm reports. Console and saved JSON now expose every
  arm's blocking findings at the top level; accepted advisories remain nonblocking.

- Varied single-jab commentary with eight equivalent labels while preserving the move ID,
  mechanics, target and outcome. Added deterministic replay and complete-bout regressions
  proving that only rendered commentary changes and all terminal RNG streams match.
- Expanded named composed-survival commentary from 16 to 50 authored templates across
  standing and ground exchanges. The change is presentation-only and adds no move IDs,
  mechanics or simulation work.

- Authored 40 additional survival techniques for the opt-in 440-move engine, covering
  standing, clinch, ground and specialist recovery. Added ownership filtering, including
  corrections to three existing physical-top/bottom recovery names, trace validation,
  default/nested parity tests and a paired whole-bout CPU benchmark. Existing survival
  decisions and recovery amounts are reused; no executable or normal-play promotion yet.
- Survival-expansion verification reduces 300-bout top-ten concentration to 19.03%,
  retaining all 39 full-calibration group summaries and showing no measured CPU slowdown
  in paired 44-bout samples. Validated 1,359 new recovery selections with zero invalid roles;
  remaining small-sample unused-ID failures are retained.
- The 3,000-bout survival expansion observes all 40 additions, reduces concentration from
  25.97% to 19.30%, and validates 13,247 selections without ownership errors. All remaining
  four long-run absences belong to pre-existing specialist submissions.

- Added current-candidate move concentration diagnostics with exact bout/RNG parity,
  choice-time recovery facts and scored-versus-offered selection counts. Coverage now
  retains per-move counts. The investigation identifies a 25.9452% fixed-action floor
  from ten survival IDs in the 25,000-bout run; no balance or acceptance gates changed.

- Added the requested default-off 1% resolved-kick power experiment with fail-closed
  resolver instrumentation, RNG/nesting/default-parity regression coverage and no EXE
  or normal-play change pending calibration.

- Full 2% standing head-damage calibration recovered 12 finishes (13 competitive) and
  reached 47.99% competitive. The user accepted middle-timing drift as an advisory;
  candidate remains disabled pending competitive and coverage gates.
- Combined with the retained head-damage change, the 1% kick-power trial reaches 2,292
  finishes and 48.16% competitive with no full-calibration failures. Coverage still has
  25 unused IDs and 25.66% top-ten concentration; the trial remains audit-only.

- Added the requested audit-only2% standing head-strike impact experiment, with resolved
  target filtering, fractional damage, unchanged non-head/ground/clinch damage and no
  additional RNG draws. Added channel/draw/nesting/default-off regressions; normal play
  and the EXE remain unchanged pending full balance and coverage results.

- Added default-off, evidence-backed top/guard submission-chain identities and a contested
  Catch cradle setup with single-use neck-crank follow-through. Preserved skill/style,
  ownership, expiry, control credit and actual hold evidence, with no forced actions or
  added simulation passes/draws. Added lifecycle and provenance regressions, full-corpus
  provenance validation and paired whole-bout profiling. Not enabled in normal play.
  Separate/joint 3,840-bout calibration retains all39 group summaries; the combined
  300-bout sample recovers top-chain use but still fails unused-ID/concentration gates.
  Added fixed paired Catch-matchup diagnostics; natural cradle completion remains
  unobserved, despite passing controlled resolver tests. Measured CPU rises2.55% in
  the 800-move diagnostic; no zero-overhead or release-readiness claim.

- Added a default-off heel-hook identity repair for the experimental catalogue:
  match the actual attacking submission instead of a positional reversal. Preserved
  skill/position limits and inside/outside distinctions, with resolver/trace, scope and
  normal-play parity regressions. No forced selection, added RNG draws or EXE change.
  The 900-bout trial recovers three named heel hooks (13 to 12 unused IDs); the ordinary
  300-bout gate remains unresolved. Full calibration group summaries are unchanged.
  Also rejected incompatible nested audit contexts, with a fail-before/pass-after test.

- Made the overall 20–24 distinct-move mean advisory by user approval, retaining raw
  coverage measurements and explicit per-arm advisories. Boundary regressions preserve
  unused-ID, concentration and other hard checks; no gameplay or EXE changes.

- Applied the user's temporary chain-median waiver in both Phase 31 and joint candidate
  gates, retaining median observations/advisories and every other acceptance condition.
- Added source-bound, read-only repertoire opportunity cohorts with zero-action slots,
  fixed-pool matching, unused-ID pool partition and regression coverage. No gameplay,
  simulation-cost, variety-target or EXE changes.

- Added an isolated, default-off boxing combination-intent trial: suppress jab/power-punch
  demand when the current named pool contains no successor. No action, named move, expiry,
  initiative or bonus cap is forced or changed. Tests cover pool parity, body/head targets,
  authored/counter gates, expiry, nested isolation and normal-play parity.
- Refreshed exact-parity chain diagnostics on the mount-refined candidate and added
  800-move profiling that counts the trial's additional pre-selection scoring work.
- Measured the pool-aware trial: sampled completions rise to 1,548/4,656, but full
  calibration falls to 2,267 finishes / 598 KO / 727 TKO and competitive 47.33%.
  Retained the mount-refined base and the trial's default-off status; 97 affected tests pass.
- Measured selector plus added preview cost at 12.27% with 800 moves (control 10.74%).
  The budget passes, but profiled CPU time increases; no normal-play performance change.

- Added paired jab-timing diagnostics with complete corpus/group reconciliation, input,
  source and registry fingerprints, caller RNG checks, exclusive output and regressions.
  First named, mechanical and plan differences are recorded separately; a shifted timing
  bucket alone is not labelled a delayed finish. No production mechanics changed.
- Completed the full paired timing investigation and exact recheck of 75 changed outcomes:
  25 finishes gained and 15 lost. Fixed false plan-change labels from unequal tails or
  different actors, retained triggering projection fields and added negative regressions.
  No supported combat bug or reason to promote the jab-only trial was demonstrated.

- Added a default-off jab-only repertoire trial on the mount-refined candidate, with
  action-filter, live-chain, normal-play parity, nested restoration and CLI regressions.
  Broad and jab-only readaptation cannot be combined. No production promotion or EXE update.
- Completed the jab-only trial: 2,285/605/719 finish/KO/TKO across 3,840 bouts and all
  39 groups. Competitive finishes rise to 47.92%, but timing and repertoire gates still
  fail. Retained the mount-refined base and documented a separately reproduced historical
  fixture-hash failure without changing its reference.

- Made specific named-move sample absences advisory, as requested, while retaining unknown,
  structural, concentration and aggregate repertoire failures. Historical reports stay unchanged.
- Added mount-refined repertoire choice diagnostics with exact complete-bout/RNG parity,
  source/input/registry checks and tests for repeated choices, near-score final alternatives
  and narrowly scoped frequency policy. No fight mechanics or selector weights changed.

- Investigated scarf-hold coverage on the mount-refined candidate. The missing use and two
  interrupted setups all belonged to one changed bout; the other14 observed setup decisions
  are exactly unchanged. Documented the causal trace and29 passing focused tests. No mechanics
  or acceptance changes; fixed-sample missing content remains a release failure.
  Extended900-bout frequency evidence finds76 scarf setups and2 uses, preserving first300
  observations and current source hashes. No missing/invalid prepared observations; overall
  variety and chain gates still fail. The longer sample does not replace fixed-sample acceptance.

- Added an opt-in mount-specific gift-wrap refinement retaining both original back branches.
  It trades the mount hammerfist chain edge for mount arm-pinch control, without changing the
  ordinary move menu. Includes graph, drift, nested/error restoration and a previously disrupted
  back-control fixture regression. Default candidate and normal play remain unchanged.
  Fresh calibration improves competitive finishes to47.53% with2,275 finishes /605 KO /725 TKO
  across3,840 bouts/39 groups, removing the broad trial's timing failure. Gift-wrap completion
  rises17/58 to22/58 while retaining nine body-triangle selections. The competitive floor and
  coverage gates still fail, including absent scarf-hold armbar; retained as an experiment.
  Thirty affected tests pass and both report source fingerprints verify.

- Added an isolated gift-wrap control-branch trial after inspecting high-frequency roots.
  It tests existing hip-pressure riding across mount/back instead of back-only control,
  retaining the other two branches and every selector gate. Includes single-root, full-DAG,
  idempotence, source-drift and nested/error restoration checks. No release promotion.
  Fresh coverage improves the root from 17/58 to 26/60 completions, but full 3,840-fight
  calibration fails competitive finishes (47.19%) and middle timing; coverage loses scarf-hold
  armbar and reaches 25 unused IDs. Trial stays disabled. Thirty affected tests pass, including
  real mount follow-through and the unchanged control; source fingerprints verify.

- Added a focused audit-only continuation-commitment candidate after live selection diagnostics.
  Fresh/unhurt action menus mix at most half their demand toward positive preview branches;
  the existing skill/gas/target preview bounds the mixture. No extra draws, expiry changes,
  forced identities or survival changes. Includes conservation, restoration and default parity tests.
  Full calibration rejects this trial (competitive 47.05%, early/middle timing failures);
  it stays disabled. Increased chained share does not compensate for worse variety or median one.
- Fixed a false Von Flue provenance failure when a pending wrap expires at the horn in an
  exchange-only trace without an explicit round-end marker. Forward rounds clear only validator
  pending state; stale angle facts, malformed rounds and missing same-round evidence still fail.
  The new regression fails before the fix; all 71 affected tests pass afterward.
  Fresh five-arm coverage preserves every mechanical signature and changes only the combined
  angle boundary count and its false failure. Genuine release failures remain unchanged.

- Added audit-only chain action/selector observations with per-bout control parity, source/input
  fingerprints, exclusive output and regression coverage. Captures actual returned weights and
  gates without rerunning choices or changing normal play; missing observations fail explicitly.
  The verified 300-bout run preserves all control audits/RNG states. Of 196 named-stage losses,
  86 resolve another submission identity and 51 involve eligible pool exclusion/selection.
  Documents remaining uncertainty and the larger action-choice surface; 30 affected tests pass.

- Added an isolated earlier-repertoire-adaptation trial, reusing the existing adaptability
  curve and 14-point ceiling while leaving live follow-ups unchanged. Includes purity,
  nested restoration, normal-play parity and full-calibration CLI checks. No release promotion.
  The completed 3,840-fight/39-group trial records 2,267 finishes / 611 KO / 715 TKO,
  with competitive 47.26% and middle timing failures. Distinct variety improves to 17.1833
  but still fails; chain median one, 25.53% top-ten share and 23 unused IDs also fail.
  Fresh default controls match exactly, source fingerprints verify, and 42 affected tests pass.

- Added the user-approved audit-only stronger-continuation trial. It doubles only the existing
  action-choice chain bonus, retaining the skill/gas curve, legal integer ticket budget and
  maximum combination with prepared intent. Initiative and default candidate remain unchanged.
  Nested/error restoration, survival, prepared-intent and default-off parity regressions included.
  Fresh control coverage is exactly unchanged. The +4 trial raises chained share to 21.47%
  but fails chain length/variety; its 3,840-fight calibration records 2,289 finishes / 626 KO /
  727 TKO, competitive 47.47%, a timing failure and zero sampled doctor stoppages. It is not
  promoted; default candidate, normal play, release references and EXE remain unchanged.

- Recorded the completed full shipping regression run and latest affected reruns in
  `docs/FIGHT_RELEASE_CHECKPOINT.md`, including the smoke display-probe limitation.
  The updated combined 800-move selector benchmark passes at 10.88% median share.
  Candidate balance/content failures remain explicit; the existing EXE is unchanged.

- Extended existing top-position style preferences to experimental turtle/front-headlock
  control, where specialist action names previously bypassed them. Reuses the established
  style factors, with physical ownership and default-off guards; adds no RNG or legal actions.
  New policy and integration regressions protect plan ordering, survival and normal-play parity.
  All six affected suites pass (60 tests). Fresh full calibration improves competitive finishes
  to 47.40% with 2,272 total finishes / 606 KO / 725 TKO, still below acceptance. Coverage has
  slightly lower top-ten concentration but worse distinct variety and 24 unused IDs; no promotion.

- Fixed an experimental registry orphan: the existing fence-drive move now matches the
  failed-shot source of its force-cage action. Preserved its stable ID, normal-play registry
  and candidate-context restoration; regression checks cover source positions and nested errors.
  This changes candidate selection eligibility and requires fresh calibration before packaging.
  Fresh five-arm coverage confirms the restored identity and identical mechanical signatures;
  total unused moves remain 23 because another fence-drive variant goes unobserved.
  Full 3,840-bout calibration records 2,269 finishes / 604 KO / 724 TKO; competitive finishes
  improve to 47.26% but still fail the 48% floor. All 39 groups are retained; no build promoted.

- Closed two candidate-reporting gaps: enforce the written full-catalogue unused-move limit
  and 200-follow-up floor, with explicit active-ID inventory. Boundary regressions reject nine
  unused moves and 199 follow-up declarations; generic and retired IDs cannot inflate coverage.
  No fight mechanics, release targets or continuation cap changed.
  Fresh five-arm coverage preserves all mechanical signatures and exposes 23 unused active moves
  (limit eight); the active follow-up count passes at 363. These failures remain visible.

- Removed unsupported defense-tag reaction preference from experimental chains. Multiple legal
  successors remain available to the existing weighted selector instead of forcing a guessed
  tuple-index choice. Root accounting, expiry and legacy behavior are preserved.
  Fresh full calibration records 2,268 finishes and competitive 47.22%; variety and chain length
  still fail. All seven affected regression suites pass. No candidate executable was packaged.
  The fresh combined 800-move selector profile passes at 10.89% median share, below the 15% cap.

- Added an explicit audit-only thirteen-root continuation graph trial, with immutable/idempotent
  application, full graph validation, nested-context regression coverage and source fingerprints.
  The evaluator retains full calibration and every failure; no normal-play graph was changed.
  Full trial: 2,270 finishes, competitive 47.19%. Chained share improved, but variety and specialist
  coverage worsened, so the graph trial remains disabled by default.

- Completed separate full-corpus trials for clinch target preview, round-streak reset and counter
  fallback. Latest candidate: 2,267 finishes, competitive 47.19%; only two draft moves remain
  unobserved in coverage, down from eleven before these fixes. Balance, variety and chain-length
  acceptance still fail; all eleven affected regression suites pass, and no EXE was replaced.
  Combined-candidate selector profiling with 800 moves also passes the 15% gate at 11.06% median;
  this does not claim unchanged whole-career simulation latency.

- Corrected experimental clinch-chain target previews: body-knee opportunities use the
  existing contest and cleaned weapon-ticket probabilities instead of assuming every clinch
  strike targets the head. Previewing adds no random draws or guaranteed continuations.
  Its isolated 3,840-bout trial still fails competitive finish acceptance; it is a correctness
  fix, not demonstrated balance recovery.
- Reset experimental actor streaks at round breaks, preventing the comeback intervention from
  penalizing a fighter for the previous round's expired sequence. Within-round comeback rules
  and default-off behavior remain unchanged.
- Preserved eligible ordinary authored combinations in experimental counter windows when no
  counter survives skill/rarity gates. Actual eligible counters retain priority; wrong-style
  combinations remain excluded. Regression tests cover both chain preview and final selection.
  Repinned the natural prepared-submission observer regression to a setup in fresh coverage,
  preserving nonempty observation and complete trace/result/RNG parity requirements.

- Added an audit-only bottom-guard knee-line entry route driven by leg-entry versus sweep
  skills. It reuses the existing contest and records explicit generic entry evidence without
  sweep, takedown, submission or physical-top credit. Half guard and normal play are unchanged.
  Coverage rejects missing provenance and false credit; promotion still requires fresh full audits.
  Fresh 3,840-bout calibration records 2,255 finishes / 593 KO / 700 TKO; only competitive
  finishes fail balance (46.98% versus 48–54%). Coverage records three valid entries, but
  variety, chain length and ordinary content gaps remain. No candidate executable was built.

- Wired enabled tactical plans into experimental turtle/front-headlock physical-top menus.
  Chase, conserve and submission-hunt preferences now affect existing specialist actions before
  chain weighting, matching the plan energy costs already charged. Survival, ownership, bounded
  execution and default-off behaviour are preserved; no actions are added through policy aliases.
  Candidate reports now fingerprint both new policy modules and reject source changes during collection.
  Full-corpus timing passes in the combined trial, but competitive finishes (46.91%), variety
  and chain targets still fail. Enabled-plan natural-bout tests cover the tactical integration;
  the frozen schedule does not exercise enabled plans. No experimental executable was packaged.

- Added a bounded experimental ordinary repertoire: up to eight already-eligible close-score
  alternatives can participate instead of an unconditional five-move cutoff. Authored/counter
  priority and strongest-five chain preference are retained; no random draw or legal action is
  added. The variety observer shares the same pool construction and tests actual selection parity.

- Corrected experimental repertoire adaptation: higher adaptability now increases, rather
  than reduces, the existing anti-repetition selection pressure. The two-use threshold,
  14-point cap, legal move gates and legacy default-off scoring remain unchanged.
  Fresh calibration records 2,262 finishes / 587 KO / 712 TKO; remaining candidate gates
  still fail. Full engine regression and 32 adjacent tests pass, and the 800-move selector
  benchmark stays below its ceiling at 13.89%. No failing candidate was packaged.

- Corrected experimental leg-escape defence to use the knee-line controller's leg-lock,
  positioning and discipline skills rather than generic bottom-game defence. Production
  arithmetic, escape thresholds and RNG draws are unchanged; candidate promotion remains gated.

- Replaced exact historical finish/KO/TKO totals with a user-approved closeness policy:
  two percentage points of headline-rate drift on the full unchanged 3,840-fight corpus.
  Retained competitive, subgroup, method-family, timing and rare-stoppage checks, plus exact
  parity for neutral refactors. Boundary regressions cover both acceptable and excessive drift.
  No fight mechanics, save format or executable changed; historical reference files remain intact.

- Added an audit-only round-boundary counter-expiry trial. It clears proven prior-round
  counter windows before either fighter's initiative score, preserving same-round behavior
  and the normal playtest build. Focused tests cover lifecycle, isolation and artifact safety;
  full calibration and five-arm coverage remain mandatory before any promotion.
  The trial records 2,257 finishes / 586 KO / 708 TKO and is not promoted. Coverage
  failures now print explicitly and return failure rather than an empty success summary.

- Added an isolated failed-shot-retention comparison to the fight candidate audit, with recorded
  standing departures and zero-takedown shot destinations. This helps distinguish lost standing
  opportunities from weaker strike resolution; it changes no career mechanics or simulation cost.
  Regression coverage protects the successful/cage paths, audit isolation and control-arm parity.
  A separate counter-window observer records old windows at actual initiative calls without
  changing their lifecycle or treating guard observations as lost attacks.
  The full 3,840-bout-per-arm experiment recovers some standing offense but remains rejected
  at 2,279 finishes / 609 KO / 724 TKO and loses failed-shot residence. The audit report retains
  that result, complete control-trace parity and the passing 13.96% selector benchmark.
  Added a separate audit-only Sprawl And Brawl escape-intent trial that keeps the scramble,
  every legal action and the original integer action-ticket budget available.
  Its full calibration gains one finish and five KOs (2,265 / 591 / 714), but still fails release
  gates; its 300-bout coverage arm is unchanged. Neither experiment is enabled in normal play.

- Corrected the common-ground registration regression to validate the final layered follow-up
  definitions, so the canonical portable-build suite now distinguishes intentional Phase 31
  successor enrichment from an accidental change to the underlying techniques.
  The complete shipping suite, locked 3,840-bout result/action baselines, three-seed stability
  playtest and portable startup check now pass, and both Windows executables were rebuilt for the
  validated 346-move production checkpoint; the rejected 400-move candidate remains audit-only.

- Extended the paired candidate-striking audit with trace-proven finish/knockdown cohorts plus
  gas, streak, counter, position and style attribution, and a candidate-only pocket-action-bias
  ablation. The complete 3,840-bout comparison rejects removing that bias (2,258 finishes versus
  the candidate's 2,264) and locates the KO/TKO deficit primarily in 68 fewer bouts producing any
  knockdown—especially fresh standing lanes—not a new post-knockdown stoppage failure.

- Added chain-root completion cohorts for position, outcome, actor streak, gas and declared
  successor-action breadth. Sensitivity trials show that ordinary commitment/cadence tuning cannot
  meet both median-two chaining and the variety gates. A turtle-hammerfist frequency bonus was
  rejected after it merely exchanged one unused specialist for another and cost one candidate KO.

- Extended chain-opportunity diagnostics with root-specific existing-move candidates, action
  coverage and context counts. A three-root successor trial and its one-two-only ablation were
  rejected after full calibration fell to 2,243 and 2,246 finishes respectively; no failed
  successor map is enabled in the candidate or production engine.

- Documented and rejected defense-tag tuple-index branching after audit showed that selected
  defenses may be breached and follow-up tuples do not carry universal reaction semantics.

- Added a read-only variety-opportunity diagnostic that measures distinct-move headroom within
  observed legal final pools, retaining interrupted/zero-opportunity fighters and selector priorities.

- Corrected experimental continuation previews to respect eligible counter-technique priority,
  preventing initiative credit for a follow-up the live selector cannot choose.

- Added an experimental, bounded standing-combination initiative benefit. Existing survival,
  counter opportunities, actor-streak handling and combination expiry remain authoritative;
  the feature stays disabled in normal play pending complete calibration.

- Added an audit-only standing-chain ablation comparison to isolate action-weight displacement
  without changing pocket mechanics, ground combinations or any production finish formula.

- Chain diagnostics now distinguish emergency survival, undeclared next actions and declared
  actions without a named continuation, while retaining interrupted roots and existing bounds.

- Added experimental retained-grip shoulder positioning for Von Flue follow-ups. A real failed
  guillotine can lead to a contested control beat, then an optional choke; the defender can clear
  the wrap. Interruption/expiry and explicit provenance prevent position alone from granting it.

- Added a combined-only extended frequency diagnostic using the same deterministic fight corpus.
  It retains failed content/variety targets and is unavailable as a shortcut for full calibration.
- Added audit-only prepared-submission probability and outcome observations, separating ordinary
  weighted choices from opponent interruptions and emergency survival without changing mechanics.
  Observation/root reconciliation rejects missing telemetry. Extended defense-repetition audits
  now enforce the specified rolling 300-fight window while retaining whole-run totals separately.
- Added a read-only matched current-normal/candidate striking diagnostic with action/position
  totals and input/source/trace fingerprints. It does not substitute legacy subgroup data for a
  current comparator or infer unrecorded stoppage checks from final result labels.
  Its optional full-calibration mode uses the exact 3,840 fixtures per side and canonical groups,
  with schedule-matching tests and no sample-size override.

- Corrected experimental specialist skill lookups and physical-control accounting. Front-headlock
  and turtle controllers receive their earned control time; knee-line ownership remains distinct.
  Explicit award-point evidence supports trace/box-score checks across referee stand-ups.

- Added bounded experimental preference for following through on a valid prepared submission.
  Readiness, gas and enabled plan execution affect existing action/technique weights; survival,
  alternative choices, eligibility gates and one-exchange expiry remain intact.

- Added experimental strong-guillotine defense assessment separating stopped choke pressure,
  retained neck grip and shoulder positioning. Existing control credit remains unchanged; the
  follow-up stays optional. Candidate gates validate the grip/angle facts and setup provenance.
- Added setup follow-through diagnostics so unsuccessful and interrupted opportunities remain
  visible rather than reporting only successful specialist moves.

- Added experimental scarf-hold near-arm isolation and a separate, single-use armbar opportunity.
  Commentary distinguishes setup from the actual attempt; unrelated control signatures and chains
  receive no false credit. Candidate audits reject attempts without consecutive setup evidence.
  A same-exchange referee stand-up cancels the setup and takes narration priority, preserving the
  existing inactivity threshold and preventing stale ground-position commentary.
- Separated experimental side-control intent into arm isolation versus riding/pinning. Skills,
  bounded style familiarity and enabled plans guide intent before the existing setup contest;
  ordinary control techniques are no longer displaced by every successful hold.
- Candidate calibration artifacts now preserve all canonical group breakdowns alongside the
  headline result and unchanged acceptance failures, making tier-specific balance drift inspectable.

- Added explicit experimental retained-guillotine setups for Von Flue attempts. The opportunity
  is ownership-bound and single-use, with truthful setup/use commentary and trace-provenance
  checks; neither half guard alone nor a stale move name enables the choke.

- Added an experimental two-beat straight-ankle entry from top guard. Knee-line control must be
  contested before a later finishing attack, with explicit entry/denial commentary and no phantom
  pass, takedown or finishing credit. Candidate audits validate entry position and ownership.

- Fixed experimental leg-lock escapes being narrated against non-leg submissions. Defense
  selection now follows explicit current technique evidence rather than broad shared families;
  a permanent candidate audit rejects contradictory leg-only defenses.

- Corrected experimental submission bonuses being awarded to fighters underneath dominant
  positions. Top ownership now gates dominant-position benefits, and bottom guard-work benefits
  require guard/half guard. Restored the valid top-guard Ezekiel removed by the pool cleanup.

- Separated experimental submission pools by actual position and top/bottom ownership, removing
  impossible mixed-geometry and unsupported setup labels. Normal-play pools remain unchanged;
  the changed candidate distributions still require finish-calibration acceptance.

- Fixed experimental submission follow-through preferring unavailable holds. A pure current-pool
  preview weights compatible opportunities without extra random draws or duplicate branch credit;
  normal-play selection remains unchanged.

- Made experimental continuation intent skill- and energy-sensitive, replacing the flat action
  bonus while preserving legal options, survival overrides and the original random-draw budget.
  Follow-ups remain fallible; interrupted roots still count in chain-quality measurements.

- Connected 21 experimental submission variants to pre-draw mechanical selection, with legal
  position, role, skill and exclusive-style gates. Existing pool length and per-slot choke/leg
  classification remain unchanged; attacks requiring unproved setups are deliberately excluded.

- Added submission-identity audit counts by technique and position. Incompatible named claims
  fail the candidate quality gate; generic fallback remains visible as missing move coverage.

- Added experimental persistent pocket distance from contested punches, defender-attributed
  pivots, jab exits and neutral inactive disengagement. Close-range action preferences differ
  without extra distance RNG, damage or control credit; normal-play behaviour remains unchanged.

- Added pocket-residence diagnostics to candidate fight audits: entries, occupied beats and exit
  reasons reconcile separately, including zero-beat entries interrupted by a horn or finish.

- Corrected experimental submission identity selection: resolved locks filter named candidates
  before signature preference, so a heel hook cannot be credited as a rolling-kneebar signature.
  Unsupported delivery variants use an uncredited generic identity instead of invented facts.

- Completed the audit-only 400-definition catalogue with fifteen leg-game drafts. Added a
  skill-sensitive experimental knee-line retention after an actually attempted, non-finishing
  leg submission. Existing finish rolls and normal-play entry rules remain unchanged; positional
  counter-lock reversals are not credited as submission attempts or completed takedowns.

- Fixed unearned recovery, success credit and contextual praise after automatic separations in
  the experimental specialist engine. Neutral restarts clear both fighters' pending combinations
  while retaining control points genuinely earned before the separation.
  Chain diagnostics also exclude restart-interrupted roots from possible successor coverage
  without dropping them from the attempted-chain denominator.

- Extended the audit-only specialist candidate with contested rear-control entries from cage
  pummelling and eight standing-back techniques. Stationary rear locks have a bounded separation;
  the unfinished candidate remains disabled in normal play pending calibration.

- Added exchange-occupancy and per-draft frequency evidence to joint fight-engine trials.
  The pocket target now measures its share of standing exchanges explicitly, preventing ground
  time or distinct context counts from distorting the planned distance-mechanics gate.

- Added a scoped joint chain/specialist experiment harness, including the five drafted entry
  identities in complete-fight trials. Its five-arm comparison separates content, chain-choice
  and specialist-entry effects; final variety targets remain explicit failures until met.
  Candidate registry bindings, feature flags and caller RNG restore after nested trials or errors.
  Implemented audit-only legal follow-up action weighting without extra draws or forced techniques.
  In the first combined 300-fight trial, chained selections rise from 17.51% to 19.15% and observed
  positions from eight to eleven; attempted-chain median remains below target. Normal play is unchanged.
  Added nine front-headlock drafts and role/resolver tests, including named breakdowns for the
  previously generic front-headlock ride. Trial reports now bind exact registry fingerprints and
  exclude generic fallbacks from authored-move counts. These additions are not yet registered for normal play.
  Extended the draft with ten turtle and seven failed-shot techniques, plus bounded guillotine
  grip-change successors. The audit catalogue is now 377; production registration remains gated
  on complete ordinary-use and calibration verification.

- Recorded the approved joint chain/specialist development sequence. Mechanical candidates must
  pass the existing finish-calibration gates before release; prior verified measurements and
  immutable snapshots remain separate from in-progress results.
  Added an opt-in specialist-entry candidate and read-only full-corpus evaluator. The first trial
  failed finish calibration and remains disabled in normal play; no release baseline was replaced.

- Tightened the chain-opportunity diagnostic with exact three-technique coverage over legal
  positions, roles and targets. Overlapping opportunities count once; the diagnostic still does
  not relax chain acceptance or alter simulation behavior.
  Retained a tested, unregistered defensive-follow-up experiment; it is not a shipped content change.

- Added 30 further follow-up lists across clinch, striking, top control and bottom recovery.
  Paired 300-fight chaining rises from 15.53% to 17.51%, while the leading top submission falls
  from 20.39% to 19.64% of top-submission selections. Broad mechanics remain identical in the pair;
  the attempted-chain median remains unfinished. No failed roots or moves were deleted.

- Added a reproducible chain-opportunity audit that counts interrupted roots, distinguishes
  expiry/ownership/round losses and reports likely next-action coverage without changing fights.
  Its optimistic bound is explicitly limited to the observed root cohort, not a new balance gate.

- Added move-retirement support without removing permanent IDs. Retired techniques remain
  loadable in old signatures/mastery but cannot be selected or newly acquired through generation
  or camps. The default-false schema migration is explicitly verified; no current move is retired.
  Integrated 30 action-diverse follow-up lists. In 300 paired fights, chained selections increase
  from 12.03% to 15.53% with identical broad mechanical evidence; the longer-chain target remains open.
  Historical registry references are preserved through a separate successor-only migration.

- Specialist submission attacks, back takes and escape attempts now count as ground activity,
  including defended attempts, matching ordinary grappling. Static rides and holds still trigger
  referee warnings and stand-ups; no new specialist entry chances or finish conversion were added.

- Added a strict selection-level baseline across 44 complete fights and 1,786 chosen move
  payloads, protecting move/defense facts, sequence traces and mechanics during selector refactoring.
  Split selection into six focused candidate, static/context scoring, rarity, draw and payload helpers
  while preserving exact pre-refactor selection evidence.
  Cached static fighter/move scores for one bout and hoisted shared exchange context, with
  nested/failed-bout cleanup and protection against stale replacement definitions.
  The 800-move diagnostic's median selector share fell from 27.99% to 14.06%, below the fixed 15% gate.
  Added a permanent 400-line ceiling for individual catalogue source files.

- Wired 125 reviewed legacy follow-up lists, bringing original moves with successors to 163/200.
  A checksum-bound migration manifest protects all other historical fields and lookup ordering.
  Eight links use named positional submissions to avoid over-selecting the generic submission chain.
  Chain ownership now covers specialist positions; reporting explicitly flags the unfinished
  attempted-chain-length target. Added a repeatable isolated 800-move selector profiler.

- Live move chains now consider alternative branches matching the actual next action, retain
  top/bottom ownership legality and track unique occurrences with observed depth. A continuation
  can take priority only within the strongest five eligible ordinary choices; authored signatures
  and counter restrictions remain protected. Successful same-position sweeps can continue chains.

- Added an immutable follow-up graph with import-time cycle/dangling-target rejection and
  deterministic potential-depth reporting. Existing move definitions and selection order are unchanged.

- Expanded to 346 moves with six shots, seven takedowns, eight advances, seven controls,
  eight ground-striking chains and eight bottom submissions. All 18 styles now have at least
  18 authored techniques; tests protect actual shot landings, neutral mechanics and legal follow-ups.

- Expanded the active catalogue to 302 moves: ten power punches, six jabs, ten kicks, eight inside
  strikes, seven clinch entries, seven controls and five exits. The coverage report now
  measures total selections, distinct techniques per fighter, top-ten concentration and chain share.
  Fixed referee stand-ups being credited as effective fighter responses in trait/camp commentary.

- Added 33 bottom-position and recovery techniques: ten guard recoveries, eight sweeps, six
  stand-ups, five control ties and four survival actions. Expanded named defenses from 18 to 40,
  restricted body blocks/shoulder rolls to suitable attacks, and varied factual defensive calls.
  The defense reachability audit supplies valid target/weapon context for those restrictions.
  Shoulder rolls reject knees and elbows even when they use the broad power-punch action.

- Expanded top-position submission identities from 9 to 25 with 16 new techniques, including
  half-guard kimuras, D'Arce/Brabo and arm-triangle chokes, top-guard attacks and side-control
  finishes. The catalogue now has 216 moves; novices retain the ungated submission-chain fallback.

- Added a deterministic 300-fight move-coverage gate that separates missing positions, actions,
  targets, skill gates and style eligibility, with explicit allowances for existing gaps. Added
  tested technique-authoring helpers and a complete schema reference for the upcoming expansion.

- Split the MMA technique catalogue into domain modules while preserving its public imports,
  all 200 move definitions and their selection order. Added immutable precomputed lookups and
  import-time checks for misspelled metadata and removed stable move IDs.

- Added a historical move-registry parity gate for the planned catalogue expansion. It checks
  every definition field, registry order and legal action/position/target lookup against the
  original 200-move reference, and rejects changed ordering or a corrupted reference.

- Hardened generated-fighter style assignment. Every new entrant now finishes generation with a
  supported primary style and a validated non-duplicate secondary style, even if an enrichment path
  supplies a blank or legacy behaviour label; the repair is deterministic and adds no RNG draw.
- Improved between-round coach feedback. Verified head coaches and gyms now deliver a factual round
  read followed by a clear instruction based on knockdowns, damaging offense, gas, repeated moves,
  successful families or control. Balanced, Technical, Excitable and Concise voices present that
  feedback differently without consuming RNG; stablemate bouts do not assign one coach to both corners.
- Added fact-driven camp commentary. Fighters with a real matching Gym receive natural pre-fight
  corner introductions naming their saved camp, coach, location and specialties, plus bounded live
  calls when completed boxing, kickboxing, wrestling, BJJ, sambo, clinch, gameplanning or conditioning
  evidence matches that Gym's strengths. Promotion, academy, unknown and legacy camp strings are
  described naturally but conservatively without invented coaches or specialties, while opponents
  from the same verified Gym receive one shared stablemate introduction instead of contradictory
  corner claims. Live camp calls are deterministic, capped at one per fighter per round and two per
  bout, and seeded A/B coverage proves they cannot affect fight mechanics, stats, scoring or results.
- Added natural fight-night commentary for all 46 saved fighter traits. Every bout now introduces
  each fighter's trait in broadcast language, while combat-relevant traits receive additional live
  callouts only when recorded targets, moves, positions, damage, gas, timing or fight context support
  them. Contextual calls are deterministically capped at one per fighter per round and two per bout;
  they consume no RNG and do not affect actions, damage, scoring or finish rates.
- Added one exclusive authored finisher for every supported MMA style. Striking disciplines receive
  style-native knockout techniques, wrestlers receive causal ground-and-pound stoppage sequences,
  and submission styles receive distinct chokes, joint locks and leg locks. Primary or secondary
  style ownership is required even when a move is saved as a signature. All 18 finishers appear in
  the representative 880-fight corpus and only identify the technique beneath an already-resolved
  KO, TKO or submission, so no damage, finish or stoppage roll was added.
- Added one exclusive authored combination for every supported MMA style. Boxing, kickboxing, Dutch
  kickboxing, Muay Thai, karate and taekwondo receive distinct ordered striking chains; Sanda,
  wrestling, catch wrestling, jiu-jitsu, Luta Livre, sambo, judo and the mixed disciplines receive
  style-native strike-to-entry, takedown, transition or submission-threat sequences. All 18 retain
  ordered component evidence, are available through a fighter's primary or secondary style, and are
  excluded from unrelated styles. The 880-fight representative report observes every addition with
  no dominance, legality, style-distance or reachability diagnostic; no broad action, damage or
  finish roll was added.
- Added twelve one-time visible-damage milestones across persistent head, body and leg trauma,
  including facial marking, torso bruising, guarded movement, leg welts, weight shifts and a
  visible limp. Exact structured cuts now name their recorded location and describe heavy bleeding
  or threatened vision only when those facts exist. These deterministic lines survive Broadcast,
  appear chronologically in replay analysis and consume no fight RNG or add damage/stoppage rolls.
  Post-fight recovery now also consumes the completed bout's trauma before career-stat cleanup;
  a 256-fight gate found 1,043 damage events with complete factual narration and Broadcast retention
  while the locked finish and action baselines remain the mechanical authority.
- Expanded the ground catalogue from 117 to 140 moves without changing grappling resolution. Six
  position-specific striking chains now distinguish guard, half guard, side control, mount, back
  control and turtle offense; five passing/climbing transitions, four sweeps, four recoveries/get-ups
  and four retention/control rides add further ground identity. Twenty-two additions appear in the ordinary
  880-fight representative corpus, while the turtle wrist-ride chain remains legally reachable in
  its specialist state. Ground-strike components reconcile at realistic volume and mounted TKO
  narration follows the causal registered move. The exact finish and broad-action baselines remain
  the authority, so none of this content adds a strike, pass, sweep, escape or finish roll.
- Expanded the standing technique catalogue from 101 to 117 moves. Ten new combinations add
  body-to-head boxing, stance shifts, pressure flurries, hand-to-kick chains and punch/elbow/knee
  sequences; six uncommon finishers add uppercuts, corkscrew crosses, spinning backfists, axe kicks,
  jumping front kicks and switch flying knees. These identities are selected after broad exchange
  resolution and never add a landing or finish roll, preserving the exact 2,319/633/731 frozen
  finish/KO/TKO corpus. Every addition appears in the representative 880-fight report, combination
  components reconcile at realistic volume, and causal registry moves now replace contradictory
  legacy strike names in both sudden and ordinary KO/TKO finish narration.
- Made MMA play-by-play reflect who the fighters are without changing what the fight engine decides.
  Standing calls now rotate by the recorded technique, relevant detailed strength and style family,
  with broader survival, clinch, transition, escape and damage language instead of a few repeated
  resets. Ground sequences now preserve the actual submission technique, named escape defense,
  top/bottom ownership, live position and natural escape consequence; referee stand-ups are recorded
  as officiating resets rather than false fighter escapes. Broadcast duplicate limiting now applies
  to important position calls as well as routine lines. A 256-fight permanent gate and 2,048-fight
  extended audit verify profile coverage, submission continuity and low repetition while the frozen
  result and action baselines remain the mechanical authority.
- Completed the finish-system follow-up audit without replacing the accepted fight balance. The
  canonical corpus remains exactly 2,319 finishes (60.39%), 633 KOs (16.48%) and 731 TKOs
  (19.04%); competitive bouts independently remain inside the 48-54% finish and 15-19%
  submission bands, while 19.55% of five-round finishes occur in rounds four or five. Permanent
  gates now reject lost doctor/injury reachability or deficient late-round finishes. Body- and
  leg-kick knockdowns no longer masquerade as injury stoppages, serious-cut doctor checks remain
  between-round only, and authored submission, doctor, injury, head-kick and walk-off endings now
  survive the final commentary sequence. Finish, KO, submission and highlight-KO method families
  are centralized so career stats, awards, recovery, excitement and contractual bonuses agree.
- Restored the visible fight story in the default Broadcast feed without changing fight resolution.
  Landed standing offense, completed takedowns, landed ground strikes, passes, sweeps, reversals and
  escapes are now prioritised during round compaction, while identical wording remains capped and
  condensed notes count the actual omitted standing,
  standing-striking, takedown, ground-control and ground-striking lanes. Successful sweeps that retain the same named position
  now use the recorded top/bottom ownership change instead of being miscalled as denied. The
  canonical release runner also verifies the frozen 3,840-fight action-frequency baseline alongside
  the unchanged 60.39% finish, 16.48% KO and 19.04% TKO gates.
- Made Fight Night audio continuous across the full live card. A loop-safe, crossfaded arena bed now
  stays open beneath walkouts, round bells and factual reactions instead of reopening the Windows
  output for every stretch of crowd ambience; later-round bells use the mastered crowd pack, and
  closing the live viewer cancels its bed and in-flight reactions. Knockdowns and finishes retain a
  reserved reaction slot, while decision and card-complete calls no longer suppress one another.
  Live audio stop events and audio-only system entropy are excluded from event rollback snapshots so
  an active audio session cannot prevent a completed card from settling.
- Expanded the fact-driven MMA broadcast without changing combat resolution. Landed, blocked,
  slipped, countered, transitioned and escaped exchanges now have deterministic wording variation;
  recorded signatures, technique mastery, stance lanes, plan changes, momentum, damage, camps,
  championship stakes and rivalries can add context only when the trace or bout data proves it.
  The live viewer can switch between Broadcast and Detailed feeds in place, shows the selected
  voice, separates rounds more clearly and highlights knockdowns and finishes. New commentary
  quality gates reject excessive repetition, missing or contradictory facts, broken finish
  continuity, duplicate results, technical-suffix leakage and any one-bout change to the accepted
  3,840-fight finish/KO/TKO calibration.
- Reworked MMA commentary as a presentation-only three-phase upgrade. Fight Night now defaults to a
  compact Broadcast feed that suppresses repeated low-value calls while preserving the complete
  stored transcript, every structural/evidential line, sealed-scorecard flow, and Detailed review.
  Exchange prose now comes from the recorded move, target, defense, outcome and position; finish copy
  follows the actual final technique; corner advice cites visible fight evidence; and persistent
  Balanced, Technical, Excitable and Concise voices change wording without changing fight mechanics.
- Improved full-world calendar performance without reducing simulated activity. AI promotions now
  compute their card day and fatigue policy once per readiness pass, fight commentary reuses the
  head-to-head result already stored in bout state, and regional intake batches reuse one collision
  set instead of rescanning the world for every recruit. Promotion monthly reviews are now assigned
  to stable, evenly distributed weekly groups; the sixteen regional circuits retain one card and
  one development review per circuit each month without a month-boundary card pile-up.
- Added named defensive technique identity beneath every exchange. Eighteen registry-backed defenses
  cover guards, parries, evasions, kick checks/catches, sprawls, whizzers, cage frames, underhook turns,
  hand fighting, wall walks, submission escapes and technical scrambles; traces and release reports
  retain the attempted defense without changing whether the calibrated broad action succeeds.
- Added persistent technique mastery for offensive moves and defenses. Camps develop focus-aligned
  techniques, sufficiently mastered moves can become signatures, academy prospects develop and carry
  mastery into their professional careers, and veteran mastery declines gradually after the athlete's
  prime. Profiles and academy reports expose the strongest techniques; legacy saves default safely.
- Added deliberate in-fight stance switching and defense/position-aware branching sequences. Skilled
  switchers and adaptable fighters can change side from plan and matchup evidence, while authored
  follow-ups can select an alternate legal branch after frames, evasions or escapes. Every switch and
  branch is retained in the trace and remains one ordinary broad action.
- Added a per-round replay analysis explorer showing effectiveness, top moves, named defenses, sequence
  paths, stance changes and corner-plan changes. Expanded the shipped authored-signature cohort from 40
  to 114 fighters across 13 styles, including natural homes for all 17 legal specialist techniques that
  are rare in the representative synthetic sample, and strengthened their legal selection priority. The locked 3,840-fight rates remain
  exactly 60.39% finishes, 16.48% KO and 19.04% TKO.
- Deepened the technique layer without changing the calibrated broad outcome model. Complete fights
  now retain elite failed-shot/front-headlock, standing-back-control and leg-entanglement states;
  registry energy, miss risk and counter vulnerability shape later move choice; successful authored
  follow-ups create bounded two-tick sequences; and all 18 styles have explicit technique-tag identities.
  Stance lanes and striker/grappler matchups also influence legal technique selection, while corners
  evolve plans from repeated opponent moves and effective move families with evidence and confidence.
- Expanded the shipped authored-signature cohort from nine to 40 fighters across 11 styles. The
  Simulation Lab now reports top moves, completed sequences, signature effectiveness, average technique
  load/risk, stance-lane usage and fight-plan evolution. The general release report has grown to 880
  fights with distinct style-distance and open/closed/switch-stance checks, while a separate 720-fight
  specialist-transition report proves the rare positions and their legal follow-ups occur in complete
  bouts. The frozen 3,840-fight finish percentages and competitive finish converter remain unchanged.
- Completed the fight-move release audit. A deterministic 840-fight report now samples every supported
  style plus behaviour, tier and stance, and checks registry reachability, dominance, mismatch-only use,
  counter/position/target legality and energy bounds. All 101 moves are reachable and the safety checks
  are clean; the frozen 3,840-fight result and action baselines remain exact. Turtle breakdown and sit-out
  traces now use their real broad actions. Standing-skill development preserves its prior per-skill
  exposure after the three-skill boxing expansion, and the Database Editor audit safely handles sparse
  legacy skill overrides without mutating them on view.
- Expanded Fight Night and analysis presentation around the exact recorded technique. Calls now label
  target, defense and meaningful follow-up in text; completed rounds show bounded move-family
  effectiveness, Quick Fight Simulator results summarize each corner's main families, and fighter
  statistics retain career family/signature totals. Raw trace counts reconcile with every summary,
  official cards remain sealed, and presentation consumes no mechanics RNG.
- Added bounded tactical move selection beneath the existing fight plans. Prior public trace can make
  body/leg work and feints open later families, repeated techniques become less selectable, and
  adaptable counter fighters exploit real patterns only inside a counter window. Every choice retains
  explainable selection reasons, bout-local read counts cap at 12, and no new RNG or direct finish
  modifier was added.
- Added stable fighter signature moves. The Database Editor exposes three registry-backed selections,
  nine authored stars have first-cohort move sets, and skill-supported generated fighters receive
  deterministic signatures without RNG. Legal signatures are preferred but never guaranteed; traces,
  post-fight telemetry and career totals track attempts, effective uses and finishes. Unknown future
  IDs degrade safely, duplicate-name fighters remain independent, and outcome calibration is unchanged.
- Expanded the MMA registry to 101 moves with ground-strike chains, pressure passes, mount/back/turtle
  transitions, sweeps and reversals, major choke/arm-lock families, and legal ankle/knee/heel/toe-lock
  attacks. Every submission now validates its positional attack path and explicit failed-attempt
  outcomes; major grappling styles produce distinct legal technique mixes without adding a redundant
  submission-chaining rating or changing finish conversion.
- Expanded the MMA registry to 77 techniques with over-under, underhook and Thai-plum entries; cage
  pummels and exits; mixed shot entries; trips, throws, knee taps and high-crotch finishes; re-shots,
  whizzers, mat returns, standing rides, escapes and front-headlock go-behinds. Every takedown now
  validates an entry family, legal defenses and possible finish positions, with distinct Wrestling,
  Judo, Sambo and Sanda move mixes and unchanged calibrated outcomes.
- Expanded MMA standing technique coverage to 54 registered moves. Kicks now retain target, lead/rear
  side, range band and legal defensive families; new options include calf kicks, teeps, side/switch/
  question-mark/wheel/spinning kicks, punch-kick chains, intercepting/flying knees and step-in/spinning
  elbows. High-risk moves are skill-gated and uncommon, five major kicking styles produce distinct
  move mixes, and the exact 60.39% / 16.48% / 19.04% calibration remains unchanged.
- Added three distinct MMA boxing ratings—combination punching, body punching and counter timing—
  with deterministic legacy-save derivation, generation, development and editor/profile support.
  Seven new boxing sequences include double-jab-cross, cross-hook-cross, body/head changes and three
  counter-only returns. Technique skill now governs which legal sequence appears, counter moves need
  a real counter window, and broad damage/gas resolution remains unchanged at the exact 60.39% /
  16.48% / 19.04% calibration.
- Added the canonical MMA move registry and trace contract. The initial 32 named punches, combinations,
  kicks, entries, takedowns, clinch actions, ground transitions and submission families now retain
  stable IDs, legal positions, targets, skill/defense bundles, risk metadata and follow-ups beneath
  the existing calibrated actions. Technique selection is deterministic and uses an explicit generic
  fallback; broad action resolution and the 60.39% / 16.48% / 19.04% outcome calibration remain unchanged.
- Completed the move-expansion foundation. Jiri Prochazka and Brandon Royval now retain supported
  Kickboxer/BJJ styles, Lito Adiwang and Kevin Belingon use the supported Sanda identity, and shared
  universe validation rejects unsupported MMA styles, profile styles, secondary styles, traits and
  behaviours. Legacy Wushu/behaviour-as-style saves normalize safely. New universes deterministically
  give roughly 65% of fighters a distinct supported secondary discipline, displayed as a mixed style
  and contributing a bounded 35% action-selection influence. A new 3,840-bout action baseline freezes
  pre-expansion frequency and effectiveness; headline calibration remains exactly 60.39% finishes,
  16.48% KO and 19.04% TKO.
- Added a phased fight-engine moves, skills and combinations roadmap. It begins with the confirmed
  four-record unsupported-style repair and validator gap, then separates technique identity from
  style and behaviour before expanding boxing, kicks, clinch, wrestling, ground work, submissions,
  fighter signatures and tactical chains. Every phase retains the accepted 60.39% finish, 16.48% KO
  and 19.04% TKO calibration without changing competitive finish conversion.
- Repaired the shipped universe's source identity coverage. All 1,534 canonical MMA fighters now
  carry durable source IDs, all previously blank birthplace/hometown pairs have deterministic
  provenance-marked regional data, and the four broad-reach media packages now serve Russia,
  South Korea, the Middle East, and Africa. Legacy custom databases still receive stable IDs only
  in memory until explicitly saved, while Database Editor additions and duplicates mint fresh IDs.
- Added a narrative-system audit and phased development plan covering rivalries, title chases,
  career journeys, injuries, academies, contracts, promotion histories, relationships, and combat
  sports. The plan makes calendar speed a release constraint: narrative updates must be event-driven,
  indexed, bounded, transaction-safe, RNG-isolated, and verified against long-career performance
  budgets rather than adding a new whole-world weekly scan.
- Implemented the first narrative-system phase. Rivalries, title chases, championship reigns, title
  losses, and redemption runs now advance persistent ID-safe story threads directly from existing
  simulation events; matchmaking and Fight Night explain why a connected bout matters, while a new
  Storylines browser separates active and resolved chapters. Thread/beat limits, direct-key lookup,
  deduplication, legacy-save defaults, transaction rollback, and presentation-RNG isolation prevent
  narrative history from becoming a second simulation cost. Champion-retention title losses no
  longer receive success rewards, declined veteran runs cannot be penalized twice, and homegrown
  title projects receive at most one backed extension.
- Extended persistent narratives through serious-injury treatment, recovery clearance, return-fight
  payoff and medical retirement; contracted comeback/farewell commitments; academy graduate to
  homegrown champion lineage; and contract-promise fulfilment or breach. Fighter Profiles now read
  connected chapters from the direct participant index. Added a deterministic 12-week A/B calendar
  regression that requires identical gameplay and shared-RNG outcomes while enforcing the narrative
  overhead budget. Story status counts are now maintained incrementally, so routine writes remain
  constant-time and full bounded pruning occurs only when a hard active/resolved limit is exceeded.
- Expanded narrative memory across weight-cut turnarounds and division reinvention, camp-fit reviews
  and gym moves, ID-backed friendship/stablemate bouts, AI promotion strategy and ownership eras,
  other-sport contender/title/retirement careers, MMA transitions, and crossover-superfight results.
  Fighter Profiles now assemble a unified bounded Career Timeline lazily from direct story and
  fighter-local indexes. End-of-year awards select and archive at most eight defining current-year
  threads instead of reconstructing historical results. Friend relationships now persist a stable
  fighter ID and only migrate legacy names when one unambiguous loaded-world match exists.
- Added academy mentorship through graduation, fit/attention-driven gym splits, hometown-hero market
  milestones, talent-war signings, and bounded story-aware AI matchmaking. AI story value is cached
  only for already-credible candidate pairs and cannot bypass readiness, division, ranking, title,
  or stale-rematch rules. Replaced the noisy one-shot performance gate with three alternating paired
  samples; repeated 12-week aggregate runs remain inside the no-slowdown budget, with the current
  full-suite measurement at +2.69%. Added a synthetic 25-/50-/100-year
  storage regression that kept indexed update time flat and the current century payload below 800 KB.
  Academy graduation rollback now restores story threads and rebuilds their transient indexes when
  a late graduation hook fails.
- Expanded narrative scope with scored Promotion Rivalry chapters. Contested contract signings,
  academy recruitment and sanctioned superfights now build a deduplicated two-year series between
  known companies, with decisive and cycle-end resolutions but no recurring promotion scan.
  Significant fighters also retain room-specific coaching-loyalty chapters through arrivals,
  breakthroughs, title wins, setbacks and gym splits. Academy mentorship remains active after
  graduation through a persisted prospect ID and can resolve with the graduate's senior title win.
  The current canonical post-expansion paired calendar gate measured +2.69% overhead across 12 aggregate
  weeks, inside the 5% budget; its disabled arm exits before the new story-key work so the
  comparison includes the real helper cost.
- Expanded Hometown Hero stories beyond popularity thresholds. Main-event and title bookings in an
  established fighter's hometown or birth market now announce a homecoming, expose its stakes in
  fight context, and settle as a triumph, setback, heartbreak, unresolved result, or home-title
  payoff. Player and AI paths emit these beats from existing booking and regional-settlement events;
  the post-homecoming performance gate measured -2.28% overhead and added no calendar scan.
- Added persistent Contract Sagas connecting renewal pressure, stalled or broken talks, expiry,
  release, rival-company signings, returns and cross-company revenge fights. A fighter carries one
  direct optional thread key, duplicate names remain ID-safe, and legacy/current saves default and
  round-trip correctly. Contract helpers exit before story-key lookup when narratives are disabled,
  and the narrative plan now treats calendar-speed limits as an explicit release-blocking gate. The
  current deterministic paired test measured +0.68% median overhead with identical world/RNG state.
- Added ID-safe Staff Tenure stories connecting appointment, renewal warnings, stalled talks,
  renewal, release and natural expiry. Staff Profiles expose the current chapter and Storylines use
  the saved staff identity, while legacy staff default safely to no active key. Hooks reuse existing
  staff actions and the existing warning loop rather than adding calendar work; the post-change
  canonical paired test preserved identical world/RNG state.
- Expanded Staff Tenure into performance-backed staff careers. First-of-kind scouting dossiers,
  medical clearances, event matchmaking results, marketing outcomes, broadcast breakthroughs and
  fulfilled fighter promises now create bounded achievements or setbacks for the responsible staff
  lead. Profiles and Storylines expose a 0–100 legacy score and distinct milestone count; duplicate
  outcomes cannot farm beats or legacy. The post-expansion paired gate measured +0.49% CPU overhead
  with identical world/RNG state. Both arms now collect deferred startup garbage before timing so
  unrelated load cleanup cannot create a false calendar regression.
- Hardened the narrative performance gate after a Windows scheduling pause produced contradictory
  wall-clock results on unchanged code. The same alternating paired A/B now gates on process CPU
  time—the simulation work controlled by the game—while still reporting wall-clock medians for
  diagnosis; the existing 5% proportional ceiling and world/RNG equivalence checks remain unchanged.
- Added persistent Feeder Pathway careers for player-funded child promotions. Development loans and
  paid transfers now follow the same fighter through child main/title results, recall or promotion,
  parent debut, featured breakthrough, parent championship, departure, or retirement. Matchmaking,
  Fighter Profiles, Storylines, and the child operations screen expose the connected chapter by
  durable fighter ID. Rejected moves create no story, late hook failures restore rosters, belts,
  cash, ledgers, Chronicle, story indexes, and RNG, and ordinary fight settlement exits after two
  direct key checks without scanning rosters, alumni, Results, or history. Three post-change paired
  calendar runs retained identical gameplay/RNG state and ranged from −1.94% to +2.06% total
  narrative CPU variance, showing no repeatable slowdown and remaining inside the noise-tolerant
  rejection gate. The full 30-suite isolated regression run passes.
- Turned the one-shot Giant Slayer achievement into a persistent Breakout Run. An eight-point
  underdog win now carries prove-it pressure into draws, follow-up wins, setbacks, main events,
  title tests, championship arrivals, retirement, or a stalled run. The same ID-safe chapter appears
  in matchup context, Fighter Profile timelines, Storylines, annual review, Chronicle, and bounded AI
  matchmaking intent. The result hook reuses the achievement's already-computed upset flag, performs
  only direct-key lookups, consumes no simulation RNG, and added no calendar scan. Focused and clean
  canonical paired runs measured +0.64% and −1.61% CPU variance with identical gameplay/RNG state,
  showing no repeatable slowdown; all 30 isolated suites pass.
- Added ID-safe Career Crossroads for established fighters after a third consecutive simulated MMA
  loss. The chapter carries decline pressure into draws, deeper losing runs, division reinvention,
  recovery or championship wins, roster release, contract exit, and final-fight retirement; its
  current stakes appear in matchmaking, Fight Night, Fighter Profiles, Storylines, Chronicle,
  annual review, and bounded AI booking intent. Player, AI, and regional result hooks reuse the
  bout record already being settled, while inactive paths use direct empty-key checks and a
  four-result bounded origin lookup rather than a calendar or history scan. Current/legacy saves,
  duplicate callbacks and names, event rollback, and presentation RNG are covered. Three clean paired
  calendar runs ranged from -19.04% to +2.66% CPU variance with identical gameplay/RNG state, showing
  no repeatable slowdown and passing the noise-tolerant release gate; the complete isolated regression
  runner also passes.
- Expanded Fighter Relationship stories beyond a one-fight headline. Friends and featured
  stablemates now carry the first result into an unresolved draw, a respectful series conclusion,
  rivalry-driven fallout, a three-fight verdict, or a former-stablemate rematch after changing camps.
  Matchmaking, Fight Night, Fighter Profiles, Storylines, Chronicle, annual review, and bounded AI
  intent read the same pair-ID thread. Active keys are stored only on the involved fighters, capped
  at four, so unrelated bouts return before constructing a pair key or querying the story index;
  draws and regional cards now use the same relationship interpreter. Duplicate callbacks/names,
  legacy/current saves, thread normalization, RNG purity, and event rollback are covered. A rejected
  unconditional-lookup version failed the performance gate and was replaced; three optimized clean
  calendar runs ranged from -3.70% to +1.88% CPU variance with identical gameplay/RNG state, showing
  no repeatable slowdown. The complete isolated regression runner also passes.
- Turned retirement decisions into persistent Career Farewell chapters. Final-fight stakes now
  surface in matchmaking and Fight Night; automated retirement shows prefer an already-eligible
  career rival, friend, former stablemate, significant ID-backed former opponent, champion, or fellow
  veteran without expanding the candidate pool. The result retains the final opponent, relationship,
  outcome, Chronicle payoff, and both fighter IDs across MMA, AI, regional, draw, no-contest, and
  other-sport paths. Current and legacy saves, duplicate callbacks and names, normalization, RNG
  purity, and player-event rollback are covered. Three clean paired calendar runs ranged from -1.96%
  to +4.44% CPU variance, and the canonical run measured +3.10%, with identical gameplay/RNG state,
  no new recurring simulation pass, and the complete isolated regression runner passing.
- Added the fight-engine preservation foundation for the staged realism roadmap. A UI-free audit
  harness now freezes 3,840 fixed-seed bouts across competitive low/mid/high tiers, mismatches,
  styles, behaviours and bout lengths; records per-exchange position, action, damage and stat
  evidence; publishes confidence intervals; classifies every detailed fighter attribute as a direct
  fight input or documented pre-fight input; and restores caller RNG and fighter state. The new
  dedicated regression suite locks the existing 59.64% total finish rate, 15.70% KO rate, 19.66%
  TKO rate and 23.46% combined submission-family rate before later engine phases begin. MMA fights
  now also retain a structured `FightResult` containing per-exchange position, action, gas, damage,
  strike, takedown, submission and knockdown deltas plus scorecards, metrics and a terminal official
  result; the legacy result tuple remains unchanged. Presentation now uses a bout-local random
  stream, mechanically meaningful fouls are separated from atmospheric flavour, and changing the
  commentary phrase bank cannot alter a result. Combat, officiating and presentation now have
  explicit bout-local streams; exchange commentary renders through the recorded trace, whose events
  classify outcomes and retain hurt, cut, knockdown, position, control and stoppage evidence. The
  post-separation 3,840-bout audit remains at 59.79% finishes, 16.25% KO and 18.98% TKO without
  changing competitive finish conversion.
- Rebuilt MMA judging around round-local trace evidence. Judges now apply effective striking and
  grappling first, effective aggression only when offense is close, and control only when both are
  close; gas, home, experience, pressure and discipline no longer score by themselves. Clear
  dominance cannot be overturned by ordinary variance, evidence-backed 10-8 and rare 10-10 rounds
  are supported, and cards expose unanimous/split/majority decision and draw verdicts while keeping
  canonical save-compatible methods. Repeated fouls can produce a visible round-specific deduction.
  The post-judging corpus remains at 59.32% finishes, 16.25% KO and 19.24% TKO.
- Separated transient hurt from lasting head, body and leg trauma. Defensive success, survival and
  corner work can now settle hurt and restore bounded gas without healing accumulated damage. Body
  damage taxes sustained output, leg damage impairs kicks/shots/movement, and head trauma degrades
  reactions. Cuts retain location, severity, bleeding, swelling and vision risk; doctor stoppages
  identify that evidence, and the same Fight Night metrics now extend medical layoffs and severe
  injury holds. The calibrated corpus passes at 60.39% finishes, 16.48% KO and 19.04% TKO.
- Added a 356-bout full-system fight-engine regression spanning every fight plan, major style
  matchups, 3/5/6/7-round rules, extreme ratings and duplicate-name fighters. It reconciles every
  exchange with the final box score, position path, stoppage, scorecards and judging evidence. The
  audit found and fixed knockdowns being classified and scored against the defending corner,
  No Contest audit rows retaining winner/loser IDs, and survival erasing reported head trauma. A
  separate permanent trauma channel fixes the evidence without changing calibrated mechanics: the
  3,840-bout audit remains exactly 60.39% finishes, 16.48% KO and 19.04% TKO.
- Added ID-safe pre-fight plans for both player and game-AI MMA bouts. Matchmaking and the booked-card
  editor offer Balanced, pressure, counter, wrestling, cage, body, leg, submission, conservation,
  lead-protection and finish-chasing instructions. Plans alter action mix, targets and energy rather
  than overriding results; defended attacks create real counter windows, and adaptable corners can
  change approach between rounds using only visible fight evidence. Existing scheduled cards load as
  legacy Balanced bouts. The frozen 3,840-fight audit remains unchanged at 60.39% finishes, 16.48%
  KO and 19.04% TKO, without changing competitive finish conversion.
- Upgraded each MMA trace beat into a coherent exchange containing its setup, attack, explicit
  defensive response, counter opportunity or consumption, and follow-up. Failed attacks can now be
  audited as the source of a real counter window; fixed-seed samples confirm counter plans produce
  more successful counters than comparable pressure plans. Strike combinations retain each ordered
  attempted/landed component with a hard per-exchange bound. These additions consume no extra combat
  randomness, and the locked corpus remains at 60.39% finishes, 16.48% KO and 19.04% TKO.
- Added a validated MMA positional state machine. Failed shots, front headlocks, turtle, standing
  back control and leg entanglements now have legal ownership and escape routes alongside cage
  takedowns, re-shots, mat returns, wall get-ups and submission-defense consequences. Brief scramble
  positions are retained in the exchange path without pretending they were consolidated into the
  following beat. Impossible ownership and illegal transitions fail immediately in focused tests;
  wrestler, submission-specialist and striker samples follow distinct paths. The full audit remains
  unchanged at 60.39% finishes, 16.48% KO and 19.04% TKO.
- Completed the referee and foul-consequence pass without recalibrating finishes. Explicit referee
  profiles preserve the existing stoppage and stand-up values; foul records now include warnings,
  recovery time, intent and the responsible official, with a three-incident cap and scorecard
  deductions. Severe accidental fouls can produce a rules-correct No Contest or technical decision,
  and No Contests leave records, Elo and titles untouched across player, AI, academy and independent
  cards. Visible trauma also drives bounded medical, confidence and recurrence consequences, while
  early/late stoppage review flags never rewrite the official result.
- Finished the fight-engine cleanup and tuning migration. Removed two unused finish helpers, added
  versioned and validated fight-setting defaults, and moved Gate Multiplier into a separate
  versioned business setting with automatic old-save migration. Simulation Lab now identifies which
  controls affect mechanics and which affect business only. The accepted 3,840-bout calibration
  remains exactly 60.39% finishes, 16.48% KO and 19.04% TKO.

This development cycle expands the playable Combat Sports and Fighting Academy management loops,
turns Scouting into a persistent evidence-led department, hardens fighter identity and Fight Night,
and adds card-specific event economics backed by a broader release regression suite.

### Finance and Company Growth

- Expanded Finance into a long-career operating dashboard. A new History & Outlook page retains up
  to 30 annual revenue/cost/profit summaries, breaks the last 12 months of event revenue into ticket,
  broadcast, sponsor and merchandise streams, records monthly roster size and purse exposure, and
  projects milestone cash/event timing without pretending that popularity, stability or safety
  growth is predictable. Compact annual and roster snapshots remain useful after detailed weekly
  transaction retention rolls forward.
- Added eight persistent late-game strategic investments across facilities, international operations,
  staff departments and prestige projects. Each has an explicit milestone/popularity/cash gate,
  capital price, monthly upkeep and bounded event effect covering medical cost, attendance,
  broadcasting, sponsorship or merchandise. Purchases and upkeep use canonical finance transactions,
  existing saves default safely to no projects, and Finance exposes total first-year commitment and
  current monthly strategic burn before approval.

### Combat Sports

- Reworked Boxing and Muay Thai as distinct rulesets instead of presentation variants of one
  striking calculation. Boxing now scales non-title contests through six, eight, and ten rounds,
  uses 12-round title fights, records knockdown-led 10-8/10-7 rounds, and returns three official
  cards with unanimous, split, majority, and draw verdicts. Standard Muay Thai runs three rounds,
  title bouts run five, and its judges prioritize effective kicks, knees, elbows, dumps, balance,
  and clinch control; Lethwei remains a five-round knockout-first ruleset. Replays retain official
  cards and round evidence, while decision variants no longer inflate finish statistics.
- Completed the ownership-and-economics foundation for Boxing, Kickboxing, Muay Thai/Lethwei,
  Wrestling, and BJJ child promotions. Persistent player rosters and booked corners now repair to
  fighter IDs, including same-name athletes; flagship prospects use the same negotiated term,
  purse, exclusivity, and finance workflow as private-market recruits. Contracted purses are paid
  during card settlement, expired deals receive one renewal window before the athlete returns to
  the flagship, and expired athletes cannot be booked. Player cards now keep their own numbering
  and history, are limited to one per month, and write complete, collision-resistant finance
  metadata without incrementing the AI circuit. Added a focused Combat Sports regression suite.
- Fixed remaining Combat Sports identity leaks. Championship ownership, defenses, vacancies,
  lineage, season statistics, records, awards, Hall of Fame entries, card rotation, and world-roster
  repair now distinguish same-name athletes by `fighter_id`. Fight-night readiness commentary reads
  the ID-keyed telemetry instead of displaying false zeroes, and one-fight independent opponents no
  longer pollute permanent circuit records or win annual awards.
- Expanded player-owned Combat Sports into a future-event management loop. Matchmakers can now
  schedule a built card for a future month/week, choose Local, Regional, or Arena production,
  set a marketing budget, and see the same revenue/cost/profit forecast used at settlement.
  Scheduled cards persist across saves, reserve their athletes from smart cards, execute when the
  calendar enters their week, produce a completion Inbox item and replay, and can be cancelled from
  the new Upcoming Scheduled Cards panel. Contract, medical, duplicate-date, and one-card-per-month
  checks prevent invalid or overlapping shows.

### Fighting Academy

- Rebuilt the Fighting Academy into a long-term youth-development system. Prospects can follow
  measurable 4/8/12-week blocks with objectives and end-of-block reports; compete from local through
  international level with opponent quality, strength of schedule, two-bout tournaments and amateur
  titles; and develop persistent personalities, satisfaction, loyalty, promises and departure risk.
  Graduation now supports the main roster, a 12-month MMA developmental deal, player combat-sport
  divisions, or a regional feeder with retained matching rights that can be exercised from Alumni.
  Major rival promotions operate persistent regional youth programmes, graduate their own cohorts
  and compete for unsigned leads.
  The Academy page exposes the complete workflow plus a Rival Academies view. Null-shaped old saves
  repair safely, every identity-bearing ledger uses durable IDs, the live four-week card clock is
  accurate, and training fatigue remains consequential. Tournament entry, graduation and matching-
  right signing now roll back cash, finance, roster, belt, prospect and RNG state after a late
  failure; malformed schema-six prospect values are normalized, and rival development can no longer
  reduce a prospect's stored rating. Stability coverage exercises every phase and these failure paths.

### Fighter Profiles and Scouting

- Hardened fighter Profiles as observational, identity-safe views. Unscouted portraits no longer
  print exact ratings or private style details, duplicate-name fighters retain the correct employer,
  championship, opponent and W/L history, and distinct same-labelled archives remain searchable.
  Reopening a Profile now preserves its active window, laptop-sized displays keep the fixed action
  footer reachable, and child-sport Profile queries no longer initialize titles or rewrite rankings.
  Spectator Mode and stale Profile windows cannot sign, transfer, or move fighters after ownership
  changes; contract submission revalidates the live owner, and expired scouting reports stay hidden
  when negotiation opens. New championship history records retain durable fighter IDs.
- Made championship administration and belt history ID-first. Administrative belt awards,
  vacancies, previous-champion references, and history rows now retain `fighter_id` alongside
  display names so a same-name athlete cannot inherit another fighter's reign or Profile lineage.
- Hardened scouting across saves, staff assignments, report upgrades, and the recruitment UI.
  Ambiguous legacy name-keyed reports no longer leak intelligence between same-name fighters, and
  legacy migration no longer consumes simulation RNG or refreshes old dossiers as current. Scouts
  and their workloads use durable IDs, null-shaped old-save collections repair safely, and replacing
  a report preserves the last completed intelligence through cancellation or an expired observation.
  Talent searches can rediscover unusable reports, retain their ranked lead list, and open the
  headline fighter. The Scouting screen now reflows its controls at laptop widths, scrolls assignment
  history in both directions, shows real completion dates, and exposes cancelled/expired filters.
- Deepened scouting into an evidence-led recruitment department. Pending assignments no longer leak
  conclusions derived from hidden ratings, automatic dossiers are limited to one discounted paid
  assignment per week, and talent searches rank existing fighters without manufacturing new market
  supply. Searches now accept age and style constraints plus an ability, potential, value,
  marketability, or roster-need priority. Standard, faster paid Priority, and quarterly Ongoing
  briefs create explicit capacity choices, while thin pools return labelled Near Matches;
  completed work builds regional knowledge, durable dossier history, and deduplicated watchlist
  alerts. Live observations retain opponent, result and fight metrics, prior intelligence remains
  visible during upgrades, ageing reports lose certainty after six months, and exact current ratings
  require a high-confidence repeated full evaluation. Established academy networks now close when
  their assigned scout's contract expires instead of extending that contract forever.

### Fight Night and Simulation

- Hardened the watched Fight Night experience end to end. A live card is now a single guarded
  session, so reopening Watch cannot rerun press conferences or weigh-ins, active bouts cannot be
  skipped accidentally through the next-fight control, full-card skips require confirmation, and a
  failed transactional settlement remains retryable. Exact judge cards stay sealed until the
  result while public round telemetry continues to update. Duplicate-name corners retain distinct
  metrics, winner identity, projected records, and replay labels. Archived replays preserve their
  original location and event economics, while compact live layouts and the scrollable end-event
  report keep controls reachable on laptop displays.
- Fixed fight-state identity and response tracking: clinch controllers now retain their private
  per-bout slot, only landed strikes build unanswered-offense streaks, meaningful grappling and
  positional responses clear those streaks, and the horn resets them. Same-name tournament
  entrants now resolve and restore fatigue by identity, including alternate replacements, while
  ambiguous legacy friend names no longer alter fight-night context.

### Persistence and Save Safety

- Rebuilt Game & Saves as a clearer Career Library with an active-career banner, visible library
  counts, a scrollable save browser, selected-save metadata, grouped actions, and selection-aware
  controls that stay disabled until a valid career or snapshot is chosen.
- Hardened save operations against partial I/O failures: Quick Save and slot-load now continue when
  optional backup creation fails, while backup restore validates the source before touching the
  destination and reports backup/write failures without escaping the UI callback. Non-object JSON
  saves are now rejected cleanly before split-save hydration.
- Hardened save-folder housekeeping by skipping symlinked entries during external block pruning,
  preventing accidental deletion outside the intended save slot tree.

### Event Economics and Rivalries

- Made every card an economic decision. Ticket price was a single company-wide number nudged in $5
  steps, and it had no effect on turnout at all: raising it lifted the gate and cost nothing.
  Ticket price, production tier and marketing spend are now booked per event, and price drives
  attendance against what that specific card can carry. Gate revenue peaks a little above the
  market rate, so undercutting leaves money on the table and gouging empties seats and costs gate,
  atmosphere and merchandise. Marketing spend buys turnout with diminishing returns. The booking
  screen projects attendance, fill, gate and total spend live, names the market price, says whether
  you are priced about right, and warns when a card commits a dangerous share of your cash before a
  ticket is sold. Cards saved before this change, and every AI card, keep using the company-wide
  defaults.
- Tied production quality to who is actually watching. The four production tiers (Lean, Standard,
  Premium, Spectacle) trade staging cost against build, atmosphere and broadcast value, and the
  richer tiers only repay their cost behind real broadcast reach. With no coverage a lean show is
  correct; a regional webcast or streaming deal wants Standard; a cable deal justifies Premium; and
  Spectacle pays off once a wide-reach network is carrying the card. Production is now something to
  upgrade in step with your broadcast contract rather than a setting to leave alone.
- Priced untouched cards at the market rate. The company-wide default never scaled with the
  promotion, so a player who ignored the pricing lever drifted further from a sensible number the
  bigger they got, and could lose money on a card that should have been profitable. A card you have
  not priced yourself now follows the market rate automatically; editing the price takes manual
  control until the show is scheduled, and the next card starts on the market rate again.
- Made feuds bookable. A rivalry already fed hype quietly, but nothing surfaced it and nothing let
  the player build one, so the Media Desk and Matchmaking were effectively separate games.
  Selecting two rivals in Matchmaking now reports the grudge, its heat, its origin and whether a
  rematch is being demanded, and a booked grudge lifts both projected hype and the gate in
  proportion to heat, weighted down when the feud is buried on the prelims. Calling out an existing
  rival in the Media Desk escalates the feud instead of resetting it, and press conferences and
  press tours keep a live feud in the headlines, so a grudge can be built over several weeks and
  cashed in on fight night.
- Balanced the new event economics against the rest of the simulation before release. A demand floor
  meant gate revenue climbed for ever once it bound, making the maximum price the correct answer at
  up to nine times what a card could carry; demand now decays to zero and a badly priced show is
  allowed to flop. The market-price curve let drawing power compound with venue prestige, inflating
  a mature promotion's gate roughly sixfold against purses, overhead and production costs that do
  not grow at that rate; the curve is now shallow enough to keep late-career gates in proportion.
  Production tiers were cheap enough that the richest option was correct from the first televised
  card, so their costs were raised and their compounding build bonus reduced until each tier is the
  right answer somewhere on the broadcast ladder. Regression coverage pins the elasticity peak, the
  reach-dependent tier crossover, and the auto-pricing contract.
- Rebalanced player-company finances across career stages. Curated opening-roster fighters now begin
  on 55% founder-era contracts, making a credible first card affordable without discounting generated
  regional depth or changing existing saves. Monthly office costs now grow with company popularity,
  from the existing $12,000 regional base to meaningful national and global infrastructure, and a
  second or later player event in the same month suffers progressive audience, sponsor, and broadcast
  cannibalization while every signed purse and production cost remains fully payable. Finance forecasts
  expose the scaled office cost, focused regressions pin all three rules, and a reproducible early/mid/
  late audit lives under `analysis/`.
- Hardened the release candidate around those systems. Grudge matches and Media Desk escalation now
  resolve rivals by durable fighter ID so duplicate names cannot award hype or gate demand to the
  wrong bout; invalid or stale rival selections are rejected before cash or a limited media action
  is consumed. Scheduled-event economics retain safe defaults for older saves and round-trip their
  card-specific values. The focused regression is deterministic and part of the canonical isolated
  suite.

### Release and Verification

- Unified the release workflow: `Build Portable.bat` runs the shipping suite, validates the universe,
  and builds both the game and Database Editor, while the editor-only script remains an optional
  focused build. Build failure restores staged runtime data, stale staging data is cleared, and the
  portable check verifies both executables.
- Expanded the canonical isolated suite with focused Fighter Profile, Scouting, Combat Sports,
  Fight Night experience, and event-economics regressions. The release runner now exercises those
  systems alongside persistence, finance, UI data, media, child promotions, identity, audio,
  database validation/editor coverage, advancement notifications, popup lifecycle, and the
  multi-seed stability playtest.

## 3.0.9 - 2026-08-17

This release consolidates the child-promotion safeguards, persistence and identity hardening,
universe-data validation, release tooling, and UX/stability improvements completed since 3.0.8.

- Added `FEATURE_DEVELOPMENT_BACKLOG.md`, an evidence-backed roadmap separating completed systems
  from the next feature slices: child-promotion scheduling, executable super-events, finance
  reconciliation, decision-center UX, and 100-year simulation validation.
- Made player and AI/child promotion finance auditable: cash movements now use canonical transaction
  metadata, weekly closes reconcile ledger totals to actual cash with explicit repair entries, child
  promotion receipts/costs are recorded, and Finance surfaces show reconciliation status.

- Hardened MMA child-promotion ownership and interactions: empty launches roll back, ordinary
  takeovers are blocked, parent belts are vacated before champion loans, closed parent divisions
  reject transfers, paid transfers record parent-ledger fees with confirmation, and retired/orphaned
  loan metadata is repaired. The manager now guards duplicate windows, clears stale details, keeps
  identity labels consistent, and provides scrollable rosters.

- Added a deterministic 48-week child-promotion progression regression covering AI events, parent
  profit sharing, contracts, protected loans, callback safety, and save/load persistence.
- Fixed annual AI weight movement allowing custom-promotion fighters to enter divisions the
  promotion had deliberately closed; added a deterministic 48-week handoff regression.

- Fixed same-name fighters sharing live fight state. Scorecards, damage, stamina, control,
  knockdowns, and stat lines now use private per-bout identity slots, while names remain purely
  presentation text. Player event references resolve durable fighter IDs first and reject ambiguous
  legacy names instead of silently selecting or moving the wrong athlete.
- Made completed player events transactional: a late finance, awards, archive, media, or presentation
  failure restores the pre-event domain state and RNG rather than leaving a partially applied card.
- Hardened save loading: split-save data blocks must stay under the owning slot's generated
  `DataBlocks` folder, malformed model rows now report their exact location transactionally, and
  unknown forward-compatible model fields no longer crash a current build.
- Capped player result summaries and verbose event logs so long careers retain searchable results
  without unbounded save, startup, or Log-screen growth.
- Added a canonical isolated regression runner used by both the test and portable-build launchers.
  Every maintained suite, including child-promotion, identity/persistence, database-editor, and
  long-run stability coverage, now receives its own runtime-data directory.
- Fixed Database Editor Save As so a cancelled or failed validation/backup/write leaves the current
  database selection intact; added focused Save As regressions.
- Unified database-editor, runtime, and release-pack universe validation behind one side-effect-free
  schema check. Invalid scalar values now produce actionable diagnostics, and normal universe loads
  normalize legacy data in memory without modifying the selected source file.
- Made load staging inspect all nested runtime values, added profile-window reuse keyed by fighter ID,
  and made temporary fight caches plus fight-night audio cue locking safe on exception/concurrent paths.
- Model collection fields now use independent default factories while legacy `null` values remain accepted on load;
  optional career arcs retain their explicit inactive `None` state.
- Portable builds now verify the recorded offline CPython/PyInstaller toolchain instead of installing
  build dependencies during packaging; test failures identify their subsystem, deterministic seed, and data root.
- Migrated runtime `Toplevel` creators to the shared call-site/entity window registry, replacing stale
  duplicate popups and preserving fighter/entity identity in detail windows.
- Routine contract and broadcast notices from calendar advancement now become one Inbox/news summary;
  due-event decisions remain explicit modal blockers.

## 3.0.8 - 2026-08-02

This release reworks the scouting and recruitment loop, replaces one-off production providers with
negotiated broadcast contracts, rebuilds the Legacy Ledger, and moves Fighting Academy and Combat
Sports into the main navigation as normal management screens.

- Moved Fighting Academy and Combat Sports into the main navigation as normal management screens.
  Both previously opened their primary workspace in a separate window, so reaching them meant
  leaving the main application context and managing a floating window. Selecting either from the
  sidebar now switches the main screen in place, exactly like Roster, Contracts, or Finance, and the
  existing Finance, Scouting, and World entry points route to the same pages. Academy purchase moved
  from a popup to an inline empty state on the page, since a tab cannot raise a modal every time it
  is selected. Detail views are deliberately unchanged and still open as focused popups: academy
  prospect profiles and card replays, child-promotion management, and circuit records and history.
  Academy and Combat Sports state, saves, and simulation rules are untouched, and neither page is
  constructed until the player actually opens it.
- Fixed the Fighting Academy build page reporting a frozen cash balance. Unlike the popup it
  replaced, the panel stays on screen for as long as the player has no academy, so reading company
  cash once while building the page left it stuck at whatever the balance was when the page was
  first opened (commonly $0) and froze the Build Academy button in that state. The price check is
  now live, and when funds are short it names the shortfall instead of only the balance.
- Fixed the same page stretching to fill the whole scrollable screen, which pushed the price line
  and the Build Academy button far below the fold. It now keeps the compact proportions of the setup
  dialog it replaced.
- Changed Contracts ordering so fight-counted comeback deals no longer sit at the top of the table
  from the moment they are signed. A comeback contract now only pins to the top once it is down to
  three or fewer guaranteed bouts; longer commitments sort below the regular expiry-ordered roster.
- Added a "Closed divisions" toggle to Free Agents and Regional Prospects so talent in divisions you
  have closed can be shown or hidden. Both screens default to showing them, closed-division prospects
  are highlighted amber like the free-agent market already did, and Regional Prospects reports how
  many rows the toggle is hiding.
- Fixed scouts wasting their spare assignment slots. Auto-assignment only considered a scout with a
  completely empty workload, so a two- or three-slot scout holding a single player brief never used
  the rest of their capacity. Every free slot is now filled, which is where most of the lost scouting
  throughput was going: a 3-slot and a 2-slot scout now reach full workload in one week instead of
  idling four slots indefinitely.
- Rebuilt talent searches to return a shortlist instead of a single name. A search cost 2.4x-3.1x a
  basic report, occupied a scout for 2-5 weeks, and surfaced exactly one lead, so scouting the market
  broadly was never affordable. A search now returns 3-6 ranked leads (scaled by the scout's
  networking), each with its own dossier and signing verdict, at roughly a fifth of the previous cost
  per lead. The headline lead keeps the scout's best read; secondary names come in at lower
  confidence so a dedicated report is still worth buying on anyone you take seriously.
- Reworked the signing recommendation to judge roster fit by quality rather than headcount. Divisional
  need was measured purely as "fewer than ten fighters", so a 27-year-old 73 OVR was reported as not
  needed in a division of 27 journeymen. Recommendations now weigh where the fighter would slot into
  your existing division, reward an upgrade on your current top five, and state the projected
  divisional rank in the reason text.
- Stopped short-term problems vetoing good signings outright. Being injured or on a losing streak sat
  in the same hard-blocking list as a closed division, so any injury made a 90 OVR free agent an
  automatic PASS. Injuries and skids are now priced into the score as scaled concerns; only a closed
  division or pending retirement blocks a deal outright.
- Added plain-English descriptors for every scout verdict (RECOMMEND SIGNING, MONITOR, PASS, PENDING)
  and every recommendation logic mode (Balanced, Aggressive, Strict, Prospect Focus, Value Focus,
  Roster Need). Verdict meanings now appear in the Scouting legend, the Logic selector tooltip,
  completed-report inbox mail, and the Free Agents scouting panel, which also shows the same formal
  verdict the Scouting centre uses instead of an unrelated second opinion.
- Suppressed scouting dialogs during automatic assignment so a full scout or a cash shortfall can no
  longer interrupt week processing with a popup.
- Rebuilt the Legacy Ledger. The all-time fighter list was a fixed-width text dump capped at 60 names
  with a single sport filter. It is now a sortable table of up to 400 careers with Sport, Status,
  Gender, Division, and name-search filters; columns for division, status, last company, win rate,
  peak ability, titles, defences and awards; Hall-of-Fame and retired rows colour-coded; double-click
  to open a full profile; and a breakdown line itemising exactly which career achievements produced
  the selected fighter's legacy score. Company Eras became a sortable table of legacy, reputation,
  show count and era count, with the full era history shown for the selected company instead of only
  the most recent three.
- Added a motivation adjustment to comeback negotiations. A retired fighter who wants to compete
  again now shaves up to 12% off their asking price, while one who has to be talked into it adds up
  to 6%, applied to the comeback premium as well. The curve is neutral at 65 motivation and
  interpolates between set anchor points: 0 = -6%, 30 = -4%, 50 = -2%, 65 = 0, 75 = +4.5%,
  85 = +7.5%, 100 = +12%. The negotiation window states the fighter's motivation and the exact
  adjustment.
- Replaced one-off production-provider purchases with real broadcast contracts. Adding a provider was
  a permanent unlock from a fixed list of three with no requirements and no expiry. Networks are now
  a six-tier ladder from Regional Webcast to Apex Worldwide, each gated behind a company-popularity
  requirement that must be met before talks open, signed for a fixed term of 12-24 months against a
  rights fee. Contracts count down monthly, warn in the inbox at two months remaining, and expire
  with a dialog prompting renewal; events still assigned to a lapsed network fall back to no coverage
  rather than being charged for a partner that no longer carries them. Company Editor lists remaining
  contract months per network plus the popularity needed to unlock the tiers still locked, and the
  booking screen shows the selected network's remaining term. Saves written before this change keep
  their providers and receive a full term on load rather than expiring immediately.

## 3.0.7 - 2026-08-02

This release carries the complete 3.0.6 Matt-Dev and Brett-Dev feature merge, including child
promotions, staff contracts and effects, academy progression, Dark Mode, sortable tables, finance
and save integrity fixes, extended Fight Night commentary, and the packaged Database Editor.

- Replaced exposed duplicate-source suffixes such as `BAMMA`, `FA`, and `Legend` with a compact
  `(D)` marker after the fighter's display name across player-facing screens while preserving raw
  names for save compatibility and identity resolution.
- Made champion, interim-champion, and duplicate markers idempotent across live Fight Night
  commentary, scoreboards, archived replays, official results, profiles, ledgers, news, inboxes,
  and matchup text. Existing output such as `(C) (C)` now renders with one champion marker.
- Included the spectator calendar crash fix, all merged child-promotion, staff, academy, finance,
  sorting, theme, save, stability, and five-round presentation fixes from the 3.0.6 release line.
- Rebuilt and validated both portable executables: `MMA Warriors.exe` and
  `MMA Warriors Database Editor.exe`.

## 3.0.6 - 2026-08-01

- Fixed a packaged calendar-advance crash in AI roster cuts and upgrade reviews. Scheduled fighters are now resolved through the shared durable-ID/legacy-name reference set before either review considers releasing or replacing them.
- Expanded staff into a real employment system: every staff role now has a documented gameplay effect, staff quality and morale influence those effects, and the Staff screen supports hiring, firing, severance, contract offers, renewal negotiations, and an Expiring Contracts tab. Legacy saves receive safe staff contract defaults.
- Connected Broadcast Producers, Drug Testing Officers, Doctors, and the existing scouting, marketing, matchmaking, and talent-relations roles to visible simulation outcomes including broadcast reach, testing cost and accuracy, medical costs, recovery, card building, scouting, promotion, and fighter negotiations.
- Balanced staff economics after audit: Talent Relations now gives quality-scaled cash leverage in fighter and transfer talks, compliance staff reduce manual testing costs as well as event costs, and Doctor recovery benefits scale across specialist quality instead of using a single threshold.
- Expanded Fighting Academy engagement with recurring development challenges. Players can choose specialist camps, pressure-test prospects in amateur bouts, or align coaching plans; decisions affect cost, fatigue, confidence, development, amateur experience, reputation, and the saved academy story history.
- Fixed secondary and popup data tables whose headings did not sort. Every `Treeview` now receives the shared numeric, date, record, and text sorting behavior, including Overall and other rating columns.
- Added an explicit Dark Mode theme as the default presentation, with neutral dark surfaces and readable gold/red accents. Save, database, and region list controls now start in the selected palette instead of flashing light gray.
- Fixed AI promotion takeovers carrying stale expired contracts into the player Contracts screen; inherited zero-month deals are now renewed for 12-24 months so the takeover does not mass-release the promotion's roster.
- Updated `Build Portable.bat` so every portable build validates and includes `MMA Warriors Database Editor.exe` alongside the game.
- Expanded the MMA Child Promotions manager with promotion health columns, AI operating mode, roster search, gender/division/status filters, contract and potential columns, fighter detail readouts, and double-click profile access.
- Fixed child-management ownership filtering, expired child-fighter transfers, parent profit-share ledger entries, and status filters hiding all parent loan candidates.
- Audited the opening fighter identities: all 4,850 live fighter objects have unique `fighter_id` values, while the 56 repeated base-name groups are intentional promotion/legend/free-agent career snapshots. Synthetic `Legend`, `FA`, and `BAMMA` suffixes are now hidden from roster, market, contract, scouting, profile, child-promotion, and fight-replay displays; raw names remain available internally for save compatibility and identity resolution.
- Fixed live five-round title-fight presentation trimming too much round-summary text. The watcher still keeps official judge cards sealed until the result, but each round now retains its unofficial cumulative score and gas telemetry, and a regression advances a title fight through all five rounds to verify summaries, scorecards, metrics, and the result remain visible.
- Changed Fight Night playback to full commentary mode. The engine no longer deduplicates repeated calls or inserts middle-exchange omission markers, and the live watcher renders the original round-summary lines instead of replacing them with condensed broadcast summaries. Official scorecards still reveal only at the result.
- Verified player-configured extended title fights: seven-round title/main-event cards now retain every round introduction, transition, summary, scorecard, metric, and final result in the live watcher.
- Rebuilt the `Matt-Dev` portable package with the full-commentary watcher and included `MMA Warriors Database Editor.exe`; `Portable Check.bat` passed while preserving packaged saves, databases, and logs.

### MMA AI Child Promotions

- Added player-funded MMA child promotions. The player chooses a large startup budget, a roster-building identity such as Youth Prospects or Big Names, and a 0-100% share of future child-promotion profits returned to the parent company.
- The child opens with a real MMA roster signed from the free-agent market, then runs through the existing AI promotion engine for matchmaking, cards, contracts, development, finance, and long-term strategy.
- Added a Companies-screen manager for launch, strategy and profit-share changes, parent-to-child fighter loans, recalls, and parent transfers of AI-signed child fighters. Loaned fighters remain parent-owned and cannot be released, replaced, contract-expired, retired, or removed by a child promotion's normal AI roster reviews.
- Added save-compatible child metadata, protected-loan repair, profit-distribution tracking, and smoke coverage for launch, finance, loans, recalls, and round-trip persistence.
### Brett-Dev Merge Notes

### Save, Load & Results Integrity

- Made career loads transactional: a read, migration, or apply failure restores the complete live
  career instead of leaving a partially replaced hybrid state.
- Made Results Database migration idempotent. Repeated Quick Loads no longer manufacture `|2`,
  `|3`, and later copies of the same completed card, while genuinely distinct same-week cards keep
  separate entries. Existing duplicated index rows are repaired on load.
- Made world serialization observational by moving champion/division repair out of the save writer;
  saving no longer adds fighters or consumes simulation RNG.
- A metadata-sidecar write problem is now logged as a recoverable cache warning after the primary
  save succeeds, rather than incorrectly reporting that the whole save failed.

### Contracts, Events & Financial Pressure

- Rejected negative or out-of-range contract money, percentage, PPV, and guarantee inputs before
  they can change cash or roster state. Terms remain normalized to the supported 1-60 month range.
- Player promotions now pay the full purse they negotiated. The hidden company-size discount was
  removed, and win, finish, PPV, and guarantee obligations use the same projection, payout, event
  report, and finance-ledger calculation.
- Guaranteed fights now advance on every official player bout, including draws, and unfinished
  commitments remain visible. Expired contracts leave or use the explicit paid auto-renew workflow;
  valuable fighters no longer silently receive a free random extension.
- Outside non-exclusive draws now use the shared draw-result path instead of being recorded as a
  loss. Event booking and conflict checks retain fighter IDs so duplicate display names remain
  independently bookable.
- Negative company cash now creates escalating, persistent financial-pressure consequences through
  stability, morale, and inbox warnings, and clears the streak after recovery.

### Scouting, World Data & UI

- Fixed Basic Dossier crashing after charging the player when Staff had not been opened. Lazy screen
  refreshes now tolerate unbuilt widgets, and successful scouting state stays synchronized.
- Added a player setting for automatic idle-scout assignments and clearer assignment messaging;
  academy guidance now recognizes an already-hired scout.
- Restored the Regions screen to the active dark palette and clears stale Results detail when a
  filter changes the visible card set.
- Corrected the Eurasian Fight Circuit's World Hub level label, repaired geographically incorrect
  regional team lists, and made opening gym membership/capacity balance prevent elite rooms from
  starting at several times their intended load.

### Balance, Tests & Portability

- Reworked the Simulation Lab audit around competitive low/mid/high-band matchups with overall gaps
  of six or less, per-tier finish rates, and restored RNG/name state. Its lightweight business
  stress figures are explicitly labelled synthetic rather than real player-event finance.
- Added focused regression suites for persistence, contracts/finance, scouting/world data, audit
  isolation, and portable batch-file path rules.
- Removed developer-specific `C:\Users\...` interpreter paths from the launcher, shipping tests,
  and build scripts. They now prefer the repository `.venv` and fall back to `py` or `python`.


### Fight Excitement

- Fixed AI and regional cards scoring about fourteen points more exciting than the player's for no gameplay reason. Those paths passed raw combined popularity as promotional heat while player cards passed the real hype value, so the same fight scored 66 on an AI show and 53 on yours. Every caller now uses one scale, and the gap measures zero.
- Rebalanced the metric so the fight decides the score rather than the poster. Promotional heat was roughly three-quarters of the number: who was fighting moved it more than what they did, which sent Fight of the Night to the biggest names instead of the best fight. Heat now contributes about a fifth, and the bout itself drives 61% of the variation, up from 43%.
- Fixed a finish scoring worse than going the distance. Later rounds paid up to fifteen points while a knockout or submission paid twelve, so a five-round decision outscored a first-round knockout. Finishes now score eighteen and beat decisions by fourteen points on average.
- Removed round number as a driver of excitement entirely. A fight is exciting or it is not, and when it ends does not change that.
- The score now reads what actually happened in the cage instead of inferring it. Knockdowns, submission attempts, significant strikes landed per round, and how two-way the exchanges were all feed the number, using stats the fight engine already produced but nothing consumed. A back-and-forth war and a one-sided shutout no longer score the same.
- Halved the even-matchup bonus, which sat at 15 or 16 of a possible 16 on almost every bout and acted as a flat offset rather than telling good fights from bad ones.
- Recalibrated every threshold that reads excitement to the range real cards actually produce. Event grades of A and B required 78 and 64, above the highest score a card average could ever reach, so no show in the game could earn either; meanwhile the median card graded F and failed the owner-goal excitement check outright. Grades now run A at 53, B at 49, C at 44 and D at 40, producing a normal spread of 7% A, 21% B, 47% C, 21% D and 5% F.
- The owner-goal excitement gate now passes 82% of cards instead of either all or almost none, so it rewards a good show without being a formality.
- Retuned the fanbase reaction to the top and bottom fifth of card averages, and the technical-crowd bonus to a level a card can actually reach; both previously sat above the maximum and never triggered.
- Separated the per-fight bonus labels from the event-level bands. Individual bouts spread far wider than card averages, so Fight Night now flags roughly the top sixth of bouts as bonus contenders rather than reusing an event threshold no single fight was measured against.

### Build & Tests

- Fixed the portable build aborting before it reached PyInstaller. `Build Portable.bat` gates on the smoke test, which failed on every run with `AttributeError: 'FightEmpireApp' object has no attribute 'city_box'`: management screens build lazily when first opened, and the event-booking city check read a booking widget roughly two hundred lines before that screen was built. The test now opens the booking screen before reading from it, and repopulates its fighter table afterwards so later checks see the current roster.
- Made the Matchmaking click-selection check tolerate a display that cannot lay out the fighter table. It drove clicks through real pixel coordinates, which do not exist when the booking pane is never mapped on a headless or minimised build machine. The full assertions still run wherever Tk lays the table out; elsewhere the test reports the skip explicitly and still exercises the underlying selection logic, instead of failing a build for an environment limit.
- Raised the youth-market batch assertion to match the larger combat sport intake.

### Starting Database

- Brought every real fighter over 42 in the starting database down to their prime age, so a new world opens with recognisable names competing rather than retiring. Twenty-seven athletes were affected, including Bobby Lashley (50 to 30), Chael Sonnen (49 to 31), Vitor Belfort (49 to 27), Brock Lesnar (48 to 28) and Holly Holm (44 to 31). Each new age comes from the database's own convention of prime start plus three, so every fighter lands inside their authored prime window with ratings, records and profiles untouched.

### Combat Sports (Child Promotions)

- Opening a combat sport division now asks for confirmation first. Selecting a sport and pressing the manage button launched the promotion immediately and spent the $120,000 startup investment with no prompt; the dialog now states the cost, your cash after launch, that the promotion starts with no athletes, and that it cannot be undone. Insufficient funds are reported before anything is charged.
- Added a combat sport free-agent market, kept entirely separate from the MMA free-agent pool. Established athletes aged 22 to 33 arrive with real records, ratings and popularity, so a division can be staffed with competitors who are ready now rather than only teenagers.
- Fixed thin hiring markets. The signable pool targeted four athletes per division, capped additions at twelve per refresh, and only ever generated 18- and 19-year-olds — across roughly twenty divisions per sport that left many showing a single name or none. The youth target was raised and the free-agent pool added alongside it.
- Fixed a per-pass intake cap that left over half of Boxing's thirty-four gender and weight combinations short of four prospects however many times the market refreshed. Launching a division, or refreshing its market, now fills every division to its full target in a single pass instead of a fixed batch, while normal two-monthly replenishment stays paced. Every division of every combat sport now opens with six youth prospects and three established free agents per gender.
- Added contract terms for combat sport athletes. Signings previously set no contract length at all, so athletes could never expire, be renewed, or be negotiated with.
- Added negotiation to sport signings and renewals: a contract dialog shows the athlete's expected purse and term, lets you set both, and offers a Test Reaction check before sending. Athletes accept, hold out for closer terms, or reject outright based on their rating, popularity and age, with prospects favouring long deals and established names favouring short ones.
- Sport contracts now count down monthly and raise inbox warnings at three, two and one month, plus an expiry notice when a deal lapses.
- Added a Combat Sports tab to the Contracts screen listing every child-promotion athlete with sport, division, rating, time remaining and purse, using the same red/orange/yellow expiry highlighting as the MMA roster, with Negotiate Renewal and Release Athlete actions and a per-bout payroll total.

### Regional Titles & Lineage

- Fixed regional belts changing hands in ordinary development bouts. The championship flag was matched on the *division* rather than on the reserved contenders, so once a circuit booked a title fight every other bout in that division on the same card was flagged a championship too — and each one ran the crowning routine. The belt ended up on whoever won the last development bout of the night instead of the actual title contest, changed hands on cards where no title fight was scheduled, and left the real champion competing in bouts that were never recognised as defences. Championship status now matches the specific reserved pairing.
- Verified across 96 simulated feeder cards (390 bouts): divisions carrying more than one championship bout on a card went from roughly ten per month to zero, and reigning champions placed into non-title bouts went to zero.
- Added a TITLE LINEAGE panel to the fighter profile listing every championship event in a career — inaugural reigns, crownings, defences, and vacancies — across every promotion the fighter has competed for. The profile previously showed a single line and only for a *reigning* champion, so a vacated or lost title vanished from the fighter's record entirely; 156 vacated championships in the reference career were invisible.

### Title Fights & Booking Conflicts

- Fixed a scheduled title fight silently losing its champion, and with it the championship on the fighter's record. The double-booking repair that runs on every save load resolved clashes purely by date, so an ordinary undercard booking made a week earlier would win the fighter and the champion was quietly replaced with a TBA in their own title defence — leaving a belt advertised in a bout the champion never appeared in, an unrelated free agent signed into the empty corner on fight night, and the champion's record showing the other, non-title bout instead. Championship bouts now claim their fighters first regardless of date.
- A bout that still loses a fighter to a booking clash now has its championship sanction removed rather than continuing to advertise a belt nobody present can win, and the downgrade is announced in the news feed and the inbox.
- Interim status is now recomputed for title fights that survive a booking repair, instead of keeping whatever was true when the bout was first booked.
- Fixed any bout still holding a TBA slot skipping the weigh-in completely. Weigh-ins ran before the opponent was resolved, so neither corner was ever weighed: no scale weight was recorded, a missed weight could not be detected, and a blown cut could never downgrade a title bout to a catchweight non-title contest. A 240 lb fighter could contest a 155 lb championship without ever stepping on the scale. TBA opponents are now signed before the weigh-in, and the whole existing weight-miss, fine, and commission-removal pipeline applies to them normally.

### Fight Night

- Fixed the fight-log corner read always reporting an 8-week camp for both fighters. It read `red_camp` / `blue_camp` / `camp_weeks` from the booking dictionary, keys nothing has ever written, so every bout fell through to the hardcoded default and hid the real preparation — including short-notice replacements who had no camp at all. It now reads the fighter's actual camp length, matching the corner read already used elsewhere in the engine.

### Fighter Weight & Division Fit

**Root cause fix — walk weight was decided before the division was.**

- Fixed walk weight being derived from a randomly rolled division inside the fighter generator and then left stale when the caller overwrote the fighter's division. A fighter built as a heavyweight could be seeded straight into flyweight and keep a 295 lb frame, cutting 169 lb every camp and sitting permanently pinned at the maximum weigh-in penalty. This affected 13% of every world (43% of all regional-feeder fighters).
- Added a `weight` parameter to regional-feeder fighter creation so the division is known at build time instead of being patched on afterwards, and updated the initial feeder roster build and the monthly regional youth intake to pass it.
- Added `assign_fighter_division()` as the single supported way to change a fighter's division, so the frame and the class can no longer silently disagree.
- Added `plausible_walk_weight_band()`, derived from the same limit and spread constants as the fighter generator so the builder and the validator cannot drift apart again.
- Added `repair_walk_weight_for_division()`, which pulls an impossible frame back into its division's believable band and reports how far out it was.

**Save repair for existing careers.**

- Extended the division-frame migration to repair frames in both directions against each division's plausible band, rather than only the undersized tail, and bumped its version so careers created before this fix are repaired on load.
- The repair message now reports oversized and undersized counts separately along with the worst mismatch found.
- On the reference career this reset 861 frames (790 far too heavy for their class, 71 far too light; worst mismatch 148 lb) and refreshed division-fit ratings across the roster.

**Division moves.**

- Fixed the move-up path never checking whether a fighter could make the target weight. It measured frame fit only, so a 227 lb featherweight was waved into lightweight as an "undersized" move — he could not make 155 lb either, and moving up one class does not fix a frame that belongs three classes higher. Both directions now check cut feasibility.
- Fixed division-fit penalty messages blaming the wrong cause. The penalty is the sum of two independent terms — a frame that is light for the class, and a naturally small build — but the text hardcoded the walk-weight explanation, telling players a 227 lb lightweight's "walk weight is light for Lightweight" when every point came from his build. Messages now name the term that actually produced the penalty.
- Added `division_size_penalty_parts()` and `division_fit_reason()`; `division_size_penalty_for()` is now a thin wrapper over them so all callers stay consistent.
- A completed division move now carries the fighter's real frame across intact and only repairs it when it was never credible for either division.
- Made the closed-division roster reassignment generic to any promotion rather than one company, and made it move fighters to the nearest still-open division for their gender instead of a hardcoded Welterweight — dropping a heavyweight into welterweight moved them ninety pounds and left a frame that fitted neither.

**Acclimatisation.**

- Kept growing into a division as the loose, common path: a fighter competing above their natural size fills out toward the class over months, and the division-fit penalty eases as they do.
- Added the reverse path so an oversized fighter can recompose down toward their division, but only when the body allows it — cutting skill, conditioning, a smaller natural build and youth all gate it, at roughly half the speed and a third of the frequency of growing. Large-framed, poor-cutting or older fighters stay where they are and are steered toward a move up instead.
- Acclimatisation now runs on fighters carrying no penalty but an oversized frame, which the previous penalty-gated check skipped entirely.

**Tracking.**

- Added a per-fighter `division_fit_log` recording every acclimatisation step with date, division, walk weight, direction and resulting division fit, retained for the last 24 entries and persisted with the career.
- Added a DIVISION ACCLIMATION panel to the fighter profile showing the recent trail and the net frame change over it.
- The profile's Walk Weight row now shows the difference against the division limit, and a new Camp Cut row shows how much the fighter has to lose to make weight.

**Measured effect on the reference career:** every division's frames now sit inside their plausible band (0 out of band, previously 793 fighters more than 25 lb over their limit); newly generated feeder fighters are correct at birth (0 of 400 out of band, previously 252 of 400); with a normal eight-week camp 1,968 of 2,000 fighters make weight on a 10 lb median cut and a median penalty of 0; and the annual division review still applies about one move per promotion per year.

## 3.0.5 - 2026-07-31

### Fight Night Audio

- Added a 0-100 volume slider directly to the live Fight Night viewer; changes take effect on the next cue, persist with the career, and share the same normalized setting as Game Settings.
- Added and integrated a 36-file crowd-audio pack, edited and mastered from licensed real field recordings, with three distinct variants for each of 12 pre-fight, live-fight, finish, and decision trigger families.
- Fight Night now rotates non-repeating variants for arena buildup, walkouts, opening roars, clean strikes, knockdowns, submission danger, inactivity, round endings, finishes, decision tension, split-card boos, and respectful post-fight applause.
- Playback honors the manifest's per-cue gain, limits simultaneous reactions and repeat frequency so commentary stays clear, and falls back to the existing procedural cues when an asset or playback service is unavailable.
- Hometown fighters now receive the strongest audible crowd lift, while national, adopted-home, and training-base appearances receive smaller nearby-market boosts; the Fight Night introduction identifies the active local connection.
- Included source and license documentation, a reproducible mastering tool, and a cue manifest with suggested triggers, loop capability, duration, playback gain, and provenance.
- Reduced the clean-strike vocal reaction by 3 dB and attenuated the gasp layer inside the knockdown roar so short crowd exclamations no longer overpower the broader arena response.
- Preserved every accepted revised-pack mix as Variant 1 and added alternate-source Variants 2 and 3, including modern large-stadium ambience, goal eruptions, hockey outrage, group gasps, boos, cheers, and applause.

### World Locations

- Added Belleville and Kingston, Ontario to Canada's event-city and generated-fighter hometown pool.
- Updated Brett Akey's authored hometown from the province-level Ontario label to Belleville.

### Fight Night Commentary

- Fixed long five-round fights occasionally losing late-round introductions, transitions, summaries, or action commentary when the fight log exceeded its global line limit.
- Replaced global fight-log truncation with per-round commentary compaction that preserves the opening and closing exchanges while always retaining round boundaries, finishes, scorecards, recaps, and fight metrics.
- Applied the fix to both championship fights and non-title five-round main events.
- Added regression coverage for full-distance title and non-title main events, commentary bounds, and a final-tick Round 5 stoppage.

### Interface Polish

- Made ordinary Matchmaking row clicks toggle fighters into or out of the current selection, so players can build pairs and tournament groups without keyboard modifiers; double-click still opens the fighter under the pointer, and the persistent Matchup Insight summary explains the interaction.
- Replaced the low-contrast grey progress treatment with high-contrast loading bars and distinct red/blue fight freshness bars on a dark track.
- Reworked the top status bar so long promotion names use the flexible space available in maximized windows, while popularity, stability, cash, date, and advance controls remain separate and fully readable.
- Added a modal, themed loading panel with phase and progress feedback for Quick Load and Save Manager slot loads, including recovery-snapshot attempts, so large careers no longer appear frozen while their world state and screens are rebuilt.
- Rebuilt Mail / Decisions discoverability with explicit Owner Goals collapse/expand controls, persistent goal summaries, filter-aware message counts, a one-click Show All action, selection guidance, and responsive side-by-side/stacked panels.
- Reworked Add Show / Matchmaking so Show Details keeps a labelled toggle and status summary, the Current Fight Card remains visible beside fighters or above them on narrow windows, and an instructional empty-card state explains the booking flow.
- Compacted expanded Show Details into two control rows and one shared schedule/broadcast status row on wide screens, with medium and narrow layouts that safely stack the same fields, actions, and forecasts instead of clipping them.
- Consolidated fight-card hype, build, fatigue, and medical-return values into one visible booking-information column, eliminating the page-level horizontal scroll that previously hid the card while preserving the complete 20-column fighter table and its own labelled table scrollbar.
- Gave Available Fighters focused Essentials, Readiness, and Form & Fitness table views plus an All 20 view, preserving every scouting metric while making the default Matchmaking workspace easier to scan.
- Reclaimed fighter-table height with a compact, persistent Matchup Insight disclosure and alerts that only occupy space when they contain actionable text; matchup history, booking context, and the row-colour guide remain available through the labelled Expand control.
- Added a Compare Selected action that opens the existing full side-by-side fighter comparison directly from Matchmaking; all five booking actions use one row on wide fighter panes and a safe two-row grid at narrow widths.
- Shortened the fighter-table view cue so it stays on one line while its tooltip retains the full list of available scouting and readiness metrics.
- Fixed vertical splitters locking to the smaller pre-maximized startup height. Mail / Decisions now also reserves a 425-pixel top pane and a fixed two-row action grid, guaranteeing that all eight Inbox buttons appear at startup even if the sash begins at its minimum; later player adjustments remain preserved.
- Removed the full-width `NEW HERE?` alerts. Concise guidance remains beside Inbox counts, Message Detail, the fighter-column cue, and the empty fight card without consuming the vertical space needed by tables and actions.
- Added responsive layout and sash-startup regressions, full inbox-filter reset coverage, and per-theme contrast checks for the remaining inline discovery cues.

### Contract Negotiations

- Rebalanced free-agent contract evaluation so base purse and annualized compensation matter more than raw contract length.
- Added compensation-gated diminishing returns for contract security through 48 months; longer terms provide no additional signing advantage.
- Enforced the negotiation system's 60-month contract limit even when a player manually enters a higher value.
- Added regression coverage for minimum-pay long-term offers, competitively paid contracts, low-cost prospects, and the duration cap.

### Interface Accessibility

- Rebuilt shared notebook-tab states across all 24 themes with WCAG AA text contrast, a minimum 3:1 selected-surface change, larger labels, and redundant outline/elevation cues for the current tab.
- Added automated per-theme contrast checks and a documented tab palette/state guide.
- The main game window now opens maximized, while retaining its responsive fallback geometry for smaller displays and test environments.

### Developer Workflow

- Made synchronized `CHANGELOG.md`, `README.md`, and `AGENTS.md` updates a required part of every implementation improvement or fix.
- Required focused regression coverage for behavior changes, with documented reproducible manual verification only when reliable automation is not practical.
- Added smoke coverage that keeps runtime version metadata and the documented change contract synchronized across the project guides.
- Corrected the README build instructions to distinguish the portable game build from the separately validated Database Editor build.

## 3.0.4 - 2026-07-30

### Veteran Career Integrity

- Reworked late-career decline so it tapers after meaningful losses from a fighter's peak rather than reducing long-serving veterans into implausibly low-rated active fighters.
- Retirement reviews now account for the ability a fighter has lost from their career peak, while a hard review at age 46 prevents indefinitely active veterans.

## 3.0.3 - 2026-07-30

### Regional Championship Booking

- Fixed regional champions being booked in ordinary development bouts between title defenses. Regional titleholders now only compete when the belt is on the line; if no suitable challenger is ready, they sit out.
- Ranked vacant-title participants and defending challengers by divisional merit instead of promoting whichever ordinary development pairing happened to be drawn first.
- Preserved championship stakes in feeder-promotion fight logs so fighter histories correctly identify title bouts.
- Added regression coverage for champion-only defenses, title cadence, contender selection, and archived title flags.

## 3.0.2 - 2026-07-30

### Database Editor And Universe Data

- Rebuilt the fighter Skills tab as an all-skill sheet: each of the 67 individual attributes has a direct labelled 1-99 slider and numeric input, grouped by fighting discipline.
- Added live Current OVR, Suggested OVR, and difference readouts; database authors can apply the entire sheet, synchronize broad core ratings, or use the calculated suggested OVR.
- Authored exact opening detailed skills, career archetypes, Prime Start, and Prime End values for every seeded MMA fighter. The Database Editor and future new games now use the same values.
- Clarified `prime_age` as an optional legend-age override. Normal fighters now display their actual Prime Start and Prime End values and blank optional overrides no longer block edits.
- Hardened seed record lookup so same-name curated variants on different promotions keep their own authored profile rather than accidentally borrowing another variant's values.
- Added a hidden-Tk editor acceptance audit that exercises all database field controls, all 67 skill sliders, skill-sheet persistence, and authored prime windows without saving the database.

## 3.0.1 - 2026-07-30

### Hotfixes

- Fixed the shipped Universe Database Editor failing at startup because its window title referenced an undefined `GAME_TITLE` constant.

## 3.0.0 - 2026-07-30

### Highlights

- Expanded MMA finish logic with a much larger set of submissions, technical submissions, striking stoppages, TKO outcomes, and context-aware broadcast commentary.
- Improved watched-card pacing and fight-night presentation, including complete-card end handling, keyboard arrow navigation, and broader visual theme support.
- Made booking calendar-aware with named event days, recovery and camp time measured in days, and better AI contender availability and scheduling.
- Added player-directed scouting goals, recommendation controls, randomized starting scouts for custom companies, and stronger stat-driven scouting results.
- Added fighter career journeys: academy homegrown-title aims, veteran final runs, discipline and weight-management support, camp-fit work, and champion retention pressure.
- Consolidated the starting universe into one editable database file and shipped the MMA Warriors Universe Database Editor with safer selectors, copying, validation, filters, sorting, and constrained inputs.
- Improved UI responsiveness and dense-screen usability across rankings, profiles, editor tools, themes, and varied desktop resolutions.
- Reworked AI promotion financial stability and simulation efficiency while preserving save compatibility and the existing finance protections.

### Compatibility

- Existing career saves remain supported. New fields have load-time defaults and repair paths where needed.
- The distributed universe database is `Databases\\Default Universe.universe.json`; active saves remain separate and are never edited by the database editor.
- Game & Saves now preserves the selected slot by normalized path identity across refreshes, folder filters and reordered duplicate names instead of trusting a mutable list index. The library shows an explicit empty/filter state and marks entries whose metadata is unavailable, while existing load/copy/delete/backup/recovery handlers remain unchanged.
- Database/world rows now retain selection by their normalized path identity too, keeping Load DB and Use Selected Universe attached to the same file after refreshes.
- Storyline browsing now keys rows by saved thread identity and restores the selected thread after filter/refresh changes; identity-less legacy rows remain explicitly read-only.
- World Chronicle now retains the selected stored entry through type-filter refreshes rather than resetting to the newest story.
- MMA Contracts now restores the selected fighter by identity across filter/refresh rebuilds, keeping renewal/release attached to the intended row.
- Contracts and Combat Sports now share one Show-filter predicate for monthly expiry/final-month versus fight-counted obligations and Non-Exclusive status.
- Rankings now retains the selected mode/company/fighter row by identity across scope and filter refreshes; duplicate identities receive deterministic UI suffixes.
- Matchmaking Available Fighters now retains the selected fighter by identity across filter/date refreshes and fails closed when they leave the visible eligible set.
- Scouting assignment refreshes now use saved talent-search IDs where available, preserving selection and marking duplicate/legacy rows safely.
- Profile Fight History now follows archived bout identity through filters/layout refreshes, with explicit legacy fallback and duplicate-row protection.
- Media Desk reads now use a non-mutating finance envelope: opening or repainting the desk does not seed legacy defaults or migrate plan IDs; explicit plan save/commit remains the ownership boundary.
- Rankings now label Company/World P4P versus Company/World Division explicitly, and P4P rows no longer display divisional movement as if it were a same-scope history.
- Media Rights now includes a read-only broadcaster account review with delivered/owed events, shortfalls, breach strikes, renewal timing and successor status sourced from saved evidence.
- The post-change focused bundle passed 177 tests and the full smoke playtest passed; no EXE or user save was rebuilt.
### Defensive dashboard semantic accents

- Dashboard decision rows now use the active semantic status palette for urgent
  and normal notices, so light and dark themes keep readable contrast.
- Theme switches remap those tags consistently with the rest of the application's
  positive/negative/warning registry; the recorded notice text and routing remain
  unchanged.
- Comeback contracts now write the fighter's ID-linked Company Timeline return
  fact before the retired-to-roster transition, with a stable retry key and no
  change to contract, finance or retirement mechanics.
- Player-owned Combat Sports releases now write child-leave and flagship-join
  timeline facts before updating division roster IDs or booked-bout projections,
  preserving title, ranking and contract behaviour.
- Quarterly Staff progression now leaves malformed skill baselines unconsumed
  for explicit repair instead of crashing or silently losing a qualifying credit;
  valid cap and gain rules are unchanged.
- AI Staff hires now normalise retained salary/term data, stamp contract dates,
  and suffix duplicate staff IDs deterministically before roster insertion.
- Profile Fight History now bounds malformed academy amateur-history containers
  and non-finite month/week values during display. Invalid legacy dates render
  safely without repairing retained rows or changing professional bout/replay
  identity.
- Profile Fight History also fails closed for a malformed professional-history
  container, preserving valid archived rows and showing the normal empty state
  without attempting to parse or rewrite legacy data.
- The owned child-promotion schedule reader now fails closed for malformed
  scheduled-event collections and bounds invalid month/week values before
  display. Saved event identities and explicit schedule/cancel behaviour are
  unchanged.
- Storyline list pagination now fails closed for overflow-sized limits, keeping
  the bounded reader safe without altering stored threads.
- The AI Staff employment ledger now bounds overflow-sized limits and malformed
  numeric employment values during display, leaving hiring, payroll, expiry and
  saved staff data unchanged.
- Talent Relations case readers now fail closed on malformed/non-finite review
  and history dates, preserving case identity and obligations without repair or
  action side effects.
- AI Staff employment reads now fail closed for malformed promotion, staff and
  market collections, keeping the raw source intact and returning an empty
  projection.
- Talent Relations case readers now type-check evidence and obligation lists,
  preventing malformed strings from appearing as character-by-character data.
- Direct Talent Relations case-detail reads now share the bounded review-date
  and obligation projection instead of trusting raw rows.
- Resumable title-miss decisions now fail closed on non-finite/overflow miss
  evidence, preserving the recorded fine and avoiding duplicate scale rolls or
  title/RNG side effects.
- Pending title-miss fine totals are now bounded during resume to prevent
  malformed evidence from becoming an unbounded purse debit.
- Remove Belt now clears interim holders through the canonical vacancy helper,
  preventing stale interim title flags while preserving the sanction record.
- Managed Staff Profile readers now bound malformed salary, tenure-story score
  and reputation evidence rather than failing while opening the profile.
- The AI Staff Ledger now skips malformed affordability snapshots and bounds
  non-finite cash, payroll and runway values during its read-only display.
- Company hub Next Step cards now fail closed on malformed roster/event
  collections and non-finite cash/stability evidence while keeping selection
  and retained company data unchanged.
- Company Hub detail tabs now project complete fighter rows before sorting or
  rendering, and disclose malformed roster evidence without changing saves.
- Selected-company readers now defensively project MMA roster, finance, belt,
  staff and numeric summary envelopes before profile or hub rendering.
- Regional Prospects throughput/backlog readers now fail closed on malformed
  source collections and cached counts without rewriting the monthly cache.
- Regional Prospects candidate assessments now bound malformed/non-finite fighter
  values and safely project promotion identity without repair or RNG side effects.
- Company milestone outlooks now bound malformed/non-finite finance and history
  values before computing cash/event gaps and ETA, with no refresh-time repair.
- Upcoming Cards broadcaster status now projects malformed provider and rights
  values safely, preserving the retained finance envelope during refresh.
- Finance history table and detail readers now bound malformed month/week values
  before formatting, preserving raw identity-bound rows without repair.
- Staff display fields now clamp malformed/non-finite skill, morale and salary
  values, keeping employment rows readable without mutating saved contracts.
- Media Desk dashboard, receipt and campaign readers now bound malformed or
  non-finite retained amounts without repair or RNG side effects.
- Media campaign-plan summary projection now bounds malformed quote/detail,
  completion-ledger and revision fields without mutating the retained plan.
- Media receipt detail now prefers saved contract/outlet identity when linking
  audience evidence, with event matching retained only for identity-less legacy rows.
- Game & Saves selected-save detail now bounds month/week metadata and renders
  an unavailable date instead of allowing malformed legacy values to crash the reader.
- Game & Saves library refresh now uses non-creating autosave/backup enumeration,
  keeping missing save folders absent until an explicit write action.
- Foundation membership history readers now bound malformed paging, as-of and
  result-limit inputs, preserving retained history and avoiding refresh-time
  crashes or unbounded slices.
- Regions profile/team readers now fail closed on malformed gym, promotion and
  specialty rows, with bounded sort keys and no reader-side world mutation.
- World Hub promotion/gym repaint now bounds malformed dates and numeric,
  capacity and specialty fields while preserving identity selection and keeping
  membership synchronization out of the reader.
- Fighter Search now defensively projects malformed source collections and
  record-history fields while preserving debounce, paging, scouting visibility
  and identity-bound selection.
- Results archive/detail readers now bound malformed dates and play-by-play
  containers, keeping unavailable evidence visible without mutating archives.
- UI-data regression coverage is now green at **182 tests** after the latest
  reader-safety pass; the smoke playtest remains green.
- The maintained regression runner now completes every requested isolated suite,
  including Fight Night, traits/profile/persistence, UI-data, Media, Staff,
  finance, scouting/regional, Combat Sports, identity, audio/window lifecycle,
  fight-engine calibration, full-system, database-editor and stability checks.
  The expected headless Matchmaking coordinate probe remains skipped; no EXE or
  user save was rebuilt or modified.
- Matchmaking's stacked laptop layout now reserves a 720px scrollable workspace
  and 520px fighter explorer so filters, actions and four readable rows remain
  reachable; wide mode restores its normal height. UI-data coverage is green at
  **183 tests**, with the expected headless coordinate probe still skipped.
- Academy showcase-card and alumni readers now preserve stable saved identities
  and show malformed retained rows as unavailable evidence instead of crashing
  or routing actions through table positions. Replay, graduate-profile and
  matching-right actions fail closed for unavailable rows; UI-data coverage is
  **184 tests** (217 combined Academy/profile/UI checks), smoke remains green,
  and no EXE or user save was rebuilt.
- Scouting Target Board now clears the selected-target identity, intel, advice
  and market cards whenever filtering or paging removes the selected source row,
  preventing stale scouting evidence after Tk row deletion. The refresh remains
  read-only and preserves reports, shortlists and stable fighter identity.
- Target Board selected-target advice cards now derive recommendation, monitor
  and pass colours from the active semantic palette for readable theme changes.
- Target Board legend swatches now retain semantic keys and update with live
  theme changes, including shortlist and stale states.
- Profile Overview now projects malformed/non-finite pulse and skill metrics to
  bounded display values without changing the fighter; readiness remains the
  inverse of fatigue and scouting-hidden ratings remain hidden.
- Profile comparison picker rows now use saved fighter identity (with
  deterministic legacy fingerprints and duplicate-safe suffixes), restoring
  or clearing selection by identity instead of mutable visible row index.
- Development tab readers now bound malformed score/driver values and retain
  explicit unavailable chronicle rows, preserving the recorded change-order
  plot and avoiding refresh-time mutation.
- Company & Title History malformed rows now use deterministic content/type
  fingerprints rather than mutable visible ordinals for their UI identities.
- The legacy Academy Alumni dialog now uses saved identities or deterministic
  fingerprints with duplicate-safe suffixes instead of list-position targets.
- Region Hub now routes through the defensive non-repairing region projection;
  malformed profile/list rows remain visible as safe unavailable evidence and
  opening or refreshing the hub cannot repair regions or mutate world state.
- Company Proposal Ledger ID-less legacy rows now use deterministic retained
  content/type fingerprints with duplicate-safe suffixes instead of mutable
  visible ordinals; the append-only proposal reader remains read-only.
- Storylines and World Chronicle readers now fingerprint ID-less legacy rows
  from retained evidence/type (including deterministic empty-row keys) rather
  than mutable list positions; malformed rows remain read-only and unavailable.
- Media settlement receipt readers now fingerprint ID-less retained facts and
  fail closed for opaque/stale row IDs instead of resolving the current list
  position; receipt detail remains a pure presentation read.
- Draft-card, booking-slot, regional-prospect, sponsor, World Hub, profile,
  Regions and scouting-pack readers now replace ordinal legacy keys with
  deterministic content/type identities; stale IDs fail closed and refreshes
  remain read-only.
- Finance history/runway/scenario and Awards/title-lineage readers now bind
  legacy rows to recorded boundaries or deterministic retained-content keys,
  keeping historical detail selection safe through reorder and refresh.
- Staff brief, work-receipt and exception readers now bind legacy evidence to
  deterministic retained-data keys with duplicate-safe suffixes; malformed or
  stale rows fail closed and refreshes remain non-mutating.
- Staff roster/candidate/contract and child scheduled-card readers now use
  deterministic retained-data keys when saved IDs are absent; legacy event
  actions fail closed instead of resolving visible positions.
- Talent Relations now registers an explicit `contract_batch_commit` Staff
  action for Full Auto. It targets only a saved Contracts workbench batch,
  preflights conservative quote headroom against player ceilings/reserve, and
  delegates to the existing Foundation-idempotent row owner. Actual row costs
  and structured evidence are retained in Staff work records; recommendation
  modes stay read-only and manual Contracts creation/negotiation is unchanged.
- Fighter Treeview rows now bind to saved fighter identities instead of
  visible list positions across roster, contracts, matchmaking, free-agent,
  region and company views. Duplicate malformed identities receive deterministic
  suffixes while the existing source mappings preserve safe selection/actions.
- Rebooked title-miss bouts now preserve stable fighter IDs and championship
  terms when moved to an existing future card. The original decision evidence
  stays with the cancelled event, the new card gets a fresh weigh-in, and
  ambiguous name-only legacy rows fail closed.
- Last-Minute Replacement candidate lists now use deterministic retained-data
  keys for ID-less fighters, preserving duplicate legacy rows and safe prompt/
  commit routing across reloads.
- Rebook requests without a suitable future card now remain as durable
  `needs_review` entries with one actionable notice and quiet retries, then move
  the same identity-linked title terms once a later eligible card exists.
- Staff retention readers now use deterministic opaque type/content keys for
  empty or malformed legacy rows and restore selection by source object, so
  duplicate unavailable employees cannot be reassociated after a reorder.
- Booking Workbench option and draft-review readers now use saved fighter/
  booking IDs or deterministic retained-content keys with duplicate suffixes;
  visible row positions no longer route review or commit actions.
- Academy amateur-history rows now use saved bout/fight/event identities or
  deterministic legacy fingerprints with duplicate suffixes; malformed rows
  remain visible as unavailable evidence instead of being index-bound.
- Fighter Profile and Company History managed windows now use saved fighter
  IDs or deterministic legacy identity projections instead of process-local
  object IDs.
- Fighter Profile's Fight History column chooser now shares that durable
  identity key, preventing duplicate managed windows or stale chooser routing
  after a profile refresh/rebuild.
- Staff profile compatibility repair now derives missing employee IDs from
  retained employment facts instead of list indexes, preserving identities
  when legacy staff/candidate rows are reordered before load.
- Owner Objective compatibility repair now derives missing goal IDs from
  retained objective facts instead of list indexes, with deterministic suffixes
  for duplicate legacy goals and no change to boundary evaluation.
- Talent Relations case compatibility now derives missing case IDs from
  retained fighter/source/obligation evidence instead of list indexes, keeping
  duplicate legacy cases distinct without mutating the reader's raw collection.
- Followed Story subscription compatibility now derives missing IDs from
  retained target and coverage/creation facts instead of list indexes, keeping
  duplicate legacy follows distinct without changing reader purity.
- Scouting Decision Pack compatibility now derives missing IDs from retained
  pack/candidate facts instead of list indexes, keeping duplicate legacy packs
  distinct without changing the read-only scouting refresh path.
- Manual drug-testing compatibility runs now validate finance, cost and cash
  before sampling or case creation; malformed/non-finite values produce a clear
  deferral with no charge or RNG draw.
- Matchmaking draft move/remove actions now resolve through source-bound fight
  identity or a unique saved booking ID; duplicate equal legacy rows fail closed
  instead of routing to the first row.
- Media story reader navigation now follows source identity or a unique retained
  story ID; duplicate equal legacy headlines no longer jump to the first row.
- Detached story-ID fallback now preserves the original identity field, avoiding
  cross-field matches between legacy `story_id` and `story_key` values.
- Combat Sports running-order actions now resolve the selected bout by source
  identity or a unique saved bout/booking/fight ID; duplicate equal legacy
  rows fail closed rather than using equality-based list indexing.
- Legacy Academy lead/prospect actions now resolve by retained source identity
  or a unique saved `prospect_id`, preventing refresh/reorder drift from a
  Listbox position while preserving the existing Academy workflow.
- Inbox fighter context and action checks now require a saved fighter identity
  or one unique name-only legacy match; duplicate careers no longer redirect
  related context or serious-injury decisions.
- World Hub gym actions now resolve through the retained row identity, with a
  unique-name-only fallback for pre-identity widgets; duplicate gym names fail
  closed instead of opening the first matching gym.
- Simulation Lab tournament fields now use retained fighter identity keys for
  refresh, Draw & Seed and tournament execution; duplicate names cannot route
  a sandbox bout to the wrong career.
- Simulation Lab Red/Blue corner selectors now display disambiguated duplicate
  names and use the retained fighter object for cards, profiles and quick sims.
- Staff department target resolution now prefers fighter IDs and rejects
  ambiguous name-only legacy targets instead of selecting the first career.
- Super-event readiness readers now use saved fighter IDs and safely ignore
  ambiguous legacy name-only participants rather than raising or miscrediting.
- Matchmaking tournament review, TBA fill and title toggles now fail closed on
  unavailable or ambiguous fighter identities and keep the card unchanged.
- Scheduled-card conflict repair and date checks now fail closed on ambiguous
  legacy fighter names, preserving the card while reporting the identity issue.
- Event scheduling, cancellation and immediate-run snapshots now resolve
  participants through stable references rather than display-name lookups.
- Event-atmosphere and championship-value calculations now use those same
  references, safely omitting unresolved or ambiguous legacy corners instead
  of selecting a same-named career.
- Division closure, special-belt vacate/delete actions and card-editor previews
  now resolve saved fighter IDs and fail closed on ambiguous legacy corners;
  duplicate display names cannot redirect a future-bout or title action.
- Fighter Profile's Development tab now labels its monthly outlook as a
  non-guaranteed signal, adds score-band/ceiling-gap context, keeps signed
  drivers on one scale and exposes a compact recent-change ledger from saved
  evidence only.
- Staff evidence auditing now surfaces malformed work/brief collections and
  rows, missing operation IDs and unsupported work statuses as stable findings;
  the diagnostic remains read-only.
- Malformed progression envelopes and credited-work payloads are now reported
  explicitly instead of being treated as empty evidence.
- Yearly play-level audit measurements now retain bounded Staff work-log,
  brief, exception and progression counts, credited-work totals and malformed
  evidence counts without repairing or rewriting Staff state.
- Long-run hard-invariant checks now surface duplicate or missing Staff work /
  operation identities, malformed ledger rows and orphaned progression credits
  as source-bound findings.
- Yearly audit measurements now include bounded Staff payroll totals and
  malformed-salary counts for affordability diagnostics without changing
  finance or employment state.
- Manual Testing Desk commissions now use the saved provider and requested
  sample count, charge the deterministic provider quote, and freeze that
  provider/quote on the case; the established compatibility screening
  resolver and gated J5 sanctions remain unchanged.
- Yearly Play Audit measurements now separate player, AI and owned-child talent
  cohorts, show per-company active divisions and eligible challengers, report
  scheduled-event coverage for each source, and surface missing or duplicate
  fighter/promotion/event identities without repairing the audit world. The
  managed report presents the same evidence.
- Testing Desk cases now preserve negative, preliminary-positive, inconclusive
  and invalid sample outcomes distinctly; review timelines and summaries keep
  unclear/invalid evidence visible without inferring an injury, violation,
  confirmation or sanction. Legacy status-only rows retain their recorded
  meaning without being rewritten during display.
- Profile company timelines now fail closed on malformed membership-reader
  envelopes or rows, preserving the explicit incomplete-history state without
  changing retained facts or redirecting the selected fighter.
## 2026-09-17

- J5 Testing Desk case writes now fail closed for unknown provider/policy IDs
  and cross-wired frozen snapshots, while preserving the legacy `Enhanced`
  policy label and catalogue-backed defaults. Added regression coverage for
  contract identity mismatch and no-mutation rejection.
- ID-less legacy Testing Desk cases now keep `fighter_id` blank and use their
  deterministic `fighter_reference` for filtering and identity; display names
  remain presentation-only so duplicate careers cannot merge.
- Testing Desk configuration and quote previews now show an explicit
  unavailable state for missing provider/policy catalogue entries instead of
  silently presenting the baseline provider or a misleading zero quote.
- Manual testing now validates the saved contract before sampling, charging or
  rolling; unavailable provider/policy state fails closed with no cash, case or
  RNG change.
- Malformed rules/finance envelopes now produce bounded unavailable Testing Desk
  quotes and block manual commission before any mutation.
- Re-ran UI-data/play-audit compatibility coverage; all 222 tests passed.
- Manual testing now stops before mutation when retained case evidence is
  malformed, preserving legacy rows for explicit review or migration.
- Direct Testing Desk case creation now respects the disabled `None` policy and
  cannot create hidden cases while testing is off.
- Retained Testing Cases with unknown or cross-wired frozen contracts now show
  as unavailable and are excluded from spend totals without rewriting evidence.
- Re-ran affected Testing/Staff/UI reader coverage; all 292 tests passed.

## 2026-09-17

- Play-level audits now expose a bounded wall-clock limit in Simulation Lab
  (default 300 seconds, clamped to 30–1,800). Timeouts stop only after a
  completed weekly step, retain a resumable checkpoint, and are labelled
  `time_limit` rather than complete or failed-invariant.
- Title weight-miss prompts now leave preparation explicitly pending when
  dismissed or invalid. No silent Keep Belt fallback, fight simulation,
  settlement, archive or finance charge occurs until the player commits a
  valid action. Added regression coverage for pending decisions and guards on
  opening/settling pending packages.
- Pending title decisions also surface their retained reason in the Results
  status area when a settle/skip action is attempted.
- Focused J5/title/replacement/audit regressions: 101 tests passed; affected
  Staff/UI compatibility regressions: 357 tests passed.
